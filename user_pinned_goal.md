# user pinned goal

###### **只存用户明确认可的内容。每一条加入前都经过弹窗确认；未勾选的候选不写入。**

###### **在使用子Agent进行Review的时候，必须读这份文档作为前提条件。**

###### **必须完全遵循用户认可的目标：**



## 交互设计

1. 传统/SKILL双模式：传统模式下用户可以点按两侧分页栏来展开/收起/切换菜单。SKILL模式下，软件播放动画，变成悬浮窗模式指定显示在屏幕右上角，包含一个迷你时间轴可以让用户拖动或者播放。在底部有一个展开操作按钮，展开之后会在悬浮播放预览下面额外展示一个Agent修改预览窗口（和AI菜单中的修改预览窗口一致）。在悬浮窗的左上角有图钉（始终置于顶层，默认状态）。还有放大（展开完整窗口）。桌面端工具在调用的时候有自己的识别码，所有两侧菜单中可以显示桌面端Agent的操作/详细修改预览。但是消息框右下角没有发送按钮，相应的，显示为一个居中在消息框的按钮“回到Cluade（ChatGPT）对话”。
2. 整体使用圆角风格。应用典型的Microsoft Fluent Design（微软流畅设计系统）规范下的现代深色生产力工具风格（Dark Mode Studio UI / Flat 2.0）。
3. 软件的主题色是高饱和度青蓝色和低饱和度酱紫色。



## 架构设计

1. **云端拆两块**：文档云端（WebSocket：项目信息、单词操作块的接收/分发、锁、广播操作被覆盖的通知、卡片源码）+ 素材云端（H264流式传输，先传800\*600小分辨率版本/完成以后传原素材版本。可以分包传输，要支持断点续传）。
2. **同时支持本地/云端种素材管理模式**：同时支持本地和云端两种访问，本地模式直接访问文件、跳过素材云端。但文档云端还在，文档云端运行在本地，因为它负责管理所有的修改请求。
3. **编辑器分为在线浏览器模式和本地模式**：在线浏览器模式没法请求到预渲染进程，所以只能通过离屏的舞台来在空闲时间预渲染。但是可以取到云端的HTML加载热舞台。（将来我们会1.在素材云端为在线浏览器模式留口子，其它本地用户可以把渲染好的结果上传到云端，云端合并所有用户预渲染结果。并负责分发。2.给浏览器模式配专门的在线渲染重型控件服务。请为这些内容留接口。）。本地模式可以请求预渲染进程。后台 iframe 的预渲染角色本任务就实现。
4. **预渲染进程分Agent，User，Full三种模式**：Agent模式（只有Agent在本地\[托管在服务器本地]）不预渲染，只查询精确某帧。User模式（只有交互编辑在本地）只预渲染重卡片。Full模式（交互编辑，Agent都在本地）优先Agent查询，其次再进行预渲染。\[交互要求的及时渲染挪到了播放预览舞台，所以预渲染不需要服务用户的即时交互请求，只需要在闲时完成用户渲染任务]。Agent 请求只在一个专用 Chrome 中走，「优先」只是可以插队、不是进程优先级更高：普通 Chrome 按先来后到处理预渲染请求；专用 Chrome 每做完一个任务，先从 Agent 任务队列里按先后取下一个；Agent 队列空时接普通预渲染任务，不独占，只是 Agent 请求优先（不打断正在跑的那个）。Agent 任务永远不排在排队中的预渲染任务后面，也永远不占用别的 Chrome，所以 Agent 并发占不满预渲染资源、用户永远等得到预渲染。Agent的query请求跑在预渲染进程。用户交互的query请求跑在自己的离屏幕异步舞台。预渲染跑在预渲染进程（但是给Agent留一个Chrome的优先通道）。
5. **AI 菜单的操作预览可以插队**：AI 菜单（聊天气泡）的操作预览请求是用户触发的，可以插在预渲染任务前面：在普通 Chrome 的队列里排到所有待预渲染任务之前，不打断正在跑的那一批，也不占用 Agent 的专用 Chrome。
6. **用户播放预览窗口**：按帧走、慢帧等、不冲。舞台每渲完一帧才报一次 t。用户不能在拖动时间轴/点击时间轴来随机访问的时候看到motion（CSS）类动画控件的推帧过程。
7. \*\*帧率：\*\*最高支持60帧。要支持在项目选项中可以切换帧率（包括24/25/30/60）。项目选项面板加 fps 下拉（24 / 25 / 30 / 60），所有步长按 1/fps；新项目默认维持 30。
8. **用户交互界面**：store跟随t。用户交互界面无论是拖动时间轴；点击时间轴；播放窗口在播放中。都不可以有卡顿，延迟。
9. 暂停时使用活渲染：播放暂停时/用户点击时间轴某时间/用户拖动时间轴后松开按键，所有组件都是精确的活渲染。
10. **音频播放：音频跟着 t（停顿超过约 40 毫秒就暂停音频、恢复时对齐）。** 阈值固定 40 毫秒，不按帧算。



## 卡片划分

轴一：帧怎么得到

1. 随机访问（direct）：React 按 t 直接算出画面，跳到任意帧代价一样。
2. 可定位的推帧卡（stateful 且 seekOk: true）：CSS @keyframes / transition、WAAPI、Motion 的 transform / opacity 动画。它们本质是文档时间线上的动画，能用 currentTime 直接钉到目标帧，一步到位。
3. 只能逐帧推的卡（stateful 且 seekOk: false）：Motion 的 spring / layout 动画、AnimatePresence、粒子、任何自己累积状态的 JS。能用子树虚拟时间推的在可见舞台里追，读全局帧循环时间戳的对象可以通过覆盖返回值解决。

如何区分SeekOK：从第 0 帧一帧一帧推到第 8 帧，冻结控件 HTML（含内联的计算样式）。复位后，一步把子树钉到第 8 帧（钉 WAAPI 动画的 currentTime + 组件 t 直接给 8），再冻结。比对：两份 HTML 标签、属性、文本逐字比，样式里的数值允许 1e-6 相对误差；相同就是SeekOK。



轴二：画在什么上

1. DOM 卡；
2. canvas 卡（2D canvas / three.js WebGL）。



轴三：依不依赖下层

1. Independ独立卡：只画自己，可以单独预渲染、上云共享；
2. 后处理卡：毛玻璃、滤镜、转场，画面结果受其它的图层影响。SourceDependent:依赖控件（向上追溯整条链路）的参数哈希发生任何改变就失效。BDdependent: 预渲染结果在同时间下，下层控件参数哈希变了都失效。



## 卡片渲染

1. 总规则：用户交互不允许有任何卡顿，舞台渲染和用户交互操作之间异步。（加载阶段的测量遮罩除外，见本节第 5 条）
2. **卡片的轻/重分类**：靠实测，不靠声明。1.对于不可以随机访问的卡（例如motion等，需要推帧的CSS动画卡片），每张卡加载时在离屏舞台从第 0 帧推到最后一帧，记录推帧过程中最慢一帧的耗时 t\_m\_c（单帧最差）。如果$t\_m\_c$大于$1000/FPS×70%$可以直接归为重卡（必然是重卡）。2.对于可以随机访问的卡，预先随机访问几帧（固定随机抽 8 帧），找到最差的一次作为耗时 $t\_c$ 。如果$t\_c$大于$1000/FPS×70%$可以直接归为重卡（必然是重卡）。
3. 代价优化函数：按照$1000/帧率（FPS）×70%$来计算，使用贪心搜索，优先吸纳单帧最便宜（frameMs 最小）的卡入轻管线。
4. 代价优化函数-长motion：长motion在探针或者预渲染的时候，可以记录单帧渲染最差时间$t\_o\_c$。播放的时候按照播放时间点(t)踩在哪些motion卡片上来计算。（$$t\_c = (t - t\_{\\text{start}}) \\times \\text{FPS} \\times t\_{oc}$$，因为motion类控件走的是虚拟时间）。
5. 代价探针：每次打开项目，在加载（转圈页面）的时候测所有卡片。新添加的卡片及时在离屏舞台测。字面：全部测完才进。可以贪心搜索，如果一个卡太慢了直接就抛弃算为重卡（超过了1000/FPS×70%）。记得加上截图orHTML保存花费时间t\_s\_c。而且跑的时候就可以存下来当作预渲染。
6. **实时加载管线**：轻 React 动画、轻 JS 滤镜转场、短 motion /CSS动画(跳转就当场从第 0 帧逐帧推过去、中间态不画出来，不需要进入HTML 打包管线)永远活在热舞台里。实际播放依然记录每个控件的加载时间，一秒窗口内累计超时（每拍耗时超出 $$1/FPS$$ 的部分之和）超过 $$1/FPS$$，就把最贵的那张轻卡降为重卡。长 motion 不可见地追、追上再显示。
7. **预渲染管线**：重 React 动画、重 JS 滤镜转场、长 CSS。异步离屏渲染成死素材，每个控件单独保存、只保实体部分其余透明；播放和拖动都只贴死素材。死素材缺失时该层透明、播放头不停。
8. **轻/重按位置动态算**：项目加载只是算一次时间代价，不再区分轻/重卡片。轻重卡片计算的位置是动态的，取所有卡片开始/结束位置的并集作为分段边界，在每一个位置分别计算该位置的轻/重卡片集合。最后预渲染中的重卡片取所有位置重卡片集合的并集；热舞台在某个位置的重卡片取该位置算出的重卡片集合，轻卡片取该位置算出的轻卡片集合。
9. **预渲染只做重卡并集**：在所有位置都判轻的不产快照、不产流、不进就绪索引；任一位置判重的整段完整预渲染。
10. **预渲染的两种形态**：对于播放时刻，使用每个重控件一条的上下拼合组成的 alpha 的 H.264 流（裁到实体框）给播放。活跃的流超过硬件解码器预算时保留合并退路：预渲染把相邻重卡合并成一条组流。对于暂停和拖动，每个重控件一份 HTML 快照（canvas 已栅格成带实体框的 img，只含该控件子树）。canvas 卡走共享 WebGL 渲染器：后台只开一个 WebGL 上下文，在交互界面的舞台 iframe 里开的一个 Web Worker（浏览器线程）。每张 canvas 卡自带 GLSL、各编各的 program（并行编译、按哈希缓存），素材纹理只上传一次；每拍主线程只传一个 t，Worker 把所有 canvas 卡一次画进一张图集，按卡裁成位图交给各自的 canvas，图层关系仍由舞台按 z 序拼接；画完回 done 主线程才推进一拍（按帧走、走虚拟时间）。

针对用户提出的内容分为
1.修改建议（额外添加内容/具体化goal）
2.当前局限性。
添加到文件reply\_to\_users\_goal.md
---



###### **以上为用户的目标不可修改。**

