# PromptCut

本文件是 coding agent 的入口，不写规则。

本仓库给 coding agent 的全部规则在 `docs/semantics/04-agent-guide.md`，开工前先完整读它。产品、架构和用户工作流程的语义在 `docs/semantics/` 下，与代码冲突时以语义为准（04 原则 2）。

下面一行是 Claude Code 的引入语法，会把 04 自动载入；其它工具按上一段去读即可。

@docs/semantics/04-agent-guide.md
