# PromptCut

本文件是 coding agent 的入口，不写规则。

给开发者的规则从 `docs/semantics/developer_guide.md` 进入：它是索引，规则在 `docs/semantics/guide_files/` 里。开工前先读索引、`guide_files/suggested_agent_behavior.md` 和 `guide_files/constraints.md`。产品、架构和用户工作流程的语义在 `docs/semantics/` 下，与代码冲突时以语义为准（`suggested_agent_behavior.md` 原则 2）。

下面三行是 Claude Code 的引入语法，会把这三份自动载入；其它工具按上一段去读即可。

@docs/semantics/developer_guide.md
@docs/semantics/guide_files/suggested_agent_behavior.md
@docs/semantics/guide_files/constraints.md
