/**
 * 右栏:AI 助手聊天面板。助手通过 MCP 工具读写项目(见 server/ 和 src/ai/)。
 * 【占位实现,AI 任务负责填充。对外只暴露 RightPanel 一个组件,不要改这个导出名。】
 */
export function RightPanel() {
  return <div className="h-full p-3 text-sm text-neutral-400">AI 助手(待实现)</div>;
}
