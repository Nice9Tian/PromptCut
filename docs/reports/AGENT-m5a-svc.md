# M5a 文档服务侧实现报告（svc）

分支 `claude/rq-m5a-svc`，基于 `claude/rq-m5a`（b17c50a）。依据：`docs/plan/render-queue-contract.md` G 节（G.1～G.6）。

## 进度

- [ ] 核心 `router.mjs`（G.2、G.3）
- [ ] 鉴权 `auth.mjs`（G.5）
- [ ] 队列模块 `modules/render-queue.mjs`（G.4）
- [ ] 登记模块 `modules/endpoints.mjs`（G.6）
- [ ] 组装层 `service.mjs`、`ws.mjs`（G.4、G.5）
- [ ] `main.mjs`、`ecosystem.config.cjs`、`scripts/remote/docservice.mjs`（G.5）
- [ ] 收尾基线

## 做了什么

（进行中）

## 验证

（进行中）

## 契约疑点

（进行中）
