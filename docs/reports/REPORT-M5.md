# M5 总验收报告

2026-09-25。M5 按主执行计划拆出的四段 M5a、C5、C6（本轮范围 C6.1～C6.4）、M5b，全部合入 main；最后一次合并是 `f9660d3`。

- 计划：`docs/plan/Master-Execution-Plan.md`（第 11 节是权限边界与推迟项）
- 各段的报告：`REPORT-render-queue-m5a.md`、`REPORT-c5.md`、`REPORT-c6-1.md`、`REPORT-c6-2.md`、`REPORT-c6-3.md`、`REPORT-c6-4.md`、`REPORT-render-queue-m5b.md`
- 语义改动：`95f9aa7`（S1、S2、文档服务定位，用户逐条确认）

## 1. 交付了什么

| 段 | 合并提交 | 内容 | 跨机验证 |
|---|---|---|---|
| M5a | `8c0cc08` | 文档服务拆成通用核心与模块；集群令牌鉴权；服务地址登记；节点侧 WebSocket 端点、断线重连、端点解析 | W1：X1～X8 全过（握手、鉴权、抢活、双向、干净断开 15.07 s 被接手、半开 34.00 s 被接手、远端重启、地址下发） |
| C5 | `cc60741` | 素材服务数据层 `BlobStore`（fs、memory、OSS 插槽）；非本机写入要集群令牌；局域网地址登记 | W2：笔记本经控制面拿到主 PC 素材服务的地址，完成分片、续传、Range、sha256 校验 |
| C6.1 | `2180491` | 核心的频道 API、出站队列与背压（合并、1013 关闭）、队列摘要订阅 | — |
| C6.2 | `cc135d6` | 素材服务的 `snap` / `px` 命名空间与客户端；任务清单随 `task.done` 下发；拉取经写入函数落盘并发布 | 并入 W4 |
| C6.3 | `4665995` | 文档服务本体最小版：项目版本、内容库、日志存储、挂载模式、本地插件（升级防崩、无头实例停用） | — |
| C6.4 | `c7bfe5b` | 按段拆键的清单进内容库、跨节点去重、A5 优先级的无条件推送、换机取用 | 并入 W3 |
| M5b | `f9660d3` | 指纹前置过滤（防锁风暴）、项目快照、真实执行器、队列模式的预渲染进程、按 D7 选素材服务 | W3：换机 0 次渲染，拉取 240 帧；W4：笔记本真实渲染 3 个任务，主 PC 拉回并就绪，本机节点 0 个任务 |

## 2. 基线（main `f9660d3`）

- `npx tsc -b --force`：退出码 0。
- `npm test`：2445 条，2444 通过，0 失败，1 跳过（需要 5190 dev server 的集成测试）。M5 开工前是 2194 条，新增 251 条。
- **导出像素**：每个动到渲染的阶段（C5、C6.2、C6.3、C6.4、M5b），都跑了 `verify-determinism`（1800/1800）与 `verify-unified-frames`（PASS）。与 main 逐像素对比均为 0 差异，M5b 在队列开关开、关两种状态下都比过。
- **测试稳定性**：查出本机「坏端口」的根因并修复，连跑 20 遍 0 失败。

## 3. 推迟项（都在 M5～M8 之内排好了，见计划第 11.2 节）

| 项 | 排到 |
|---|---|
| C6.5：D1、D2、D4，操作格式与撤销语义（先写设计稿交用户审） | M6 之后、M7 之前的专项 |
| C6.6：两档素材与换档、卡片源码同步 | C10 之前 |
| 页面直接发布 `plan`；内容库写入区分会话 | 随 C6.5 |
| 轨道流走队列；没有哈希的素材的能力闸；`watch: 'all'` 收紧；`plan` 指纹过滤认领；闲时门槛；style 顺序确定化；`readSpill` 异步；远端卡的 PNG 缓存；探针汇总字段 | M6 |

## 4. 需要用户知道的

- **5190 编辑器要重启才生效**：用户常驻的 5190 编辑器重启后，会自带本地文档服务（`/docservice`）。队列模式缺省关闭，推送只在显式开启时生效（J.12），日常行为不变。
- **远端控制面已部署到 M5b**：`8.219.80.16:8787`，模块有 render-queue、endpoints、project、content。
- **集群令牌**：存在主 PC 的 `.env.cluster`（已被 git 忽略），也经跨机消息发给过笔记本，建议轮换一次。
- **8 份子 Agent 报告还留在仓库里**：`docs/reports/AGENT-*.md`，这次没删，因为删除命令被本机的安全钩子拦下。
- **下一步是 M6**（独立渲染主机）。笔记本停在 `claude/rq-m5b` @ `58fe54c`，待命。
