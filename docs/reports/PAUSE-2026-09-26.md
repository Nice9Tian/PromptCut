# 暂停交接（2026-09-26，用量闸）

本周额度（Weekly · all models）到 91%，按主执行计划第 10 节「用量闸」暂停（用量闸已于 2026-09-26 第二轮删去；恢复顺序以下文第 3 节为准）：
- 已派出的子 Agent 全部做完，结果已经收进来；
- 不再派发新的子 Agent，等用户通知再继续开工；
- 额度重置了也不自动开工。

## 1. 已完成并合入 main

| 阶段 | main 提交 | 报告 |
|---|---|---|
| M6（M6a 凭证与票据、M6b 独立渲染主机、M6c 推迟项） | `ac551a8` | `docs/reports/REPORT-M6.md` |
| SP（阿里云托管组合、局域网发现） | `685756a` | `docs/reports/REPORT-SP.md` |
| C6.5（项目真身进文档服务、路径操作与撤销、Agent 直接写文档服务、D11 界面） | `2ffaa45` | `docs/reports/REPORT-C6.5.md` |
| main 上 `ready-index-probe` 第⑨步修复（codex） | `8482e21` | `docs/reports/CODEX-ready-index.md` |

- main 当前 `f866754` 之后只加了本文件，基线全绿：全量测试 2951 条，0 失败。
- 阿里云 `8.219.80.16` 跑的是 C6.5 版的托管组合（`promptcut-hosted`，8787 / 8788）。

## 2. 进行到一半：C6.6（两档素材与换档、卡片源码同步）

设计稿 `docs/plan/c66-design.md` 已发给用户（发给用户但不等）。四个子分支都已推送到 origin，**还没有集成、没有合入 main**：

| 分支 | 最后提交 | 状态 |
|---|---|---|
| `claude/c66-tiers` | `1af3e5c` | 完成：素材小尺寸生成、素材原尺寸重封装、持久上传队列、带宽闸；全量 2967 条 0 失败，G0-R 全过，`tiers-probe` 通过 |
| `claude/c66-fetch` | `0b87440` | 完成：按需拉取、预取、页面轮询与双缓冲换档、可播性、导出拦截（页面侧）；全量 2989 条 0 失败，G0-R 全过；换档探针 4 次中第 4 次 T5b 帧误差 3 帧（偶发） |
| `claude/c66-cards` | `b0425b2` | 大部分完成：卡片源码经内容库同步，单测 8/8，全量 0 失败；探针的「B 端装上新卡后自动重测」连续 2 次没过（停手待 codex）；G0-R 没跑 |
| `claude/c66-tests` | 见分支 | 完成：C66-T1～T8 对抗式用例 34 条（假设接口集中在 `c66-kit.mjs`） |

## 3. 恢复后按顺序要做的事

〔2026-09-26 第二轮改写，取代原来的顺序〕角色与通道见主执行计划第 6 节：笔记本会话是主会话，云端会话是渲染节点 Worker，PC 是间歇在线的辅助测试节点，主会话与云端经阿里云上的 HTTP 信箱通信，与 PC 经桌面 App 的跨会话消息通信。

1. **合入信箱**（`claude/coord-mailbox`）：
   - 先按 `docs/plan/http-transport-contract.md` 第 15 节在文件头补等待的说明（队列不变）；
   - 合入 main，阿里云上的 `probe-coord` 用新版本重启（`mail.jsonl` 保留、令牌不变）。
2. **HT：文档服务的会话与两种传输**（主执行计划第 7 节 HT）：
   - `claude/http-transport`（第 1 版 `7477acd`）按契约第 2 版返工，另开 `claude/ht-client`、`claude/ht-tests`；
   - 部署到阿里云（本机信任开关、公网地址参数、nginx `/hosted` 两项）；
   - 跨机 W-HT：云端经信箱接入，作为渲染节点认领并完成真实任务。
3. **C6.6**：
   1. **codex 攻坚两项**（worktree 模式）：
      - `claude/c66-cards`：B 端装上新卡后自动重测没发生。先查页面有没有收到 HMR 通知 `pc:card-sync`（截图上没有「已同步」气泡）。
      - `claude/c66-fetch`：播放中换档 T5b 偶发误差 3 帧。实现方的建议：比较对象改用前台帧回调的 `expectedDisplayTime`，外推到同一显示时刻；探针改为逐帧记录。
   2. **集成**（一个集成代理）：
      - 合并四个分支，对账 `c66-kit.mjs`；
      - 落实主会话已定的裁定：
        - 页面把远端素材服务地址和 rw 票据交给上传队列（`/api/media/upload-queue/target`）；
        - 打开项目时，桌面版给缺素材小尺寸的视频在后台补转，补写 `tiers.small`；
        - 服务端 `/api/export` 接上素材原尺寸未到齐的拦截（`POST /api/media/originals` 已有）；
        - 设计稿第 5 节补「本机写过的版本被覆盖时也先备份」（已在 c66-cards 实现）；
        - 卡片代码变了要显式触发重测，写进设计稿。
      - 跑 G0-R。
   3. **部署到阿里云，跨机 T9**（新写法，`docs/plan/c66-design.md` 第 6 节）：笔记本以创建者身份跑桌面版（导入、素材小尺寸、预渲染），项目放云端；云端用无头 Chrome 以成员身份当观察端，断言素材先小尺寸后原尺寸出现、卡片源码自动装上；云端不当渲染节点。放本机版 T9 是 PC 窗口项。
   4. **合入 main**，写 `REPORT-C6.6.md`。
4. **C10a 在线浏览器模式 demo 最小可用**（主执行计划第 7 节 C10a）：托管端 `/editor`、开始页的加入表单、「多用户协作」勾选（放云端）、低内存档、预渲染小尺寸；验收是手机扫码加入并看到两种小尺寸、改一处由笔记本重渲回到手机。
5. **C10 其余**：
   - 先让 Gemini 出用户侧流程稿（离线提示、素材导入置灰）；
   - 查资料：IndexedDB 配额、明文 http 页面没有 `crypto.subtle`，用纯 JS 派生兜底；
   - 放本机的项目（局域网直连、手填主机地址）是 PC 窗口项。
6. **M7 纯浏览器节点**（W7）。
7. **M8 多端联调**（W8）：
   - 含异地接入托管、在同一台服务器上起第二份实例做迁移演练、混沌项；
   - 放本机的项目与连接路径用例在这里**必须 PC**；
   - 最后出全案验收总报告。

## 4. 要在 M8 之内修的已知遗留

- `create_card` 带 overwrite 时，新内容会被改动层里的旧版盖住（c66-cards 发现，既有问题）。
- 热更新让 App 重新挂载、重新弹出「选择 AI 助手」对话框；在共享项目里，别人每改一张卡，本端就弹一次（c66-cards 发现，既有问题）。
- 聊天记录列表没有另做窗口化（C6.5 遗留）。
- codex、agy 两路拿不到 `callId`，它们的调用只能在 Agent 操作记录里撤（C6.5 遗留）。

## 5. 环境现状

- **本地**：main 工作区干净；C6.6 的四个 worktree 保留在 `.worktrees/c66-*`，都查过没有 junction。
- **笔记本 Worker**：停在 `claude/c65` @ `f892a17`，待命。（2026-09-26 第二轮：笔记本改为主会话，见主执行计划第 6 节。）
- **阿里云**：`promptcut-hosted` 在线；pm2-logrotate 已装；UFW 放行 22、8787、8788。
