# SP 集成对账报告（claude/sp-integ）

状态：进行中。

worktree：`.worktrees/sp-integ`，分支 `claude/sp-integ`（已合并 claude/sp-hosting、claude/sp-routing、claude/sp-tests 与 claude/m6 的 W5 探针跨机模式）。
端口：5470～5499、8790～8799。没推送、没合并、没部署远端、没装依赖、没建 junction、没跑 npm ci。
