# reply\_to\_users\_goal

对 `user\\\_pinned\\\_goal.md` 里 16 条目标的回应，按用户要求单独放在这个文件：1. 修改建议（额外添加内容 / 具体化 goal）2. 当前局限性。这里的内容不是约束，可以自由改；被用户认可的条目再经弹窗钉进 `user\\\_pinned\\\_goal.md`。任务书当前是第 65 版，18 条目标已全部钉齐（2026-09-17 加了第 17 条「轻/重按位置动态算」、第 18 条「AI 菜单的操作预览可以插队」），审查环路从第 50 轮重开、上限 9 轮（到第 58 轮）。审查员可以对用户目标提建议，写在最后一节，由用户决定是否钉入。

### 建议

###### 添加内容（任务书据以落笔的具体化，按目标编号）：

1. **目标 1 云端拆两块**：文档云端，一条 WebSocket（项目、操作、锁、覆盖通知、卡片源码、快照清单）；素材云端，800×600 以内的小分辨率版先传、原片后传。
2. **目标 2 本地 / 云端模式**：代码路径完全一样，只差文档服务的端点和素材层的来源；本地模式文档服务跑在编辑器进程里、只写操作日志，`.proc` 仍由页面在拿到 ack 后写；模式切换是显式操作，编辑锁只盖住「整份推送 + 版本切换」。
3. **目标 3 在线浏览器模式**（任务书目标 L）：后台 iframe 当预渲染者（闲时按锚帧优先逐帧产 HTML 快照，用户一操作就让路）；快照存 IndexedDB，和本地模式共用同一个 SnapshotSource 接口；云端快照按共享键拉下来直接进热舞台；浏览器里没有流，重卡播放按拍换 HTML 快照；云端合并分发（`POST merge/...` 回 501）和在线重型控件服务只留接口。
4. **目标 4 三种模式**：`agent` 只建 Agent 队列、不预渲染、按需拉一个 Chrome 查精确帧；`user` 只预渲染重卡，不建 Agent 队列；`full` 一个进程两条队列都建，Agent 独占一个 Chrome——「优先」只是可以插队、不是进程优先级更高：专用 Chrome 每做完一个只从 Agent 队列按先后取下一个，其余 Chrome 按先来后到做预渲染、不为 Agent 暂停；用户交互的查询（实体框、命中）走页面自己的后台舞台，不打预渲染；`PROMPTCUT\_PRERENDER\_SPLIT=1` 可拆成两个进程（可选）。AI 菜单的操作预览（聊天气泡点开的动图 / 位图）是用户触发的请求，在普通 Chrome 的预渲染队列里插到最前、不占 Agent 的专用 Chrome。
5. **目标 5 播放节拍**：父页发 play / pause，舞台每拍 `advanceTo` 一步（步长 1/fps）、等一次真实 rAF、补一拍落定、post `{ type: 'frame', sec }`；封顶项目 fps、慢帧等、不跳帧；播放到头 post `ended`。
6. **目标 6 帧率**：项目选项面板加 fps 下拉（24 / 25 / 30 / 60），所有步长按 1/fps；新项目默认维持 30。
7. **目标 8 暂停态精确活渲**：点时间轴或松开后发 `setTime(t, { settle: true })`。能用子树虚拟时间推的卡（探针测出的 `vtOk`：CSS / WAAPI 动画、Lottie、粒子）在可见舞台里自己追——给那张卡的子树一个本地时间、在快照平面后面逐步推、全局时钟不动、追到就摘平面；推不动的卡（Motion 的 JS 驱动动画只认全局帧时间戳）才由后台舞台整场景补跑到 t、补完两个舞台互换。再拖动、播放或改项目就中止。
8. **目标 9 音频**：阈值固定 40 ms；通道是父页的 `mediaStalled` 状态（直接 `pause()` 会被下一拍的同步逻辑撤销）。
9. **目标 10 / 11 / 12 探针与分派**：每张卡测两个数和两个布尔（`vtOk` 能否用子树虚拟时间推；`seekOk` 能否直接定位不用逐帧推——特别简单但很长的 motion 就是 `seekOk`，跳转一步到位、不走预渲染也不追帧）——`frameMs`（单帧最差，含冻结本控件 HTML 的时间）和 `catchUpMs`（挂载帧到最后一帧的估计）；随机访问卡固定抽 8 帧；推帧卡从第 0 帧一直推到最后一帧、累计超过一拍就截断（已推的帧照记，没推完的按平均估整段）；单帧最差超过预算才标 `capped`、判重；探针推过的帧直接存成死素材；预算 B = 1000/fps × 0.7，按 `frameMs` 升序贪心，重卡每拍扣 0.3 ms；加载阶段只算时间代价、不分轻重，轻重按位置动态算：分段边界是所有卡入点 / 出点的并集，每个位置各算一次；预渲染各位置重卡集合的并集（整段预渲染），热舞台在某位置的重卡、轻卡就是该位置算出的两个集合；加载遮罩 ProbeGate 盖住编辑器、两个舞台 iframe 照常先挂，costs 全命中时遮罩不出现。
10. **目标 13 轻管线**：`catchUpMs` 一拍内的卡跳转当场逐帧推（帧间只让微任务，浏览器不绘制，中间态看不到）；长 motion 若 `vtOk` 就在可见舞台里用子树虚拟时间不可见地追（每拍最多多推 4 步、成本计入预算），追上再显示；`vtOk = false` 的由后台舞台整场景补跑后在目标拍互换；追不上就改判为重；再平衡按一秒窗口累计超时 > 1/fps 降一张，按实测最贵的降。
11. **目标 14 / 15 / 16 重管线**：预渲染只做 K2 判重的（分段, 片段）；播放贴每控件一条上下拼合 alpha 的 H.264 流、拖动贴 HTML 快照、缺失时透明播放头不停；流数超解码器预算时合并相邻重卡成组流（退路）；抑制与 `streams` 开关无关，开关只决定有没有流平面。
12. **故事板档（可选，未钉）**：每秒一格的 JPEG 拼图给时间轴缩略图和拖动预览用；目标 1 只有两档，任务书里没放。

###### 修改内容：

（16 条全部拍板，暂无待议项。以后有新分歧写这里。）

### 局限性

1. 重卡播放是 H.264 视频画质，文字边缘不如活渲；暂停下来后各卡自己追（或后台整场景补跑再互换）才是精确活渲，等的时间取决于最长那张重卡的补跑时间（长卡几秒），期间可见舞台贴死素材。Motion 里用 JS 驱动的动画（transform 这类）子树虚拟时间推不动，只能走后台整场景补跑那条慢路。
2. 硬件解码器同时只能开约 4～8 路，重卡再多就合并成组流；GPU 进程是所有渲染进程共享的，重画布多了仍会拖慢编辑器的合成，进程隔离挡不住这一层。
3. 推帧卡的探针在一拍内被截断时，整段补跑时间是按已推帧的平均估的，估计偏乐观时长 motion 会被误判为「追得上」，靠播放中的累计超时再降级。
4. 死素材要预渲染先产出来：刚改过参数的重卡在预渲染追上之前那一层透明。
5. `full` 单进程下 Agent 有一个优先 Chrome（同优先级、只是插队；用户 2026-09-16 澄清：Agent 没任务时它接普通预渲染任务，不独占），Agent 请求最多等该 Chrome 上正在跑的那个预渲染任务结束；机器核数少时 Agent 任务和预渲染仍抢 CPU；拆分成两个进程能缓解但多一个 Chrome。
6. 在线浏览器模式没有流：重卡播放按拍换 HTML 快照，每拍一次 innerHTML 替换，重卡多时预算装不下就透明；素材只有原片一档（没有转码）；云端合并分发和在线重型控件服务只有接口。
7. 没声明能力的卡（unknown）没有死素材，暂停后在原地追帧、过程可见；审计要求这类卡为 0。
8. 单流时间分层（同一份编码先传参考帧再补 B 帧）技术可行但收益小，留待将来；现在是两档换档。
9. 跨源 iframe 的进程隔离在 Chrome 152 实测通过，WebView2 上尚未复测。
10. 没有变速播放。

### 已在对话中拍板、已按原话写入 user\_pinned\_goal.md（2026-09-16）

* 第 3 条：后台 iframe 的预渲染角色本任务就实现。
* 第 4 条：Agent 的 query 请求跑在预渲染进程；用户交互的 query 请求跑在自己的离屏异步舞台；预渲染跑在预渲染进程（给 Agent 留一个 Chrome 的优先通道）。
* 第 4 条（2026-09-17 替换末句）：「优先」只是可以插队、不是进程优先级更高——普通 Chrome 按先来后到做预渲染，专用 Chrome 每做完一个只从 Agent 队列按先后取下一个（有才取），Agent 任务永远不排在预渲染任务后面、也不占别的 Chrome。
* 第 6 条：新项目默认帧率维持 30。2026-09-17 又加入「项目选项面板加 fps 下拉（24 / 25 / 30 / 60），所有步长按 1/fps；新项目默认维持 30」。
* 第 9 条：音频阈值固定 40 ms。
* 第 10.2 条：随机抽 8 帧取最差。
* 第 11 条：长 motion 记录渲染速度 ×70% 和单次渲染最差时间；播放时按播放头踩在哪些 motion 卡上算；追不上当死素材，追得上先不可见地推、追上后显示（不超总预算）。
* 第 12 条：全部测完才进；贪心，超过 1000 ms / FPS 的卡直接判重；计入截图或 HTML 保存时间；跑的时候存下来当预渲染。
* 第 13 条：跳转当场逐帧推、中间态不画出来；再平衡按一秒窗口累计超时算。2026-09-17 又加入「长 motion 不可见地追、追上再显示」。
* 第 15 条：死素材缺失时该层透明、播放头不停。
* 第 14 条（2026-09-17 采纳审查员建议改写）：在所有位置都判轻的不产快照、不产流、不进就绪索引；任一位置判重的整段完整预渲染。
* 第 16 条：流数超硬件解码器预算时保留合并退路。
* 第 17 条（2026-09-17）：项目加载只算一次时间代价、不分轻重；轻重按位置动态算，分段边界是所有卡入点 / 出点的并集；预渲染各位置重卡集合的并集，热舞台在某位置的重卡、轻卡就是该位置算出的两个集合（按你的要求把「该集」和「补集」的说法写清了）。
* 第 18 条（2026-09-17）：AI 菜单（聊天气泡）的操作预览请求可以插在预渲染任务前面：普通 Chrome 队列里排到所有待预渲染任务之前，不打断正在跑的一批，不占 Agent 专用 Chrome。

### 审查员对用户目标的建议（每轮审查后由主 Agent 誊入；用户决定是否钉入）

> **编号说明（2026-09-16 用户重排 user_pinned_goal.md 之后）**：下面各轮建议里的「目标 N」是重排前的旧编号。对照：旧 1～4 → 架构 1～4；旧 18 → 架构 5；旧 5～9 → 架构 6～10；旧 10 → 卡片 1；旧 11 → 卡片 2（长 motion 公式单独成了卡片 3）；旧 12 → 卡片 4；旧 13 → 卡片 5；旧 15 → 卡片 6；旧 17 → 卡片 7；旧 14 → 卡片 8；旧 16 → 卡片 9。交互设计 1～3 是新增。已被用户采纳进新版的：第 55 轮第 1 条（卡片 2 的「优先吸纳单帧最便宜的卡」）、第 53 轮第 4 条的一半（卡片 1 用 1000/FPS×70% 判重）。
>
> **任务书第 70 版对新条目的解释（供用户核对）**：卡片 1「t_mc 大于预算可以直接归为重卡」和卡片 3 的公式 t_c = (t − t_start) × FPS × t_oc 合起来按位置落实——推帧卡在某位置的进入成本 entryMs = (位置起点 − 起点) × fps × 单帧最差；≤ 预算的位置轻（一拍内补齐），2 秒内能不可见追上的位置轻（追上再显示），再往后的位置判重、整段进预渲染。用户已澄清（2026-09-16）：「超预算就整段判重」的字面意思是需要预渲染，启动后各位置的轻重仍是动态的——第 71 版按此改成：t_mc 超预算的推帧卡整段进 prerenderHeavy，位置轻重照常按卡片 3 的公式算。交互设计 1～3 不在本任务书范围，只写了三条不堵路的接口约束（约束节末尾）。


* **第 68 轮 Opus-c（一致性与引用，审第 99 版；本批最后一个）**：阻塞项 7 条（AI 菜单插队落点与源码不符、get_layout 借哪个会话两处矛盾、切 glRoute 重探针发生不了、离线重放 expectedVersion 从第 2 条起过期、断线轮询没有端点、fontFingerprint 顺序表无落点、浏览器模式音频图卡素材输入无实现路径），91 处 pinned 引用 0 处指错、4 处该引未引，已折进第 100 版。对目标的建议 2 条：(1) 渲染 5「每次打开项目测所有卡片」建议补「卡的身份（参数、源码版本、帧率、审阅表、机器 / 渲染路线）没变时可直接用上次的结果，变了就重测」——我的看法：成立，任务书 K1 一直是这么落的（第二次打开不停留），建议钉；(2) 面向平台「后四者」建议改成「第 2～6 项」——成立，第 2 项自己已标在线浏览器模式，建议钉。
* **第 72 轮（Claude Opus 验证轮，审第 106 版）**：阻塞项 5 条（K6「就绪」漏 local 档、costs 变更没有通知路、canvas 卡缺 Component 的渲染分支、gl 平面帧戳在子树虚拟时间下误判、AI 菜单插队与 background 链撞 bakery），全在第 4 步及之后；结论「可（有条件）」：第 1、2、2b、3 步现在可派，84 处源码引用无一处错。5 条已折进第 107 版（未再送审）。对目标的建议：无。
* **第 71 轮（Claude Opus 可执行性审查，审第 105 版；本批最后一轮）**：阻塞项 9 条，结论「可否开工：否」，开工前必须先修 6 条（canvasPaintedBox、K2 残留 entryMs、预渲染集合只增不减的落点、K6 级联降级与「就绪」定义、第 4 步准入成环、不做清单「舞台自己冻结快照」），另 3 条（AI 菜单插队多帧、canvas 契约字段撞 H1 的 kind、浏览器模式加载不了用户卡）可在第 1～3 步施工期间补。全部已折进第 106 版，修法未再送审。对目标的建议：无。
* **第 70 轮（Gemini 可执行性审查，审第 104 版）**：阻塞项 2 条（探针键漏 sourceVersion、__pcCanvasBox 两处残留），结论「否」；已折进第 105 版。对目标的建议：「任何衍生摘要键必须完整包含渲染管线版本信息」——我的看法：任务书 A3a 共享键和 K1 探针键现在都含源码版本，这是实现层约束，不必进 pinned。
* **第 69 轮（Gemini 可执行性审查，审第 103 版；新一批第一轮）**：阻塞项 1 条（舞台页冻快照要用的 __pcCanvasBox 只在导出页挂），结论「可否开工：否」；已折进第 104 版（freezeScene 直接 import solid.ts 的 canvasBox，不经 window 全局；F 的排序、总验收指针两条非阻塞也补了）。对目标的建议：无。
* **用户 2026-09-17 钉入渲染 3 / 渲染 5 补充、面向平台改「第 2～6 项」**：任务书第 102 / 103 版同步——K2 里推帧卡的轻重改用整段最差代价 catchUpMs 判（各位置统一；在追帧上界内各位置都轻、超出都重），seekOk 卡用 seekMs ≤ B 判，渲染 4 的按位置公式只用来算播放时实际要追多少帧；K1 只对独立卡存探针帧、身份没变探针记录复用。至此 reply 里没有待用户决定的建议了。第 101～103 版未再送审。
* **用户 2026-09-17 钉入渲染 6 补充（降级不立即生效、预渲染集合只增不减）**：任务书第 101 版 K6 改成——降级卡进预渲染队列（排在当前批之后）、死素材就绪前照常活渲、就绪后下一拍切换、之后不升回；K3(b) 后台两次追不上的卡是例外（本来就在 suppressed 里等，就绪前保持透明）；验收同步。渲染 3 的建议（最便宜按每拍实际成本算）与此不冲突，只管加载时怎么分，仍等用户决定。
* **本批（两个循环：第 63～68 轮）到此结束**，任务书第 100 版。等你决定的对目标建议汇总：渲染 3（最便宜按每拍实际成本算）、渲染 5（合成一句：探针跑出来的帧只有独立卡能直接当预渲染存；依赖卡的死素材由正式预渲染产，键里带依赖对象的身份，依赖没变就一直复用；卡和依赖的身份都没变时探针记录也直接复用、不重测——用户 2026-09-17 指出原来两条写法像矛盾，其实是探针那一趟与缓存键两件事）、面向平台（后四者→第 2～6 项）。
* **第 68 轮 Opus-b（渲染与预渲染管线，审第 98 版）**：阻塞项 14 条（就绪索引一卡一条装不下流 + 快照、F5 的推送端点未定义、浏览器模式算不出 entry.key、预渲染页的 beat 无落点、agentIdle 领不到批、G1 毛玻璃卡与 K5 矛盾、粒子卡迁不进 Worker、路线 2 的 FBO 裁不了位图、measure 异步与冻快照同步冲突、vaapi 无参数、等待上界自相矛盾、canvas 快照体积、Chrome 参数冲突、共享键不含字体），已折进第 99 版；对目标的建议：无。
* **第 68 轮 Opus-a（舞台协议，审第 97 版）**：阻塞项 11 条（gl 平面帧戳用错时间基、低内存档与路线 2 互斥、glRoute 不进 costs.device、定位配方末步会 finish 掉动画、追帧每步 beat 与总则互斥、跳转时虚拟定时器结算、粒子卡验收与 seekMs 上限互斥、组流平面挂在透视层之外、播放到头没有收尾、切 fps 后整场景透明、路线 2 端口转交不在通信表里），已折进第 98 版。对目标的建议 2 条：(1) 渲染 3「优先吸纳单帧最便宜（frameMs 最小）」建议补「最便宜按该位置每拍实际成本算，需要不可见追帧的卡把追帧步数算进去」——我的看法：成立，任务书 K2 已按每拍权重 w 落，建议钉；(2) 渲染 5「跑的时候就可以存下来当作预渲染」建议补「只对不依赖其它图层的卡成立」——成立，任务书 K1 已收窄，建议钉。
* **第 67 轮重跑（Gemini 对抗式走查，审第 96 版；manager 核验）**：阻塞项 6 条（路线 2 互换后 stage 归属、J4 能力表悬空、I0 随包字体与不做清单相抵、glRoute 无落点、图集上限自相矛盾、就绪索引重建放错段落），已折进第 97 版；对目标的建议：无。循环二的两轮 agy 到此结束，接第 68 轮三个 Opus 串行。
* **第 67 轮（Gemini 对抗式走查，会话中断前落盘、未经 manager 核验）**：阻塞项 3 条（预渲染进程重启后就绪索引怎么重建、浏览器模式 SHA-256 是异步的、belowDependent 键里的「位置」父页算不算得出），已折进第 96 版；因未核验，第 67 轮按第 96 版重跑一次。用户 2026-09-17 又钉入：面向平台一节（六平台 + APP 壳先不做 + Agent 端 Windows / Linux / Ubuntu）、渲染 10 末句（共享渲染器两条路线同时实现，桌面默认路线 1、低内存档默认路线 2）——第 92～95 版已落。
* **第 66 轮（Gemini，审第 89 版；循环二第一轮）**：阻塞项 0 条，第 86～88 版 26 条修法复核全判修掉；manager 提醒它三版都没主动找出问题、三轮返工里整张一致性表和行号抽查表都曾编造 / 抄袭，按「未发现新问题」采信。对目标的建议：无。用户已把渲染 2 的 t_mc 措辞和渲染 10 的两处（每文档一个上下文、只传时间和参数）改进 pinned。
* **用户 2026-09-17 澄清渲染 2 的 t_mc**：t_mc 是推帧过程里的单帧最差耗时（= frameMs），不是从第 0 帧推到最后一帧的总时长。第 89 版据此把「catchUpMs > B 的推帧卡整段进预渲染集合」删掉，预渲染集合只剩「任一位置判重的卡」；t_mc > B 就是 frameMs > B → capped、每个位置都判重。第 65 轮 Opus-b 对渲染 9 的第 1 条建议（补例外）随之作废。**措辞建议**：渲染 2 现原文「记录起止时间 t_m_c（最坏情况的补跑成本）」容易被读成整段总时长（本轮之前的审查员和我都这么读），建议改成「记录推帧过程中最慢一帧的耗时 t_m_c（单帧最差）」。
* **第 65 轮 Opus-c（目标一致性与引用，审第 87 版）**：阻塞项 10 条（C2 与 A3a 对 cacheable 口径相反、探针存帧判据、顺序表三处落点、freezeCode 不覆盖搬进 src 的冻结代码、playbackUrl 兜底拉原片、服务端按 path 改写素材地址、Agent 专用 Chrome 接预渲染的落点、H1 图卡不进流的旧规则、.pc-awaiting 的 canvasHeavy 例外、setStreamPlanes 签名三处不一致），74 处 pinned 引用 1 处指错、4 处缺引用，已折进第 88 版；对目标的建议：无。循环一（第 63～65 轮）到此结束。
* **第 65 轮 Opus-b（渲染与预渲染管线，审第 86 版）**：阻塞项 10 条（预渲染进程加载的导出页没有 GL Worker、C2/C3 只收 DOM 卡、cacheable 不放行 sourceDependent、隔离工程平移量算错、链上键含 clipId、K5 的互斥断言、浏览器模式算不了共享键、gl 平面的 data-pc-clip 撞 isSolid、组流没有几何宿主、renderLocalSnapshots 每卡一趟），已折进第 87 版。对目标的建议 3 条：(1) 渲染 2「必然是重卡」与渲染 9「都判轻的不预渲染」字面相抵，建议渲染 9 末尾补例外「渲染 2 判必然重的推帧卡即使各位置都判轻也整段预渲染」——我的看法：成立，建议钉；(2) 渲染 10「后台只开一个 WebGL 上下文」建议改成「每个舞台文档只开一个」（front / back / 导出页各一个，跨文档共享不了）——成立，建议钉；(3) 渲染 10「每拍主线程只传一个 t」建议改成「只传时间（每张 canvas 卡一个本地秒）和变了的参数，不传像素」——成立，任务书第 86 版已按此写，建议钉。
* **第 65 轮 Opus-a（舞台协议，审第 85 版）**：阻塞项 6 条（同步 advanceTo 等不到 Worker、追帧中的 canvas 重卡被 beat 剔掉会死锁、beat 的 t 口径、缺 reset 位、seekMs 截断记上限值是乐观的、E4b 漏了 .pc-awaiting 兜底要用真实定时器），已折进第 86 版；对目标的建议：无。
* **第 64 轮（Gemini，审第 84 版）**：阻塞项 4 条（目标 M 的 beat 过滤与追帧矛盾、摘类时位图时序、后台舞台不发 beat、canvas 卡快照无处落盘），已折进第 85 版；对目标的建议：无。
* **第 63 轮（Gemini，审第 83 版）**：阻塞项 4 条（全在目标 M：`beat` 不该带被抑制的 canvas 卡、重 canvas 卡播放贴什么、新平面与选择器名单、Stage 怎么找到 gl 平面）+ manager 补的 1 条（实体框 / 墨色改由 Worker 量），已折进第 84 版。对目标的建议三条（另开 UI 任务书；浏览器模式 WebGL 降级提示；追帧中的 UI 指示）都是产品层面，不改现有条目，供你参考。
* **用户 2026-09-16 重排 pinned（卡片划分三个轴 + 卡片渲染 10 条）后任务书第 81 版的落法（供核对）**：后处理卡两种依赖各用一种键——源依赖（Sourcedependent）= 自身共享键 + 依赖链上每个控件的共享键，与下层和位置无关，走共享档可上云；下层依赖（BDIndependent）= 自身共享键 + 同一帧下层全部活跃控件的共享键与位置，只走本地档、整场景路产（今天先用整项目 entry.key 兜底，更粗）。渲染 2 的「必然是重卡」按你 9-16 的澄清读作「必然进预渲染集合」，实时判定仍逐位置。渲染 5 的截断阈值从一拍改成 B。之前建议的三条（遮罩例外、拖动贴快照、两个阈值）你已写进 渲染 1 / 10 / 5，作废。第 79～81 版都未送审。
* **第 61 轮（Gemini，审第 76 版）**：阻塞项 0 条，第 60 轮 8 条修法确认落地；对目标的建议只有措辞类（卡片 1 的落法与目标分离、架构 4 已按用户替换、提醒核对编号），无需钉入。
* **第 62 轮（Claude Opus，审第 77 版；本批最后一轮）**：阻塞项 4 条，全部是任务书落法问题（定位配方里 clock.tick 不能传挂载帧毫秒、重试路的武装时机、pause 武装在等号处的定义、pause() 回包要带 stoppedAt），已折进第 78 版、未再送审；对 user_pinned_goal 的建议：无。
* **第 60 轮（Claude Opus 执行者走查，审第 75 版；本批最后一轮）对卡片 1 / 2 / 5 / 10、架构 4 的建议**：
  1. 卡片 1 与卡片 5 在加载阶段相顶：加载遮罩期间编辑器被挡住若干秒。建议卡片 1 末尾加「（加载阶段的测量遮罩除外，见本节第 5 条）」或卡片 5 写明「遮罩期间不算交互卡顿」。我的看法：成立，建议钉；任务书第 76 版已注明遮罩不算交互卡顿。
  2. 卡片 10 把「拖动」同时写进流和快照两种形态。建议第一句「播放/拖动进度条时刻」改成「播放时刻」，或明确「拖动用快照、播放用流」。我的看法：成立，任务书一直取「拖动只贴快照」，建议钉。
  3. 卡片 2（1000/FPS×70%）与卡片 5（1000ms/FPS）两个阈值没点明是两件事。建议卡片 5 补「（这是探针自身的耗时上限，判重阈值仍按第 3 条的 70% 预算）」。与第 53 轮第 4 条相同，可钉可不钉。
  4. 架构 4 的 Agent Chrome 句——用户 2026-09-16 已按澄清替换，本条已落实。
* **第 60 轮的阻塞项（8 条）与 pinned 引用号（9 处）已折进第 76 版**；按用户指令本批到此为止，第 76 版的改动没有再送审，主要新增：重挂载定位配方（重挂载后先两次 clock.tick 建动画再钉锚点）、direct 卡由父页合成探针记录、K2 贪心用显式每拍权重 w、互换后补发 setPlan、render 的续推模式、舞台页也挂 __bfFreeze、I1 / 验收与 I4(b) 口径一致。
* **第 58 轮（Gemini 执行者走查，审第 71 版）对卡片 4 的建议**：截图 / HTML 保存时间 t_sc 不应计入播放时活渲的追帧代价（追帧时不冻结），t_c 只由单纯渲染耗时 t_oc 算。我的看法：成立，第 72 版已另记不含冻结的 stepMs 给位置成本用，frameMs（含 t_sc）只用于判重阈值；目标原文不必改。
* **用户 2026-09-16 的要求（彻查「判重 / 判轻」与「进预渲染」的混用）**：第 72 版把两件事分开叫——实时判定（轻 / 重，逐位置动态）和预渲染集合（静态，每次启动或代价变了重算），prerenderHeavy 改名 prerenderSet，A3a / C2 / G1 / L1 改成按预渲染集合取卡，「必为重」改成「每个位置都判重」，术语表加了一行。
* **第 56 轮（Claude Opus 执行者走查，审第 68 版）对目标 8、10、12、16 的建议**：

  1. 目标 8 加时限：「所有组件在 N 秒内都变成精确的活渲染（在此之前贴死素材、不得看到推帧过程）；N 秒内追不上的卡保持死素材并记一条诊断」——任务书里「两次仍追不上就改判为重」这类退化口径目前没有目标依据。
  2. 目标 10 第 2 条补：「随机访问的代价必须与跳转距离无关；代价随距离增长的卡（例如内部按 1/60 逐步推的粒子卡）一律按『不可随机访问』处理」——第 69 版已按此给 seekOk 加了代价判据。
  3. 目标 12 点明「这里的 1000ms/FPS 是探针的截断上限，判轻重仍用第 11 条的 70% 预算」（与第 53 轮第 4 条相同）。
  4. 目标 16 的「硬件解码器预算」补「（预算以目标机型实测为准，写进任务书的准入报告）」。
我的看法：第 1 条值得钉——它给暂停态活渲一个上界，任务书 K3(b)「两次仍追不上就改判为重」和 K5 的 300 ms 等待就有了依据，建议 N 取 2 秒；第 2 条建议钉入（第 69 版已实现）；第 3 条与第 53 轮第 4 条合并，钉一次即可；第 4 条可钉可不钉。
* **第 55 轮（Gemini 执行者走查，审第 67 版）对目标 8、11、16 的建议**：

  1. 目标 11 注明贪心口径：「优先吸纳单帧最便宜（frameMs 最小）的卡入轻管线」，避免被理解成背包式的总成本最优。
  2. 目标 16 改成「播放优先使用 H.264 流；流断档或在线浏览器模式时，可用 HTML 快照顶替」。
  3. 目标 8 加一句「拖动过程中不强求活渲，只在松手与暂停静止时保证精确活渲」。
我的看法：第 1 条任务书 K2 已写成按 frameMs 升序贪心，钉不钉都不影响实现；第 2 条与第 53 轮第 3 条是同一件事（浏览器模式没有流），二选一钉入即可；第 3 条目标 8 原文本来就只写了「拖动后松开按键」，拖动中不在范围内，不用改。
* **第 53 轮（Claude Opus，审第 65 版）对目标 5、8、11、12、13、16 的建议**（五条都是把任务书已经在做的事写进目标，不改方向）：

  1. 目标 8 末尾补：「（实现上接受一个可见的等待窗口：暂停那一刻先贴死素材，能用子树虚拟时间推的卡在可见舞台里自己追上，推不动的那类要等后台舞台整场景补跑后互换才到位。）」——沿用第 52 轮对目标 8 的建议，范围按第 64 版缩小。
  2. 目标 5「舞台每渲完一帧才报一次 t」后补：「任一时刻只有当前可见的那个舞台报 t，互换时先停旧的、再起新的，t 不重复、不倒退。」——双舞台互换后「哪个舞台报」是新问题（本轮阻塞项 2）。
  3. 目标 16 末尾补：「在线浏览器模式没有流，重卡播放退化成按拍换 HTML 快照，装不下的重卡透明。」——L4 的播放形态不在现有两种形态里。
  4. 目标 12「（超过了1000ms/FPS）」改成「（单帧最差超过第 11 条的预算 B；探针本身累计超过 1000ms/FPS 就截断，已量到的数照记）」——任务书用两个阈值（截断 1000/fps、判重 B = 1000/fps × 70%），结果等价，只是字面统一。
  5. 目标 11 末句改成「追得上就先以不可见状态推活控件（能按自己的时间单独推的就在原地推，推不动的在后台整场景推完再换上来），追上以后变为可见状态活控件。」——两条追帧路的用户可见差别不同。
我的看法：第 2、3 条建议钉入（第 66 版已按这两句实现）；第 1、5 条和第 52 轮对目标 8 的建议是同一件事，可合并成一句钉入；第 4 条只是措辞统一，可钉可不钉。以上都等用户弹窗确认，未确认前不写进 user\_pinned\_goal.md。
* **第 52 轮（Gemini，Claude Opus 复核）对目标 8、11、13 的建议**：目标 8 补一句「接受后台补跑导致的一个可见等待窗口后再完成换位」（全局单时钟下暂停态精确活渲必然要等）；目标 11 改成「长 motion 在播放中一律按死素材处理，不做后台不可见的追赶与互换；互换仅发生在暂停态」；目标 13 末句改成「长 motion 交给预渲染管线处理死素材，播放时不尝试后台追赶替换」。我的看法：任务书第 63 版把播放中的互换写成「后台补到目标拍停住、可见舞台走到那一拍才换」，`frame` 序列不跳、不违反目标 5，所以目标 11 / 13 可以不改；目标 8 那句等待窗口是事实，建议加。
* **第 50 轮（Gemini，Claude Opus 复核）对第 14 条的建议**：把「预渲染只做重卡：轻卡不产快照、不产流、不进就绪索引」改成「预渲染按分派状态决定是否预渲染：在所有位置都被判为轻卡的组件不产快照；只要存在至少一处被判为重卡的组件，就整段完整预渲染」。理由：和第 17 条「预渲染取各位置重卡集合的并集」对齐，也和 C2 的写法兼容。任务书第 61 版已按这个意思写（C2 改成只给 K2 判重的卡产快照）。**用户 2026-09-17 采纳**，第 14 条已改成「在所有位置都判轻的不产快照、不产流、不进就绪索引；任一位置判重的整段完整预渲染」。


### 执行批次（第 1、2、2b、3 步，2026-09-17，Claude Fable）对目标的回应

###### 修改建议（都不改目标方向，供用户决定是否钉入）：

1. **架构 6 / 渲染 1（舞台每渲完一帧才报一次 t）**：第 3 步之后播放还是由父页每帧发 `setTime`（K4 舞台节拍在第 4 步），这段过渡期「谁报 t」仍是 store。建议目标里注明「K4 落地前播放头由编辑器推、舞台跟」，免得第 3 步验收时按目标 6 判不过。
2. **渲染 1（用户交互不允许有任何卡顿）**：实测 `setTime` 自身开销 ≈ 0.3 ms，其余全是卡片那一次 React 渲染（odometer 2.4 ms、chapter-bar 1.7 ms，dev 版 React）。建议把「每次 setTime ≤ 5 ms」这类数值写成「舞台机器开销 ≤ 1 ms；卡片自己的渲染代价归探针（渲染 2）判轻重」，否则同一条验收在不同项目上时过时不过。

###### 当前局限性（本批做完后的状态）：

1. **暂停 / 拖动只发 `setTime`，不重挂载**：往回拖或远跳之后 stateful 卡（Motion / Lottie / 粒子）的组件状态是错的，要等第 4 步的快照平面（C3 / C4）盖住；第 3 步单舞台期间画面上会看到这一点。`?stage=1&preview=legacy` 保留了旧的「换项目立刻按跳转重算」路给回退。
2. **播放由父页驱动**（见建议 1）：`play` / `pause` RPC 现在回 `{ ok: false, reason: 'unsupported' }`。
3. **探针只有离线那一半**：`render({ probe: true })` 每帧冻结整个场景（不是只冻本控件），一拍上限 B 之内重卡（scene-3d）只推得了 1 帧就截断——这是按渲染 5 的规则，但意味着重卡的 `catchUpMs` 是外推值；`vtOk` / `seekOk` 两趟布尔探针留在第 4 步的常驻探针。
4. **图卡在编辑台预览里会和原素材层叠画**：`FrameScene`（导出 / 预渲染）已按 H3 过滤被图卡接管的素材层，编辑台自己的 `MediaLayers` 没有图（第 4 步 E7 素材层进舞台后自然消失）。
5. **在线浏览器模式的两条报错**（自定义卡不支持、素材输入的音频图卡不支持）没有落点：代码里还没有「本地 / 浏览器模式」这个开关（目标 L）。
6. 多 agent 并行改文件时 vite 每次保存都整页重载，探针数值和「页面有没有被重载」都要在树安静后复跑才算数。
7. **组合卡（composite）带部件时 compositing 记 `unknown`**：A0.4 说「由部件推导」，但部件定义表（`src/parts/registry.ts`）只在浏览器端，Node 侧算身份摘要时拿不到；两端只推导一边会让共享键分叉，所以先取最保守值（空组合卡按 `independent`）。放开的办法写在 `src/cards/capabilities.md` §5。
8. **离线探针把 15 张卡全判成 `capped`**：`frameMs` 按渲染 5 含冻结时间，而 `freezeScene` 对每个元素内联全部计算样式（每元素约 9 KB），单卡冻结 14～251 ms，远超 B = 23 ms（30 fps）；不含冻结的 `stepMs` 只有 0.7～6.4 ms。A3c 实测 62 张高频卡 p50 227 KB、p90 658 KB、12 张 DOM 卡超 300 KB（lottie 最大 23 MB），根因相同。任务书第 4 步准入前的「相对 UA + 主题基线的差异样式内联」不是可选项，得先做，K2 的分类和快照体积才有意义（详见 `docs/snapshot-size-audit.md`）。
9. `setTime(t, { probe: true })` 等一次真 rAF 再冻结，direct 卡的 `frameMs` 因此至少一个 vsync（≈ 17 ms）；第 4 步 ProbeGate 落地时要么只量冻结那一段，要么扣掉 rAF 等待。
