# 任务复核速读（hunman_read）

给人看的条目版。每条的证据（文件:行号、提交、单测）在同目录的 `task_recheck.md`，编号一一对应。复核时间：2026-09-22，main `f69f229`。

图例：✅ 已完成　🟡 部分完成　❌ 未完成　⏸ 未开始　❔ 无法独立验证（要跑探针 / 真机）　➖ 按决定不做

## 一眼看全局

- **R0～R7 的代码大部分落地了**；`tsc` 零错误，`npm test` 1633 / 1632 通过 / 0 失败 / 1 跳过（主会话实跑）。
- **R8 轨道流、R9 共享 WebGL、云端那一半：确实没开始**。只有前面几步留的接口位；G0-b 编码原型已做完。
- **不符合 pinned 目标的 5 处**（主会话抽查证实了前三处）：
  - 渲染 9：预渲染集合算出来了但没人用。
  - 架构 7：fps 下拉没做。
  - 架构 9：点时间轴、拖动松开后不一定精确活渲。
  - 架构 10：24 / 25 fps 下音频会反复暂停。
  - 渲染 4：长 motion 按播放位置估代价没做。
- **check 文档偏乐观**：R5、R6、R7 标「已完成」，但各有几条子项没落实。

## 第 1 节：result_decouple 查收

- ✅ S1-1 store 解环
- ✅ S1-2 MCP 路由表
- ✅ S1-3 渲染引擎搬家
- ✅ S1-4 分层（9 个文件移动）
- ✅ S1-5 vision 拆分
- ✅ S1-6 #10 指纹清单
- ✅ S1-7 守门测试 5 个文件（单独跑全过）
- ❔ S1-8 当时的验证数字（旧提交上的数，现在 main 上重跑是 1633 / 1632）
- ✅ S1-9 别的会话留下的 worktree，说法属实
- ✅ S1-10 快照失效一次；`out/**` 已在忽略名单

## 3.8 生成快照：命名与指标拆开

- ✅ S38-1 `snapshotFreeze.ts` → `createSnapshot.ts`
- ✅ S38-2 两个文件互不 import，差异内联放进 `inlineStyles.ts`
- ✅ S38-3 页面协议改名 `__pcCreateSnapshot`
- ✅ S38-4 `freezeCode` → `snapshotCode`
- ✅ S38-5 类型改名 `SceneSnapshot` / `ControlSnapshot`
- 🟡 S38-6 四个探针脚本跟着改名：`inherited-props-probe.mjs` 引用的文件不存在，脚本跑不起来
- ✅ S38-7 成本拆成 `stepMs` / `inlineMs` / `rasterMs` / `serializeMs`，删了 `frameMs`
- ✅ S38-8 等 rAF 的时间不计入任何一个数
- ➖ S38-9 画布位图换 webp（搁置）
- ❌ S38-10 用这三个数排探针和预渲染的产能、超时就只测不存：没实现，check 也没列
- 🟡 S38-11 文档里不再用「冻结」：还有 81 处
- 另：三个快照数取的是百分位，不是计划写的「单帧最差」，注释和实现对不上

## 3.9 像素映射分流

- ✅ S39-1 滤镜新增 `curves` / `matrix`
- ✅ S39-2 `classifyPixelMap` 分 A / B / C 三类
- ✅ S39-3 A 类拒绝，回等价 `ops`
- ✅ S39-4 B 类走 WebGL
- ✅ S39-5 删掉 CPU 逐像素循环
- ❔ S39-6 GPU 与 CPU 差 ≤ 2 级、1080p 0 长任务：自报数据；有一个用例 13 个像素差 255 级（决定不改）
- ✅ S39-7 C 类拒绝并说明原因
- 🟡 S39-8 工具描述：已同步，但 `effects.mjs:107` 和 `pixelMapTools.ts:73` 还写着「1080p 每帧 400 毫秒以上」，会误导 Agent
- ✅ S39-9 R1b 排在 R3 之前完成

## R0 清账

- ✅ R0-1 探针改动和 `g0-a-webview2-probe.md` 已提交
- 🟡 R0-2 `verify-unified-frames.mjs` 整条通过：根因一已修；根因二（快照重放丢 1/64 px，`blur(32px)` 下差 255 级）没修。check 前后说法矛盾：R0 说「最后一条断言红」，R7 说「导出页 60 秒没就绪」
  - → 2026-09-23 更新：已整条通过。两种说法不矛盾：「60 秒没就绪」是 R7 跑的时候目标端口 5192 上没有 dev server（`Page.navigate` 连不上不报错，页面停在错误页等满 60 秒）；有服务器时挂的才是最后一条断言。根因二重查后主因是合成层，1/64 px 排第二，两处都修了。见 `reports/replay-mismatch-report.md` §12。
- ❌ R0-3 仓库根 dev server 冷启动量测：没做
- ❔ R0-4 旧任务书文首加说明：旧任务书已整体删除，无从核对，也不再需要

## R1 差异样式内联收尾

- ✅ R1-1 worktree 的改动在 main 上重做
- ✅ R1-2 先按 3.8 改名、拆文件
- ✅ R1-3 成本拆四个数，判重只看 `stepMs`
- ✅ R1-4 比对口径 A2(8)：基线的主题维度用的是场景根 style 串，不是 `themeId`，小差异
- ✅ R1-5 `mode` 拼进 `device`，写回显式 `demoted: false`
- 🟡 R1-6 验收：首条 `verify-unified-frames` 没过，其余是自报
- ➖ R1-7 两张超 300 KB 的 lottie 不专门处理
- ➖ R1-8 `configurePreviewServer`（留给在线浏览器模式）

## R1b 像素映射 GPU 后端

- ✅ R1b-1 两种新 op 和 SVG 注入
- ✅ R1b-2 分类、拒绝、回包
- ✅ R1b-3 `compilePixelMapGlsl` + `pixelMapGl.ts`
- ✅ R1b-4 删掉 CPU 循环
- ✅ R1b-5 工具描述（有过期文案，见 S39-8）
- ❔ R1b-6 真 GPU 验收：自报

## landed-notes 的老口径还守着吗

- ✅ LN-1 `CARD_MOUNT_LEAD` / `mountFrameOf` / `onFrameGrid` 那组口径
- ✅ LN-2 第 3 步的几条 grep 类口径
- ✅ LN-3 快照改名、control 快照不带素材层
- 🟡 LN-4 共享键敏感项：单测层面守着；「B 机首次打开」要真环境。另有风险：`solid.ts` 不在 `SNAPSHOT_FILES`，改它不会作废旧的共享快照
- ❔ LN-5 性能和端到端数字
- ➖ LN-6「不做」条目：仍守着

## R2 双舞台与协议补齐

- ✅ R2-1 第二个舞台 iframe
- ✅ R2-2 舞台端口 = 编辑器端口 +1 / +2
- ✅ R2-3 OAC 头、Range / 206、HMR upgrade
- ✅ R2-4 CORS 加两个舞台源，写舞台端口
- ✅ R2-5 `stageId` A / B 与角色脱钩
- ✅ R2-6 角色闸门
- ✅ R2-7 `RenderAborted` 补 `'role'`
- ✅ R2-8 `whenStageReady` / `pushProject`：页面侧测量用的是增量 `syncProject`，功能等价
- ✅ R2-9 按 `event.source` 过滤事件
- ✅ R2-10 `PlayReply` 统一
- ➖ R2-11 `play` / `pause` 挪到 R5 做；舞台不再发 `probe` 事件
- ✅ R2-12 `setRole` 的工作项
- ✅ R2-13 单飞队列（补跑 > 测量 > 探针）
- 🟡 R2-14 宿主能力表：`hostCapabilities.prerender` 恒为 false
- ❔ R2-15 `stage-rpc-probe` 跨源复跑
- ❔ R2-16 父页最坏帧间隔 < 20 ms：探针实际判的是三轮最坏值的中位数，比计划宽松
- ✅ R2-17 端口挪位的连带改动（任务书里还写着 5197，过期）

## R3 舞台内容

- ✅ R3-1 `VideoTrack` 搬进 `src/render/`
- ✅ R3-2 `mediaDrive.ts`
- ✅ R3-3 `mediaSync.ts` 搬家
- ✅ R3-4 素材段按图卡 / 像素映射滤掉（仓库里没有真实图卡样本）
- ✅ R3-5 `data-pc-clip` 规则
- ✅ R3-6 `FrameScene` 的 live 变体与新 props
- ✅ R3-7 `legacyTimeline` memo
- ✅ R3-8 `Stage` 六个 prop、按片段重挂载
- ✅ R3-9 四处传 `t` 的地方接上追帧与抑制
- ✅ R3-10 快照平面
- ✅ R3-11 抑制
- ✅ R3-12 流平面（占位，R8 才有内容）
- ✅ R3-13 `.pc-awaiting` / `.pc-settling` 与 500 ms 兜底
- ✅ R3-14 四种类的样式表
- ✅ R3-15 `StageView` 渲 live 版
- ✅ R3-16 `setTimeout` / `setInterval` 虚拟化
- ➖ R3-17 `Date.now` 取舞台打开那一刻的真实时间：执行者改成固定日期 2026-01-01，日期卡永远显示这一天。**需你确认**
- ❌ R3-18 导出页装同一份虚拟时钟：没做（执行者说会死锁，没法核实）。靠定时器计时的卡，预渲染 / 导出和活渲可能对不上。**需你确认**
- 🟡 R3-19 打字机 / 倒计时判例：有探针（自报）；「探针第一趟 `vtOk: false`」没有断言
- ✅ R3-20 `pinAnimations` 的 `resetIn` / `syncIn` / `sync(skip)`
- ✅ R3-21 E6：非 legacy 下不跑 `Preview` 的 rAF 循环等
- ❔ R3-22 `placeholder` 模式全长导出逐字节不变
- ❔ R3-23 快照挂上、摘掉时组件实例不变
- 另：R3 的偏差没折回 `r2-r7-task.md`；几处注释过期（`play` / `pause` 回 `unsupported`、`<Profiler>`）

## R4a 分派纯函数、可调系数、离线探针

- ✅ R4a-1 `planPipelines` 两端同一份
- ✅ R4a-2 分段边界 = 入点 / 出点并集
- ✅ R4a-3 预算 B = 1000/fps × 70%
- ✅ R4a-4 贪心与停止条件（K 节算例手算都对）
- ✅ R4a-5 每卡权重分档
- ✅ R4a-6 `capped` / `demoted` / `pinnedHeavy`
- 🟡 R4a-7 预渲染集合 = 各位置重卡并集：算了，但服务端没用（见 R6-2）
- ✅ R4a-8 `unknown` 卡照常参加贪心
- ✅ R4a-9 三个可调系数：`COST_SCALE` 调小撤不掉已写下的 `capped`
- ✅ R4a-10 `stepMs` 取 p90
- ✅ R4a-11 `out/pipeline-tuning.json`
- 🟡 R4a-12 两端算出同一张表：页面不按 device / mode 过滤、取最后一条；预渲染侧取最新一条，可能不一致
- ✅ R4a-13 探针分两趟
- 🟡 R4a-14 计时趟：没有「稳健值越过 B 就停」
- ✅ R4a-15 判重只看 `stepMs`
- ✅ R4a-16 随机访问卡抽样（实际 16 帧取 p90；和 pinned 渲染 2 的「8 帧取最差」字面冲突，**需你确认**）
- ❔ R4a-17 实测结论
- ➖ R4a-18 `--mode build`

## R4b 加载遮罩、常驻探针

- ✅ R4b-1 `ProbeGate` 遮罩
- ✅ R4b-2 测完才摘遮罩；全命中时一帧不出现
- 🟡 R4b-3「每次打开项目」都挡：只有页面会话里第一个项目挡
- 🟡 R4b-4 遮罩挡编辑：鼠标挡住了，键盘快捷键没验
- ✅ R4b-5 一次只测一个片段
- ✅ R4b-6 已测的跳过
- ✅ R4b-7 新卡在后台补测
- ✅ R4b-8 常驻探针三趟
- ✅ R4b-9 布尔探针 `vtOk` / `seekOk`
- ✅ R4b-10 整条 PUT，显式 `demoted: false`
- ✅ R4b-11 `device` 串两端共享
- ✅ R4b-12 `setPlan` 下发
- ✅ R4b-13 系数写入口
- 🟡 R4b-14 探针帧转存预渲染：链路在；项目刚打开时大概率回 202 丢帧，网络错一次就永久停
- ❔ R4b-15 实测数字
- 另：单舞台退回时探针不跑，也没有提示

## R6 数据面

- 🟡 R6-1 锚帧优先：不是全部 Chrome 一起上，也不读 `wanted`
- ❌ R6-2 **只预渲染重卡集合（pinned 渲染 9）：没做**，判轻的卡照样产快照
- ❌ R6-3 `cacheable` 放行 `sourceDependent`：没做
- ✅ R6-4 共享档收窄到缺帧
- ✅ R6-5 本地档一趟整场景
- ✅ R6-6 就绪索引 SSE
- 🟡 R6-7 按项目版本 `reset`：`reset()` 没人调用，换项目时旧层会残留
- ✅ R6-8 页面侧就绪索引
- ✅ R6-9 `wanted` 四处
- ✅ R6-10 批边界消费 `wanted`
- ✅ R6-11 回溯选帧
- ❔ R6-12 33 ms 节流与投递基线
- ❔ R6-13 播放态按层、不等
- ✅ R6-14 超限帧不进索引：但每一趟都会重渲再丢，浪费 CPU
- ✅ R6-15 快照来源接口
- ✅ R6-16 重启后重建索引
- 🟡 R6-17 F5 其余：「Agent 请求重发一次」没找到实现
- ✅ R6-18 `interactive` 参数
- 🟡 R6-19 `streamPool` 只是改了名；`/yield` 没删；`NO_AGENT_LANE` 没做
- ✅ R6-20 探针帧入库端点
- ✅ R6-21 `unknown` 卡走本地档
- ❔ R6-22 冷缓存拖动、杀进程后重建

## R5 播放与追帧

- ✅ R5-1 节拍器按 1000/fps 走真 rAF
- ✅ R5-2 按绝对时刻排程，慢帧后移不跳帧
- ✅ R5-3 播放到头的收尾
- 🟡 R5-4 暂停以 `stoppedAt` 为准：会先按 `store.t` 多发一次 settle
- ✅ R5-5 `PlayReply` 与武装停
- 🟡 R5-6 音频 40 ms 阈值：24 / 25 fps 下 50 ms 的拍会误触发；停顿进行中音频照播
- ✅ R5-7 非 legacy 不跑 rAF 循环
- ✅ R5-8 (a′) 直接定位
- 🟡 R5-9 (a) 重推：不分 `vtOk`，推完也没补一拍落定
- ✅ R5-10 (b) `vtOk` 轻卡在可见舞台里追
- 🟡 R5-11 (b) `vtOk=false` 播放态互换：两次失败后不降级，反而放回活渲
- ❌ R5-12 (b) `vtOk=false` 卡在暂停 / 跳转 / 拖动下：没人补跑
- ✅ R5-13 片段粒度重挂载
- ✅ R5-14 K5 第一路
- 🟡 R5-15 K5 第二路互换：运行中丢弃新请求，连点时最后一次不互换；「连点 10 次」证据不成立
- 🟡 R5-16 点时间轴 / 拖动松开后发 settle：暂停和跳转按钮会发，时间轴上点击、拖动松开大概率不发（主会话证实代码路径）
- ✅ R5-17 追帧中止
- ✅ R5-18 K6 舞台侧降级
- ✅ R5-19 K6 父页：预渲染进程那一半的闭环没验
- ✅ R5-20 预渲染进程接上真 `planPipelines`（只接了「算」）
- ✅ R5-21 `setRole('back')` 清理
- 另：`stageSwap` / `snapshotFeed` / `demote` / 节拍循环都没有单测

## R7 露出舞台

- ✅ R7-1 摘 front 的 `opacity: 0`
- ✅ R7-2 整帧图只留在 legacy
- ✅ R7-3 删 `mediaRects`
- ✅ R7-4 非 legacy 不跑 rAF
- ✅ R7-5 开关翻成 stage 缺省
- 🟡 R7-6 `?preview=legacy` 回滚：没有合并舞台里的同名开关
- 🟡 R7-7 D5 零碎项：`/api/frames/yield` 没删；重启超限没有界面提示
- ✅ R7-8 桌面壳端口预检（没真构建、没真跑）
- ✅ R7-9 `setRole` 不重发按 `{client, job}` 判
- ❔ R7-10 总验收 + 零卡顿
- ❌ R7-11 **回归**：播放头在末尾时按播放会立即结束，不从头重播

## pinned 里的播放行为

- 🟡 P-1 音频跟着 t、40 ms 阈值：见 R5-6
- ❌ P-2 **项目选项 fps 下拉（24 / 25 / 30 / 60）：没做**（主会话证实）
- 🟡 P-3 暂停 / 点击 / 松开后精确活渲：见 R5-16

## R8 轨道流（未开始）

- ⏸ R8-0 总判定：属实，只有接口位
- ✅ R8-1 G0-a 桌面壳准入探针
- ✅ R8-2 G0-b 编码原型：5 项留白，check 只列了 4 项
- ⏸ R8-3 G1 流划分、裁剪矩形、组流
- ⏸ R8-4 G2 分段
- ⏸ R8-5 G3 编码
- ⏸ R8-6 G4 调度
- ⏸ R8-7 G5 浏览器端解码合成
- ⏸ R8-8 G6 校验与替换
- ➖ R8-9 G7 不做 MSE / `<video>` 回退
- ⏸ R8-10 `streams` 开关默认开 + 限速 + 「预渲染中」提示
- 🟡 R8-11 G0-b 结论回填分册：4 处只进了汇总段、正文没改；行号整体过期
- R8-12 计划:249「见第 7 节第 1 条」指错了（文档问题）

## R9 共享 WebGL 渲染器（未开始）

- ⏸ R9-0 总判定：属实
- ⏸ R9-1 `gl-atlas-probe.mjs`
- ⏸ R9-2 M1 契约
- ⏸ R9-3 M2 共享 Worker、路线 1 / 2
- ⏸ R9-4 项目选项 `glRoute` 切换（`device` 串那一侧已准备好）
- ⏸ R9-5 M3 节拍协议
- ⏸ R9-6 M4 探针与分派
- ⏸ R9-7 M5 迁移 `scene-3d` 和三张用户卡（粒子卡不迁，按决定）
- ➖ R9-8 M6 uber-shader
- ⏸ R9-9 M7 像素映射上下文并进来

## 云端那一半（未开始）

- ⏸ CLOUD-0 总判定：属实。已有雏形：本地内容库、`tiers` 字段、`playbackUrl` 占位、`.procp`、预渲染进程分道
- ⏸ CLOUD-1 素材上云：分片、断点续传、逐个素材先小版后原片
- ⏸ CLOUD-2 拉取与换档
- ⏸ CLOUD-3 快照上云、卡片源码同步
- ⏸ CLOUD-4 文档服务（WebSocket、`projectRev`、Agent 直写）
- ⏸ CLOUD-5 改动竞态 B0～B6
- ⏸ CLOUD-6 Agent 只查询进程（user / agent / full 三模式）
- ⏸ CLOUD-7 `see_frames` 附实体矩形（归属待定）
- ⏸ CLOUD-8 哈希迁移与 GC、离线攒日志、模式切换
- ⏸ CLOUD-9 在线浏览器模式 L1～L5
- CLOUD-10 文末 6 个待定问题：仍未定

## 文档与决策

- ❌ DOC-1 路径表回填：check 点名的 21 个文件和另外 11 个新文件都不在表里，`mediaSync.ts` 搬家、`snapshotFreeze.ts` 删除也没记
- ✅ DEC-1 判重只看活渲，快照三段单独上报
- ⏸ DEC-2 素材两档：已写进分册，实现属于云端
- ⏸ DEC-3 canvas 卡共用 WebGL：已写进 R9 分册
- ✅ DEC-4 lottie 不专门处理，超限帧通用兜底（但因为 R6-2，这两张卡实际还在产快照）
- ➖ DEC-5 粒子卡不迁 Worker
- ✅ DEC-6 G0-b 原型已做
- 🟡 DEC-7 禁用旧词清理：「烘焙」基本清完（`scripts/archive/` 里剩 21 处）；「冻结」没清
- ⏸ DEC-8 `streams` 默认开、限速：已写进分册
- ✅ DEC-9 旧任务书舍弃
- ✅ DEC-10 像素映射分流 + WebGL 后端
- ➖ DEC-11 桌面版给云端素材补转小版（以后做）
- ✅ DEC-12 g0-a 结论：舞台隔离部分已用上，解码侧待 R8
- DEC-13 第 7 节三条待定：仍未定
- 另：fold-notes 要求把 `playable` 写进 `reply_to_users_goal.md` 请你确认，没做；该文件第 9 行还是旧的上传顺序

## pinned 目标逐条对照（已按仲裁修正）

- 🟡 交互 1 SKILL 悬浮窗：没有迷你时间轴、图钉、放大、展开；没有「回到 Claude」按钮（不在本计划范围）
- 🟡 交互 2 圆角、Fluent 深色：有圆角深色主题
- 🟡 交互 3 主题色：青蓝有；第二色 `#7b5cff` 不是低饱和酱紫
- 🟡 面向平台：只有桌面版；2～6 依赖云端第 10 步
- ❌ 架构 1 云端拆两块：只有雏形
- 🟡 架构 2 本地 / 云端两种素材模式
- 🟡 架构 3 在线浏览器模式与后台 iframe 预渲染：本地模式那一半在
- ❌ 架构 4 预渲染进程三模式、Agent 专用 Chrome：只有 lane 雏形
- 🟡 架构 5 AI 菜单预览插队
- ✅ 架构 6 按帧走、慢帧等、不冲（数字是自报）
- ❌ 架构 7 fps 下拉：没做
- ✅ 架构 8 store 跟随 t、不卡顿（自报，没复跑）
- 🟡 架构 9 暂停 / 点击 / 松开后精确活渲：见 R5-16
- 🟡 架构 10 音频 40 ms：见 R5-6
- ✅ 卡片划分轴一 / 轴二 / 轴三
- 🟡 渲染 1 交互无卡顿：「同时跑预渲染」时没验
- ✅ 渲染 2 实测分轻重（「8 帧取最差」实际是 16 帧取 p90，需确认）
- ✅ 渲染 3 贪心、追帧按总代价
- 🟡 渲染 4 长 motion 按播放位置估代价：没做，现在统一用整段最差代价
- ✅ 渲染 5 探针：「每次打开项目都挡」只对第一个项目成立；「探针帧当预渲染存」没验
- 🟡 渲染 6 实时管线与降级：闭环没验
- 🟡 渲染 7 预渲染管线：只有 HTML 快照一种形态
- ✅ 渲染 8 按位置动态算
- ❌ 渲染 9 预渲染只做重卡并集：算了但没用
- 🟡 渲染 10 两种形态 + 共享 WebGL：只有快照那一半

## 需要你拍板的

1. `Date.now` 固定成 2026-01-01，还是按计划取舞台打开时的真实时间（R3-17）。
2. 导出页 / 预渲染页不装虚拟定时器，能否接受（R3-18）。
3. pinned 渲染 2「抽 8 帧取最差」和渲染 5「p90 + 最少采样帧数」字面冲突，代码按渲染 5 做（抽 16 帧取 p90），是否认可。
4. pinned 渲染 4 被渲染 3 的「整段最差代价」取代，是否认可。
5. 是否先修这 5 条：渲染 9、fps 下拉、settle 不发、24 / 25 fps 音频误停、播到头不重播。
