# 主执行计划：M5～M8（队列上网、独立渲染主机、纯浏览器节点、多端联调）

状态：**定稿**（2026-09-24），2026-09-26 增补。已并入用户的两轮批复：第一轮是 D1～D7 与三项防御，第二轮是 S1～S4 与文档服务的通用化定位。M5（M5a、C5、C6.1～C6.4、M5b）已全部合入 main，总报告 `docs/reports/REPORT-M5.md`。2026-09-26 增补并于同日修订：
- D9 定稿（第 12 节，第 8 条同日修订）；
- 新增 SP 阶段（D10 修订版：放云端即托管在阿里云，放本机即本机当主机）；
- 共享项目的两项界面排进 C6.5（D11；2026-09-26 第二轮改为「多用户协作」，见 C10a）；
- 牵线与三种直连搬到独立计划 `docs/plan/direct-connect-plan.md`；
- 由此引出的语义改动（S5）已经用户逐句确认并写入；
- 三个模型的分工重定（0.1、0.2、11.1 节）：Opus 写代码与测试，GPT / codex 查资料与攻坚，Gemini 管交互、文案与发散。
- 授权范围改定（0.3、4、10、11.1 节）：M6～M8 一次性授权，验收全过自动合并推进；推送与远端运维由主会话做，防火墙与安全组仍由用户做。
- **2026-09-26 第二轮修订**（用户定，语义三级重整之后）：
  - 新增 HT 阶段「会话模型 + 序号确认 + 两种传输」，排在信箱合入之后、C6.6 集成之前（第 7 节）；
  - 第 6 节角色重写：主会话移到笔记本；阿里云托管组合是 M5～M8 唯一的文档主机；云端会话是渲染节点 Worker；PC 是间歇在线的辅助测试节点；主会话与云端经阿里云上的 HTTP 信箱通信，与 PC 经桌面 App 的跨会话消息通信；
  - C6.6 的 T9 改写；C10 拆出 C10a「demo 最小可用」；
  - 第 10 节去掉用量闸与「装依赖要问」，加「新增费用先问」；改语义按 `docs/semantics/guide_files/suggested_agent_behavior.md`「对齐」的按阶段规则；0.2 节回退梯次加「穷尽后最小改语义」；
  - 旧词统一：「局域网模式 / 互联网模式」改为「放本机 / 放云端」与「连接路径」，「新建 / 打开共享项目」改为「多用户协作」，「原片 / 小版」改为「素材原尺寸 / 素材小尺寸」。
- **2026-09-27 修订**（用户定，据云端容器实测）：HT 拆为 **HT-a**「会话模型 + 序号确认」（排在 C10a 之后、C10 其余之前）与 **HT-b**「HTTP 长轮询传输」（记入 `TODO.md`，出现被代理挡住 WebSocket 的成员时再做）；T9 的观察端改为 PC 桌面版或笔记本第二成员实例，云端在 T9 里当渲染节点；新增第 6.8 节「对端不在线时工程照常」。实测：云端容器里 Node 的 WebSocket 经代理可用（匿名握手到达阿里云回 401），无头 Chromium 里的 WebSocket 过不了该代理（握手回 200 / 404）。

- 依据：`docs/plan/TASK-distributed-prerender-queue.md`（下称「任务书」，M0～M8 的原始定义）、`docs/plan/distributed-prerender-queue.md`（下称「设计」）、`docs/plan/render-queue-contract.md`（下称「契约」）、`docs/plan/cloud-task.md`（第 5、6、10 步）。
- 现状：`docs/reports/REPORT-render-queue-m4.md`、`docs/reports/REPORT-render-queue-card-lock.md`；代码 `server/docservice/`、`server/render-queue/`、`server/render-node/`。
- 开发规则：`docs/semantics/developer_guide.md` 索引下的 `guide_files/`。本文只排期、分工、定验收，不改规则；与语义冲突时以语义为准，与设计冲突时以设计为准。本文引出的语义改动列在第 3 节：用户已批复，由主会话在 M5a 开工前写进 `docs/semantics/`。

---

## 0. 智能体使用规则

用哪种子 Agent 由用户按任务指定（`guide_files/multi_agent.md` 第 5 行）。下面是用户为 M5～M8 这一系列任务指定的分工。派出之后的协议（worktree、报告、提交、合并）照 `multi_agent.md` 执行，本节不重复。

### 0.1 角色

| 角色 | 实际调用 | 职责 | 不做什么 |
|---|---|---|---|
| **主会话（Master / Coordinator）** | 笔记本上的会话（2026-09-26 第二轮起；此前在主 PC） | 状态流转；定稿每阶段的契约节；派活与回收；逐个读 diff；在集成分支上重跑基线；执行回退梯次；向用户汇报、申请合并与部署；经信箱给云端会话、经跨会话消息给 PC 会话下发跨机指令并收回执（第 6 节） | 不在主工作区改业务代码；不写生产代码和测试；契约与计划文档除外 |
| **工程主力 Opus 5.5** | Agent 工具，`subagent_type: opus-dev`（effort medium）；核心难点用 `opus-dev-high`（effort high）。思考等级**不低于 medium** | 写代码、重构、写测试、跑基线。每个任务一个 worktree，建在 `.worktrees/<分支名去掉 claude/ 前缀>`，一个分支 | 不推送、不合并、不部署远端、不装依赖 |
| **查资料与攻坚 GPT / codex** | `/subagent-gpt`（`gpt-manager`），模型**强制** `gpt-6-sol`，推理 `high`。<br>查资料用只读模式，允许联网检索；攻坚用 worktree 模式 | **查资料**：写契约前查外部规范与平台行为，交付「解决路径」（0.2 节「查资料先行」）。<br>**攻坚**：接手需要反复试错的问题，在交给它的 worktree 分支上自己改、自己跑，直到该用例通过且基线不变红（0.2 节第 2 级）。worktree 模式下 manager 只传话、替它提交，不干预 | 不碰没交给它的分支；不做架构决策；不写各阶段 `*-tests` 分支 |
| **交互、文案与发散 Gemini** | `/subagent-agy`（`agy-manager`），模型**强制** `gemini-3.1-pro-high`；只用内置读取工具，产出写文件交付到 scratchpad | **用户体验**：交互流程稿与界面文案（C6.5、C10 的用户侧流程，C6.5 设计稿里「用户感知到的撤销 / 重做行为」一节）。<br>**发散**：卡死时给出至少三条彼此不同的解法，每条写清前提和代价，不做取舍（0.2 节第 3 级） | 不改代码；**不做架构审查**。产出一律当假设：主会话先验证、核对与语义一致，再进计划或交 Opus 实现；改语义按 `suggested_agent_behavior.md`「对齐」 |
| **云端会话（渲染节点 Worker）** | Anthropic 云端容器里的 Claude 会话，只能出网；经阿里云上的 HTTP 信箱与主会话收发（第 6 节） | 只按主会话下发的指令 `git pull`、装依赖、以独立渲染主机或观察端身份跑节点、页面与探针，原样回传输出 | 不改代码、不提交、不推送；指令外的操作一律不做 |
| **PC 会话（辅助测试节点，间歇在线）** | 用户空闲时在 PC 上打开的桌面 App 本地会话，与主会话用 App 的跨会话消息收发（第 6.4 节） | 需要真实第二台机器的项：局域网路径的用例、独立渲染主机、主机与预渲染分离、第二成员或第二渲染节点 | 不写 main、不做集成；只用自己的端口与数据目录，不碰用户在 PC 上跑的编辑器与数据；指令外的操作一律不做 |

用户的全局约定写的是「Ultracode 时执行层一律用 `agy-manager`，但我当场指定了模型或子 Agent 类型以我的指令为准」。本系列任务按用户当场的指定：执行层是 Opus 5.5，不是 `agy-manager`。

**分工**（用户 2026-09-26 重定，取代原来的「检索与排错攻坚 GPT」「架构外脑 Gemini」两行）：
- **Opus**：写代码、写测试、跑基线，是工程主力。各阶段的 `*-tests` 分支仍由单独的 Opus 做，不交 codex。
- **GPT / codex**：查资料；接手需要反复试错的攻坚，在分支上自己迭代。
- **Gemini**：交互流程、文案，卡死时的发散方案；产出一律先验证再采纳，不做架构审查。
- **独立评估不变**：外部模型的建议和改动，都由主会话核实后决定采纳、修正或否决（11.1 节第 3 条）。

### 0.2 回退梯次

计数单位是**同一分支上的同一个测试用例**（测试名或探针断言编号）。「一次失败」= 修改后重跑该用例仍失败。换了用例不累计，用例通过后清零。

| 级 | 触发条件 | 谁处理 | 做法 | 出口 |
|---|---|---|---|---|
| 1 常规重试 | 用例第 1 次失败 | Opus 5.5 自己 | 自行定位、修改、重跑；每次修改都提交在分支上 | 通过 → 继续；连续第 2 次失败 → 第 2 级 |
| 2 codex 攻坚 | 同一用例**连续第 2 次失败**：这是最迟的时点，越早交越好。<br>**直接进第 2 级、不等两次失败的**：偶发失败、时序竞态、端口与网络环境相关、跨进程或跨机才复现的问题 | 主会话把该 worktree 分支交给 `/subagent-gpt` 的 worktree 模式（`gpt-6-sol` / `high`） | Opus 5.5 先提交现状，此后**不动这个分支**。<br>主会话把失败用例名、完整报错、相关 diff、契约对应节交给 codex。codex 在分支上自己改、自己跑、反复试，直到该用例通过且基线不变红；manager 只传话、替它提交，不干预。<br>codex 在分支上试多少次都行，直到它自己报「思路穷尽」。<br>codex 做完后，主会话读它的 diff，核对不违反语义和契约，再让 Opus 接着往下做 | 通过 → 清零，交回 Opus；codex 自己报穷尽 → 第 3 级 |
| 3 发散求解 | codex 自己报「思路穷尽」；**或**出现跨机物理通信、协议层面的阻断（公网握手不通、代理剥掉 Upgrade 头、NAT 或防火墙导致半开连接或素材服务不可达等），这类直接进第 3 级 | 主会话调 `/subagent-agy`（`gemini-3.1-pro-high`） | 交给它：问题陈述、第 1、2 级的全部记录、相关设计与契约节。<br>要它给出**至少三条彼此不同的解法**，每条写清前提和代价，不要它做取舍。<br>它的结论一律当假设：主会话先验证，再选一条交回 Opus 或 codex 执行；涉及语义的改动走第 4 级；涉及设计或对外接口的改动，按 `suggested_agent_behavior.md`「对齐」先 dry run、用户确认后再动 | 通过 → 清零继续；选定的解法执行后仍达不到验收：卡点是按现有语义做不下去 → 第 4 级，否则 → 第 5 级 |
| 4 穷尽后最小改语义（2026-09-26 第二轮新增） | 第 3 级选定的解法执行后仍不达标，且卡点是按现有语义做不下去 | 主会话 | 照 `suggested_agent_behavior.md`「对齐」的执行期规则。先确认现有语义之内的路已经试遍：自己列出并逐条试过，codex 与 Gemini 各出一轮并试过（第 2、3 级即是）。都不行时：<br>**一级**（用户体验）：停下问用户，把试过的路、为什么不行、建议怎么改一级一起列出，由用户定。<br>**二级、三级**：主会话改语义，最小修改，只改卡住的那一处；标〔裁〕，写明试过什么、为什么不行、改了什么；二级的改动当场用 `task-announce` 播报；用户合入 main 前审〔裁〕，推翻的部分返工，据它写的代码和计划一并改回 | 改后通过 → 清零继续；一级等用户定；卡点不在语义，或改后仍不达标 → 第 5 级 |
| 5 网格穷尽后才中止 | 第 4 级之后仍不达标，或卡点不在语义 | 主会话 | **先扫网格，不数次数、不看时长**（用户 2026-09-26 定）：<br>① 把可能的原因列成网格（每格一个假设、怎么验、验的结果），逐格排除；<br>② 自己的网格扫完，让 codex 和 Gemini 各补一轮新假设，把它们的假设也加进网格、扫完；<br>③ 网格穷尽、两家都给不出新思路，才整个主流程挂起：分支保留，把整张网格和每格的结果附上，按全局约定调用 `task-announce` 播报，等人类介入。<br>失败几千次都不是中断理由，几周的任务不能因为一个数字停下 | — |

补充：

- 每一级的输入、输出都落文件，写进该阶段报告的「过程记录」一节，证据直接出现在对话里（`suggested_agent_behavior.md`「对齐」）。
- 第 2、3 级期间，同阶段其它不相干的分支照常推进；相干的（共享契约节的）一起挂起。
- 回退梯次处理的是「做不出来」。「做出来了但和语义冲突」不走梯次：代码按语义改，并告诉用户冲突在哪、改了什么（原则 2）。
- **暂时性故障不进梯次**（用户 2026-09-26 定）：断网、远端重启、请求超时、端口被占、云端或 PC 会话无应答，一律无限重试加退避。它们不进网格、不计次、不进回退梯次，也不作为中断理由。
- **裁定留痕**：主会话自行裁定的地方一律标〔裁〕，每处写明定了什么、为什么。
- **查资料先行**（用户 2026-09-26 定）：凡是要实现一个外部规范或平台行为，写契约之前先调 `/subagent-gpt`（`gpt-6-sol`，`high`，只读模式）查一次资料，把「解决路径」附进该阶段的契约或报告。例子：scrypt 参数、WebSocket 的边界情况、IndexedDB 配额、浏览器媒体元素的鉴权限制、PM2 与 UFW 的行为、Node 内置模块的坑。
- **Gemini 先出稿的用户侧内容**，每份都写文件交付到 scratchpad，主会话核实后再进计划或语义；改语义按 `suggested_agent_behavior.md`「对齐」：
  - C6.5：两项菜单、成员列表、创建者操作入口，以及进入失败、「本机关机后打不开」等提示的交互流程稿和文案。主会话核对它与 `user-workflow.md`、`workflow/project.md` 的语义一致后，交 Opus 实现；
  - C10a、C10：在线浏览器模式的用户侧流程，包括开始页的加入表单、项目设置的「多用户协作」、离线提示、素材导入置灰；放本机的项目要手填主机地址，随 PC 窗口项；
  - C6.5 的操作格式与撤销语义设计稿：技术部分由主会话和 Opus 定，Gemini 只出「用户感知到的撤销 / 重做行为」一节。
- **顾问调用记录**：每个阶段的报告都有这一节。
  - 逐次列出：用途（查资料 / 攻坚 / 交互与文案 / 发散）、问题、结论、采纳与否；攻坚的附 codex 的提交哈希。
  - 一次都没调的阶段，要写明为什么。

### 0.3 所有 Agent 共守的硬约束

- 写 main（提交、合并、推送、改写历史）须用户授权。**M6～M8 期间的例外**（用户 2026-09-26 授权）：阶段验收全过，就由主会话自动合并并进入下一阶段（第 10 节）。合并用 `--no-ff`，合并提交写成「合并 <分支>:摘要」。改写 main 的历史仍须授权。
- **人工与主会话的分工**（用户 2026-09-26 改定，取代 2026-09-24 批复第 5 条的「三件事由人工在物理机上做」）：
  - **推送 Git 分支**：由主会话自行 `git push origin <分支>`。云端会话与 PC 会话靠 `git pull` 取代码；下发的指令写明分支和提交哈希，推送完成后再下发；
  - **防火墙，仍由用户做**：笔记本与 PC 的 Windows 防火墙入站规则、阿里云控制台的安全组；
  - **集群令牌**：只剩阿里云管理接口这一处要配，由主会话在服务器上配。令牌的值不进仓库、不进对话、不进跨机消息。
  - **凭证不轮换**（用户 2026-09-26 第二轮定）：沿用现有的 SSH 私钥、集群令牌、信箱令牌；笔记本的准备工作是拷这三样文件，笔记本会话开场自检核对（第 6.1 节）。
- **远端 `8.219.80.16`**（用户 2026-09-26 授权）：M6～M8 期间，部署、重启、装软件、改配置（含服务器自己的 UFW）都由主会话直接做，每次记进阶段报告。重启会断开所有连接，要在报告里写明时间和影响。
- 新增费用（换机型、加带宽、租外部服务）先问用户，任何阶段都一样（`guide_files/constraints.md`）。
- 不碰用户常驻的 5190～5192、桌面版运行时副本、用户数据目录；PC 上用户同时在用的编辑器与数据同样不碰；不结束不是自己启动的进程。
- 子 Agent 不建 `node_modules` junction、不跑 `npm ci`；删 worktree 前按 `verification.md` 检查 junction。

---

### 0.4 主会话开工清单（2026-09-27，笔记本接手起适用）

主会话每次开工（接手、重启、换机）按本节走，开场指令只需指向本节。

**先读**：`docs/semantics/developer_guide.md` 及它索引的 `guide_files/` 全部；本文第 0、4、6、7、10 节；`docs/reports/PAUSE-2026-09-26.md` 第 3 节（恢复顺序）；当前阶段的契约与设计稿（C6.6：`c66-design.md`；HT-a：`http-transport-contract.md` 开头与第 14、15 节，以及 `origin/claude/http-transport` 上的 `docs/reports/HANDOFF-http-transport.md` 第 7、8 节，其中「代理不支持 WebSocket」已被 2026-09-27 实测推翻）。

**自检**（逐项把结果贴在对话里；令牌、密钥的值一律不打印；不过的项先报用户，不绕过）：
1. 仓库在 main、与 origin/main 一致、工作区干净。
2. 基线：类型检查零错误；全量测试零失败，记下总条数。
3. 顾问可用：`/subagent-gpt` 与 `/subagent-agy` 各答一句最简单的问话。
4. 子 Agent 定义在：`opus-dev`、`opus-dev-high`、`gpt-manager`、`agy-manager`。
5. 凭证：SSH 私钥能免交互登录 `8.219.80.16`；`.env.cluster` 存在；`PROBE_MAIL_TOKEN` 已设且能读 `to-local`。位置记在 `docs/local.md`（不入库）。记下 `to-cloud`、`to-local` 当前最大 seq，只处理之后的消息。
6. 阿里云：pm2 有 `promptcut-hosted`、`probe-coord`；nginx 有 `/coord`、`/hosted`、`/media`；UFW 与交接文档第 7 节一致；两个 `healthz` 回 200；外网匿名 WebSocket 升级回 401。
7. 监听 8777、8778 的第二实例是否还在：只记录，不动（HT-a 自测与 M8 迁移演练用）。

**接手动作**：把 `docs/reports/` 下已有的 `AGENT-*.md` 按 `multi_agent.md` 归档到 `docs/archive/agent-reports/`，单独一个提交。

**开工第一件事**：经信箱让云端报到并复测（6.5 节 W-开工）；云端不在线就登记不等，直接按 PAUSE 第 3 节开始。

**规矩都在**：改语义与停点见 `suggested_agent_behavior.md`「对齐」；授权范围、发给用户但不等、裁定权在终点之下见第 10 节；角色与对端不在线见第 6 节；release 见 `constraints.md` 与 `git_and_release.md`；顾问与回退梯次见 0.1、0.2 节；汇报与播报见 `suggested_agent_behavior.md`「汇报」和全局约定的 `task-announce`。

## 1. 现状（M4 与卡片级指纹锁之后）

**已在 main**（`9a95ed8`）：

- M1 队列本体、M2 节点纯逻辑、M3 进程内联调、M4 环境指纹进结果键、卡片级指纹锁、旧缓存清理脚本。
- 全量测试：2194 条，通过 2193，跳过 1（需要 5190 的 dev server），失败 0。
- 结果键：`resultKey = sha256(contentKey + "\n" + envFingerprint)`。本机预渲染 Chrome 的指纹是 `258acaaa7c5fe509`（Windows、SwiftShader、Chrome 152）。
- 卡片级指纹锁：
  - 锁键 `<kind>:<内容键>`，谁最先产出谁得锁；
  - 别的环境不能替锁定方产剩余帧：认领时拒 `card-locked`（契约 F.1 第 3a 步），发布时拒建（F.7）；
  - 节点会话收到 `card-locked` 就丢掉这个候选（N4）；
  - **跨机复用靠这把锁**：另一台机器指纹不同，只能投递锁定方已产齐的结果，或者接手重产。
- 队列的可见性已经按项目过滤了一层（契约 A.9）：增量只发给 `queue.watch` 了该项目的节点连接；`watch: 'all'` 的连接收全部。

**文档服务骨架**（`server/docservice/`）：

- `ws.mjs`：自写的 RFC 6455 服务端，只收文本帧，单条上限 1 MB。
- `service.mjs`：
  - `authenticate(req)` 在建连时定 `principal`，缺省所有人都是匿名；角色在 hello 之后才有；
  - `mountRenderQueue(q)` 挂队列、按 5 s 驱动 `tick`；
  - `/healthz` 报连接数、发布方数、节点数；
  - 心跳 30 s，半开连接最长约 60 s 才被判断开。
- 部署：`8.219.80.16:8787`，PM2，UFW 只放行 22、8787；脚本 `scripts/remote/docservice.mjs`；探针 `scripts/probes/ws-client-test.mjs`。
- **公网上现在是明文 `ws://`、匿名身份**。

**还不存在的**：

- 节点侧的真 WebSocket 客户端；
- 鉴权；
- 服务地址登记；
- 素材服务（`cloud-task.md` 第 5 步）；
- 文档服务本体：项目文档、操作日志、`projectRev`、内容库，以及 A3b 产物入库（第 6 步）；
- 在线浏览器模式（第 10 步）。

---

## 2. 已定的决议（2026-09-24 用户批复）

| 编号 | 决议 | 落在哪 |
|---|---|---|
| D1 | 本文放在 `docs/plan/Master-Execution-Plan.md` | 本文。审查通过后在 `docs/plan/TODO.md` 登记 |
| D2 | 执行顺序 M5a → C5 → C6 → M5b；M5a 与 C5 文件不重叠，可以并行 | 第 4 节 |
| D4 | M5a 实现轻量静态鉴权：主控端（文档服务）与各节点都从环境变量 `PROMPTCUT_CLUSTER_TOKEN` 读令牌，暂不引入加密 | M5a，第 4 节 |
| D5 | 跨机指令走会话间通信层，按会话 ID 收发；Worker 在本地 `git pull` 更新代码，不用 SSH。〔2026-09-26 第二轮〕改走阿里云上的 HTTP 信箱 | 第 6 节 |
| D7 | 素材服务部署在主 PC。主 PC 把素材服务地址上报给云端 WebSocket 控制面（远端文档服务），控制面再下发给工作节点 | 协议在 M5a，接真素材服务在 C5 |
| 新增 1 | C6：文档服务按频道（项目）做 Pub/Sub 隔离，不向无关节点广播 | 第 5.1 节 |
| 新增 2 | C5：素材服务的数据层严格抽象，给以后接 OSS 或分布式文件系统留标准插槽 | 第 5.2 节 |
| 新增 3 | M5b：队列按节点指纹前置过滤，已锁定的卡直接拒绝不匹配环境的抢单 | 第 5.3 节 |
| S1、S2 | 授权主会话在 M5a 开工前改 `product/document-service.md` 及相关语义文档，写入两条特例 | 第 3 节 |
| S3 | M5 的静态令牌只用来验证集群网络连通；细粒度的用户、租户凭证与权限隔离推到 M6 设计并落实 | 第 3 节；M6 |
| S4 | 接受当前阶段的明文传输风险，不加加密；生产环境由外部网关解决 | 第 3 节；第 9 节 |
| 定位 | 文档服务是通用的轻量文本 / JSON 调度分发中心，底层逻辑不写死只服务预渲染队列；路由层通用，挂载接口与消息结构保持抽象 | 第 5.4 节；M5a、C6 |
| 环境 | Git 推送、Windows 防火墙放行、`PROMPTCUT_CLUSTER_TOKEN` 由人工在物理机上配合完成。2026-09-26 改定：推送和令牌改由主会话做，防火墙与安全组仍由用户做 | 0.3 节 |
| D9 | 用户、共享项目与权限（2026-09-26 用户定稿）：隔离单位是共享项目，`tenantId` 就是共享项目 id；自由进入 / 限定进入；创建者三项特权；设备名；Agent 沿用所属用户的凭证；加入项目的桌面版自动当该项目的渲染节点；素材服务读写都凭票据；身份是「用户名 + 设备」，角色绑在连接上（`page` / `agent` / `render`）；集群令牌退出数据面，只守阿里云管理接口（第 8 条同日两次修订） | 第 12 节；M6 |
| D10 | 连接性（2026-09-26 用户新增，同日修订；第二轮改用新词）：共享项目分两种放法。<br>放云端 = 托管在阿里云：文档服务、素材服务、全部内容与字节都在 `8.219.80.16`，成员凭项目凭证直连。<br>放本机 = 本机当主机，成员按连接路径连上来；M5～M8 里只验局域网直连这一条，作为 PC 窗口项（第 6.4 节）。<br>牵线与三种直连整套搬到独立计划 `docs/plan/direct-connect-plan.md`（未开始）。<br>托管部署可整体迁移 | 第 7 节 SP；M8 |
| D11 | 界面（2026-09-26 用户新增，同日修订）：C6.5 在编辑界面左上角「项目」菜单做了两个入口（旧入口，界面文字为「新建共享项目」「打开共享项目」），新建时二选一「托管在阿里云 / 本机当主机」，即放云端 / 放本机；不显示可达性测试结果。<br>〔2026-09-26 第二轮取代〕改为项目设置里的「多用户协作」勾选与开始页的加入表单（`docs/semantics/workflow/project.md`「多用户协作」），改造在 C10a | 第 7 节 C6.5、C10a |
| S5 | D9、D10、D11 引出的语义改动，以及「同一团队」的措辞 | 第 3 节；待用户逐句确认 |

仍按暂定执行、用户没有另说的：

- **D6**：M6 的第二台独立主机，用笔记本上两个不同代码版本的主机实例充当，不在阿里云轻量服务器上装 Chrome。
- **D8**：E5（纯浏览器只见本人任务）挪到 M7；M5b 验 E1～E4、E6。

---

## 3. 语义冲突的批复与语义改动清单

用户 2026-09-24 已批复下面四条。

**S1、S2、定位这三条的语义改动**：由主会话在 M5a 开工前写进 `docs/semantics/`，做法是：

- 在主工作区做纯文档改动；
- 每一处改动先把改前、改后的原句贴给用户过目，用户逐句确认后再写（`suggested_agent_behavior.md`「执行」）；
- 写完单独提交，提交进 main 仍须授权。

| 编号 | 冲突 | 批复 | 要写进语义的内容 | 改哪里 | 什么时候写 |
|---|---|---|---|---|---|
| S1 | 控制面登记并下发素材服务地址。语义把连接发现 / 信令写成「现在只留接口」 | 授权改语义 | 「素材服务 URL 的动态下发作为基础设施发现功能提前引入」。同时保留原有约束：控制面只交换地址、不转发字节。设备间点对点直连仍只留接口 | `mechanism/document-service.md`「连接发现（预留）」；`product/platforms.md` 第 39、54 行那两处引用跟着改 | M5a 开工前 |
| S2 | 队列按节点指纹过滤可见性。语义写的是「文档服务不替节点判断能力，只把权限关」 | 授权改语义 | 「为防止指纹锁引发并发风暴，允许特定队列（如预渲染队列）基于节点环境指纹进行前置拦截过滤，作为不干涉节点原则的特例」。能力过滤仍由节点自己做 | `product/document-service.md`「渲染任务队列」第 51 行「只把权限关」那一条 | M5a 开工前（和 S1 一起改；实现在 M5b） |
| S3 | 单一集群令牌分不出用户和租户 | M5 的静态令牌只用于验证底层集群网络连通；细粒度的用户、租户凭证与权限隔离推到 M6 设计并落实（即 D9） | 本阶段不改语义。`product/document-service.md` 与 `product/platforms.md` 里「按凭证把权限关」「纯浏览器只处理本人任务」的要求不变，由 M6、M7 兑现 | — | D9 已于 2026-09-26 定稿（第 12 节），语义改动并入 S5 |
| S4 | 令牌在公网明文传输 | 接受当前阶段的风险，不增加加密；生产环境由外部网关解决 | 不改语义 | — | — |
| 定位 | 语义把文档服务描述成项目文档与渲染任务队列的承载者，没写它是通用的分发中心 | 用户第二轮批复第 4 条；写进语义已于 2026-09-24 确认 | 补一句：文档服务的路由层是通用的文本 / JSON 调度分发中心，渲染任务队列只是挂在上面的一个模块；以后可以挂与渲染无关的文本处理任务；小消息、不传字节的约束不变 | `product/document-service.md`「职责」 | M5a 开工前，与 S1、S2 同一批逐句确认 |
| S5 | ① 语义没有「共享项目」「用户凭证」「创建者特权」；② 共享项目的两种部署（托管在公网 / 本机当主机）与局域网发现没写；③ 素材服务的读是匿名的，D9 第 7 条要求读写都凭票据；④「同一团队」没有定义。「设备间直连」仍为预留，不改 | 用户 2026-09-26 定稿 D9、D10、D11（同日修订），第二版清单逐句确认 | **已写入**（2026-09-26，与本次计划改动同一提交）：<br>`product/document-service.md`：「部署组合」末条「共享项目的两种部署」；「连接发现」末条「局域网发现（已引入）」；「版本与身份」第 2 条；新节「共享项目与权限」；「渲染任务队列」的「只把权限关」。<br>`product/platforms.md`：「渲染节点」加入共享项目的桌面应用一条，以及「同一共享项目的其他成员」；「桌面应用」第 4 条末句。<br>`product/asset-service.md`：「职责」的局域网访问一条，以及「凭票据读写」。<br>`glossary.md`：五个新术语。<br>`workflow/project.md`：「开始」第 2 条、新节「共享项目」。<br>`user-workflow.md`：主线第 0 步 | `product/document-service.md`、`product/platforms.md`、`product/asset-service.md`、`glossary.md`、`workflow/project.md`、`user-workflow.md` | 已写 |

**M5 阶段的 `principal`**：集群令牌只证明「是集群成员」，所以 M5a、M5b 里所有带令牌的连接的 `principal` 统一为 `{ userId: 'cluster', tenantId: 'cluster' }`。依赖用户边界的用例（E5、H3）不在 M5 验。

**M6 起的 `principal`**：按 D9（第 12 节）由项目凭证决定。任何数据面连接都不带集群令牌，令牌只守阿里云管理接口（D9 第 8 条修订）。`tenantId` 是共享项目 id；身份是「用户名 + 设备」，角色绑在每条连接上，见第 12.2 节。

---

## 4. 路线图

```
          ┌── M5a 网络层、集群令牌、服务地址登记 ──── W0 握手 · W1 跨机握手 ──┐
main ─────┤                                                                     │
(9a95ed8) └── C5 素材服务（数据层抽象）─► C6 文档服务本体 + 频道隔离 + A3b ─ W2、W3 ┤
                                                                                ▼
                                           M5b 队列接真业务 + 指纹前置过滤 ── W4
                                                                                │
                                            （以上为 M5，已合入 main 6ceea9b）   ▼
                              M6 独立渲染主机 + D9 共享项目与凭证 ────────────── W5
                                                                                │
                                                                                ▼
                              SP 共享项目接入：阿里云托管、局域网发现、迁移 ──── W6
                                                                                │
                                                                                ▼
                              C6.5 项目真身进文档服务 + 共享项目界面（D11）
                                                                                │
                                                                                ▼
                              合入信箱（claude/coord-mailbox）
                                                                                │
                                                                                ▼
                              C6.6 两档素材与换档、卡片源码同步
                              （两项 codex 攻坚 → 集成 → T9 → 合入） ──────── W-T9
                                                                                │
                                                                                ▼
                              C10a 在线浏览器模式 demo 最小可用 ─────────────── W-C10a
                                                                                │
                                                                                ▼
                              HT-a 会话模型 + 序号确认 ──────────────────── W-HT-a
                                                                                │
                                                                                ▼
                              C10 其余：在线浏览器模式 L1 / L2
                                                                                │
                                                                                ▼
                              M7 纯浏览器节点 ─────────────────────────────── W7
                                                                                │
                                                                                ▼
                              M8 多端物理联调（含异地接入托管、换机迁移） ─────── W8
```

- M5a 与 C5 可以并行（文件不重叠）。
- **2026-09-26 起改为串行**，原因：
  - D9 让 C10、M7 依赖共享项目的凭证；
  - 共享项目的两种放法（放云端、放本机）改变了成员接到文档服务的方式；
  - 原来「M6 与 C10 并行」的前提已经不成立。
- C6.5 的设计稿（操作格式与撤销语义）不写代码，可以在 M6、SP 进行期间写好交用户审。
- **SP 放在 M6 之后**：
  - 成员进入托管项目或局域网主机，第一件事就是交项目凭证，托管端还要凭凭证签发素材票据。这些都是 M6 的 `rq-m6-auth` 交付的，SP 先做只能再造一套临时身份。
  - M6 的跨机验收 W5 在同一局域网里完成，不依赖 SP。
- **SP 放在 C6.5 之前**：加入共享项目必须先按放法连上托管端或局域网主机，才谈得上从那里的文档服务取项目真身（C6.5 的 D1）。
- **SP 放在 C10、M7 之前**：浏览器进入共享项目同样要走 SP 的两种路由。
- **界面放在 C6.5**：
  - 两项菜单要等连接（SP）和项目真身（D1）都在，才能真的打开一个不在本机的项目并编辑；
  - 放在 SP，只能打开只读快照；放到 C10，桌面版要白等一段。
  - SP 自己用探针和命令行验，不依赖界面。
- **不在本路线里的**：直连计划 `docs/plan/direct-connect-plan.md`（自有牵线、三种直连），未开始，不在 M5～M8 之内。
- M6 起由用户一次性授权，贯穿到 M8（2026-09-26）：阶段验收全过即自动合并并进入下一阶段，见第 10 节。此前的做法是「每个阶段开工前由用户单独下发指令，本文不自动授权后续阶段」（任务书第 2 节末），适用于 M5。
- `cloud-task.md` 的第 7、7b、9 步不在关键路径上，本文不排。
- **顺序**（2026-09-26 第二轮定，2026-09-27 改定）：合入信箱 → C6.6（两项 codex 攻坚、集成、T9、合入）→ C10a → HT-a → C10 其余 → M7 → M8。HT-b 不在本路线里（`TODO.md`）。
  - **信箱先合**：跨机指令改走信箱（第 6.2 节），之后各阶段的跨机验收都靠它。
  - **HT-a 在 C10a 之后、M7 之前**（2026-09-27 改定）：2026-09-27 实测云端容器里 Node 的 WebSocket 经代理可用，云端以现有 WebSocket 就能当渲染节点，HT 不再是它接入的前提；demo 不依赖会话接续，M8 的混沌项才真正用到它。本机信任开关随 HT-a 一起做。HT-b 只为被代理挡住 WebSocket 的成员服务，目前没有这样的成员，记入 `TODO.md`。
  - **C10a 在 C6.6 合入之后**：它要用素材小尺寸、卡片源码同步，以及新加的预渲染小尺寸。
  - 跨机项（云端或 PC 参与的）不挡任何阶段合入（M8 除外），见第 6.8 节。

---

## 5. 高并发与阻塞防御

目标：多个节点、多个发布方同时围着同一个项目转时，文档服务的出站队列不积压，节点不在注定失败的认领上空转，存储不被单机 I/O 卡死。三项分别落在 C6、C5、M5b。第 5.4 节是它们共同的前提：文档服务的通用化，从 M5a 起做。验收都并进各阶段（第 7 节）。

### 5.1 信令隔离：按频道的 Pub/Sub（C6）

**现状**：
- 队列的增量已经按 `queue.watch` 的项目过滤；
- 但 `watch: 'all'` 的连接收全部；
- 文档服务本体（项目操作、`projectRev` 通知、内容库变更）还不存在，没有任何隔离；
- 出站没有背压：慢连接的待发数据会无限堆在进程内存里。

**要求**：

1. **频道是唯一的出站路由**。文档服务里所有非回包的出站消息，都经核心层的频道分发器发出（第 5.4 节的 `ctx.publish`），各模块的消息都一样，队列的 `send` 也接到它上面。频道名的前缀由模块自己定，核心不认识具体频道。本计划用到的频道：
   - `project:<projectId>`：项目操作、`projectRev`、队列任务增量、内容库变更；
   - `cluster:endpoints`：服务地址登记（D7）；
   - `conn:<connId>`：点对点回包。
2. **订阅显式、订阅时鉴权**。连接只能订阅自己有权看的频道。`queue.watch` 变成对 `project:*` 频道的订阅。纯浏览器节点在分发器这一层就过滤掉非本人的任务，队列里现有的 Q2 过滤保留，作为第二道。
3. **`watch: 'all'` 只给独立主机（`profile: 'host'`）**，而且只收**按项目聚合的摘要**：每个项目每个扫描周期至多一条，内容是 `open` 任务数、最高优先级、按指纹分的计数。设计第 7 节「文档服务负载」写的「节点多了以后改成按项目分组、只推摘要」在这里落地。主机按摘要挑项目，再单独订阅那个项目的频道。
4. **每条连接的出站队列有上限**（常量进 `constants.mjs`，以后按设计第 8 节的方式抽成环境变量）：
   - 待发字节上限，基线 1 MB；
   - 超过上限时，先合并可被取代的消息：同一任务的状态增量只留最新一条；
   - 合并后仍超限，就用 `1013 Try Again Later` 关闭这条连接。节点重连后走 `resume` 和 `queue.snapshot` 恢复，不丢状态。
5. **广播量可观测**：`describe()` 和 `/healthz` 加上每个频道的订阅数、每条连接的待发字节、合并次数、因背压被关的次数。

### 5.2 存储接口抽象（C5）

**要求**：素材服务的 HTTP 层只认一个数据层接口 `BlobStore`，不直接碰文件系统。

- **接口**（JSDoc 定义在 `server/asset-service/store/blob-store.mjs`，契约写进 `cloud-task.md` 第 5 步对应的契约节）：
  - 读：`has(hash)`、`stat(hash)`、`read(hash, { start, end })` 返回可读流；
  - 分片上传：`beginUpload(hash, size, chunkSize)`、`putChunk(uploadId, index, stream)`、`listChunks(hash)`、`complete(uploadId)`（校验 sha256）、`abort(uploadId)`；
  - 管理：`remove(hash)`（留给以后的 GC）、`usage()`。
- **一开始就有两个实现**，用来证明插槽真能换：
  - `fs-store.mjs`：沿用 `out/media/<hash>.<ext>` 的现有布局，与 `vite-plugin-media.ts` 已有的按哈希落盘兼容；
  - `memory-store.mjs`：测试用。
- **OSS 或分布式文件系统只留插槽**：工厂 `createBlobStore({ kind })` 里 `kind: 'oss'` 回「未实现」，不装任何 SDK。
- **守门**：
  - HTTP 路由模块里不许出现 `node:fs`、`fs/promises` 的引用，加一条守门测试；
  - 一套符合性测试 `server/test/blob-store-conformance.test.mjs` 对两个实现跑同样的用例；
  - HTTP 端到端测试把实现换成 `memory-store` 再跑一遍，必须全过。
- **访问控制**（配合 D7 的主 PC 部署）：非回环来源的写请求（分片上传、`complete`、`remove`）必须带集群令牌；读按语义允许局域网跨源访问，按哈希寻址。浏览器端（M7）怎么带令牌到时候再定。

### 5.3 防锁风暴：按节点指纹前置过滤（M5b）

**现状**：
- 已锁定的卡，发布时拒建（F.7），认领时拒绝（F.1 第 3a 步）；
- 但已经在队列里的任务，事后被别的指纹锁上或被接手作废时，仍然照常广播给所有 watch 该项目的节点；
- 指纹不匹配的节点照样收到 `task.opened`、发起认领、被拒、丢候选；
- 节点多、卡多时就成了「广播 → 抢单 → 拒绝」的空转风暴，接手时还会有成批的 opened / closed。

**要求**（语义依据是第 3 节 S2 的批复，改动在 M5a 开工前写入）：

1. **可见性前置过滤**。队列给节点连接发 `queue.snapshot` 和增量时，下面两种任务不发：
   - `requires.envFingerprint` 与该节点 `node.hello` 上报的 `envFingerprint` 不同；
   - 任务的锁键已被别的指纹锁住。
   节点根本看不到这些任务，也就不会去抢。
2. **锁变更只做定向增量**。锁从 X 转到 Y 时：
   - 对指纹是 X 的节点，把这张卡的 `open` 任务作为 `task.closed { reason: 'card-locked' }` 撤回；
   - 对指纹是 Y 的节点，把匹配的任务作为 `task.opened` 补发；
   - 其它节点什么都不收。
3. **认领时的拒绝保留**（F.1 第 3a 步），作为纵深防御，只兜住过滤与锁变更之间的竞态。
4. **拒绝限流**。同一节点在一个扫描周期里被 `card-locked` 拒绝超过阈值（基线 20 次），之后的认领回 `error: 'throttled'`，直到下一周期。节点会话收到 `throttled` 就退避一个周期。
5. **节点侧过滤照旧**（设计 4.3、`filter.mjs`），作为第二层，不因队列侧过滤而删。
6. **开关**：前置过滤放在常量开关后面，缺省开。关掉时行为与现在完全相同，用来跑对照组。
7. **过滤写在队列模块里，不写进文档服务核心**（第 5.4 节）。这是 S2 批复的「特定队列」特例；别的模块默认不做这种过滤。

### 5.4 文档服务的定位：通用的文本 / JSON 调度分发中心（M5a 起）

**批复**：文档服务不能在底层逻辑里写死只服务于预渲染队列。它是一个通用的轻量文本 / JSON 调度分发中心。路由层必须通用，以后可以挂载与渲染无关的文本处理任务；挂载接口与消息结构要保持足够的抽象。

**现状的问题**：`service.mjs` 直接引用队列的东西：
- `QUEUE_DEFAULTS`、`parseInbound`、`NODE_TYPES`、`PUBLISHER_TYPES`；
- 按队列的消息类型写死了路由表，连接记录上写死了 `publisherId`、`node` 两种角色；
- 挂载入口只有 `mountRenderQueue`，而且只能挂一个。

**分层**：

| 层 | 文件（M5a 起） | 管什么 | 不管什么 |
|---|---|---|---|
| 传输 | `server/docservice/ws.mjs`（不变） | RFC 6455、只收文本帧、单条上限 | 消息含义 |
| 核心 | 新建 `server/docservice/router.mjs` | 连接登记、解析 JSON 信封、按类型把消息路由给模块、模块挂载与冲突检查、频道与订阅（第 5.1 节 C6 起）、出站上限与背压 | 任何具体业务。里面不许出现 `render-queue`、`task`、`node`、`publisher` 这类业务词的引用或分支 |
| 组装 | `server/docservice/service.mjs`，新建 `server/docservice/auth.mjs` | HTTP 与升级、鉴权定 `principal`、心跳、`/healthz`、诊断、旧接口 `mountRenderQueue` 的外观（契约 G.1〔裁〕） | 业务逻辑本身 |
| 模块 | 新建 `server/docservice/modules/render-queue.mjs`（队列适配）、`server/docservice/modules/endpoints.mjs`（服务地址登记，D7） | 各自的消息、各自的连接内状态、各自的订阅授权规则 | 传输与鉴权 |

**挂载接口**（在契约 G 节定稿；下面是约束，不是最终签名）：

```js
service.mount({
  name,                      // 模块名，唯一
  types,                     // 本模块认领的消息类型：精确名或前缀（如 'task.'、'service.'），挂载时与已挂模块冲突就抛错
  connect(ctx, connId, principal),
  disconnect(ctx, connId),
  handle(ctx, connId, message),   // 同步；需要异步的模块自己排队，不阻塞核心
  tick?(ctx),                     // 核心按模块声明的间隔驱动
  authorizeSubscribe?(ctx, connId, channel),   // C6 起
  describe?(),
}) → unmount()

ctx = { send(connId, msg), publish(channel, msg), now(), log(event, fields), constants }
```

- `mountRenderQueue(q)` 保留，改成对 `mount` 的一层包装。现有的 `docservice.test.mjs` 与契约 A 节不改一个字就要全过。
- 连接记录上的业务角色（`publisherId`、`node`）挪进队列模块自己的连接内状态。核心只记 `connId`、`principal`、订阅。

**消息信封**：
- 通用信封只规定三个字段：`type`（字符串，建议 `<命名空间>.<动作>`）、`reqId`（可选，回包原样带回）、其余是模块自己的负载；
- 核心的错误回包（`bad-message`、`unsupported`、`unauthorized`、背压）与模块无关；
- 队列现有的消息名（契约 A.6：`node.hello`、`task.claim` 等）原样保留，按类型前缀归到队列模块，不强行改名，免得破坏已合入的契约和测试。

**约束不变**：只传小的文本 / JSON 消息，单条 1 MB，二进制帧拒收，素材字节和预渲染产物永远不走这条连接（`product/document-service.md`「职责」）。

**验收**：并进 M5a 与 C6 的验收：
- **R1**：测试里挂一个与渲染无关的示例文本模块（例如回显、按行统计），和队列同时在线，互不干扰；
- **R2**：守门测试，核心文件里 0 处引用 `server/render-queue/`；
- **R3**：两个模块声明同一个类型前缀时挂载失败；
- **R4**：`mountRenderQueue` 的旧测试全过。

---

## 6. 跨机角色、信箱与交互时机（2026-09-26 第二轮改写）

> 取代原「Worker 节点（笔记本端）通信与交互时机」。原来的时机表（W0～W6 已做）与 W1 跨机断言留在 6.6、6.7 节作记录。

### 6.1 角色

| 机器 / 会话 | 角色 | 做什么 | 不做什么 |
|---|---|---|---|
| **笔记本（主会话）** | 主会话；共享项目的创建者与成员；独立渲染主机；阿里云管理员 | 0.1 节主会话的全部职责；跑桌面版：导入、生成素材小尺寸、预渲染；部署与运维阿里云；经信箱给云端会话、经跨会话消息给 PC 会话下发指令、收回执 | 见 0.1 节 |
| **阿里云 `8.219.80.16`** | 托管组合，是 M5～M8 **唯一的文档主机**：文档服务 `/hosted`、素材服务 `/media`，经 nginx 走 443；另跑协调口与信箱 `probe-coord`（`/coord`） | M5～M8 只验证项目放云端，所有跨机验收的主线项目都建在这里 | 不渲染（SP 设计要点第 1 条） |
| **云端会话** | 渲染节点 Worker；只能出网的节点（语义 `product/platforms.md`） | 按指令以独立渲染主机身份认领任务；不当页面观察端（2026-09-27 实测无头 Chromium 里的 WebSocket 过不了它的代理） | 不改代码、不提交、不推送；指令外的操作一律不做 |
| **PC 会话** | 辅助测试节点，间歇在线：用户空闲时打开会话，忙时关掉（6.4 节）。PC 与笔记本在同一局域网 | 局域网路径的用例（局域网直连、局域网发现）；独立渲染主机；主机与预渲染分离（笔记本当主机、PC 当渲染主机，或反过来）；第二成员或第二渲染节点等需要真实第二台机器的场景 | 不写 main、不做集成。用户可能同时在 PC 上干别的：PC 会话只用自己的端口（第 9 节）和数据目录，不碰用户在跑的编辑器、桌面版与数据 |

- 主 PC 退出主会话：原来在主 PC 上的主会话、创建者、发布方、PC 节点，由上表取代。这台 PC 改作辅助测试节点。
- **凭证不轮换**：沿用现有的 SSH 私钥、集群令牌、信箱令牌。笔记本的准备工作是从 PC 拷这三样文件，放在仓库之外，位置记在笔记本的 `docs/local.md`（不入库）；笔记本会话开场自检核对。三样的值都不进仓库、对话和信箱消息。

### 6.2 通道：阿里云上的 HTTP 信箱

- 主会话与云端会话之间一律经信箱通信，取代原来的跨会话消息和 git 分支回传（如 `claude/cloud-node-status`）。PC 与笔记本都是桌面 App 里的本地会话，不走信箱，用 App 的跨会话消息（6.4 节）。
- **实现**：`scripts/probes/probe-coord.mjs`，分支 `claude/coord-mailbox`，HT 之前合入 main。阿里云上以 pm2 应用 `probe-coord` 运行，只听 `127.0.0.1:8799`，经 nginx `/coord` 走 443，消息落盘 `/opt/probe-coord/mail.jsonl`。
- **队列**（单向、只追加，服务端编 `seq`）：

  | 队列 | 方向 |
  |---|---|
  | `to-cloud` | 主会话 → 云端 |
  | `to-local` | 云端 → 主会话 |
- **鉴权**：每个请求带请求头 `X-Mail-Token`，令牌只从环境变量 `PROBE_MAIL_TOKEN` 取，不进命令行、仓库、消息。
- **收发**：
  - 发：`node scripts/probes/probe-coord.mjs send --base https://8-219-80-16.sslip.io/coord --queue <队列> --from <local|cloud|pc> --kind <种类> [--ref <seq>] --body-file <文件>`；
  - 等：`node scripts/probes/probe-coord.mjs wait --base https://8-219-80-16.sslip.io/coord --queue <队列> --state <文件> --timeout-min 0`。
- **等待不靠模型轮询**：用一条后台运行的 `wait` 命令（Claude Code 里 Bash 的后台运行），它在进程内反复长轮询（每次挂 25 s），收到消息才退出、唤醒会话；`--state` 文件记最后读到的 `seq`。不让模型隔一会儿调一次。
- **信封**：`{ queue, seq, t, from, kind, ref, body }`。`kind` 取：
  - `instruction`：指令；
  - `receipt`：回执，`ref` 指向指令的 `seq`；
  - `question`：要主会话或用户定的事。

### 6.3 指令与回执

- **指令自包含**（信箱消息和跨会话消息都不会附带文件内容），每条包含：
  1. 编号与目的；
  2. 分支和提交哈希；
  3. 需要的环境变量名（只写名字）；
  4. 逐条命令；
  5. 每条命令的期望输出；
  6. 回传物：完整的标准输出、探针的 JSON 结果行、`node -v`、Chrome 版本、指纹。
- **可靠性**：
  - 每条指令都要回执：信箱里 `ref` 指向指令的 `seq`，跨会话消息里写指令编号；回执里带它核对过的提交哈希。送达不等于已读，沉默不算同意；
  - 超时没回执：主会话在对话里报告，不重发第二遍，由用户决定。PC 会话 offline 不算超时，见 6.4 节。
- **代码同步**：主会话先 `git push origin <分支>`，再下发指令；对方 `git fetch && git checkout <分支> && git pull`，回报 `git rev-parse HEAD`，与指令里的哈希一致才继续。
- 每条指令下发前，先在主会话的对话里列出内容；指令与回执的原文写进阶段报告。

### 6.4 PC：握手、调度与 PC 窗口项

- **通道**：PC 与笔记本都是桌面 App 里的本地会话，用 App 的跨会话消息通信，不走信箱。
- **权限模式**：跨会话消息可能被对方的权限模式挂起、等用户批准。PC 会话要处于不挂起消息的权限模式；握手时由主会话确认。
- **握手**（开工前）：
  1. PC 会话打开后，向主会话发「PC 报到」，附能力：Node、Chrome、ffmpeg、GPU、局域网地址、仓库所在的提交，以及它当前的权限模式；
  2. 主会话核对后回执，PC 收到回执才算握手完成；主会话收到报到之前不给 PC 派任务。
- **派任务前看状态**：此后主会话每次给 PC 派任务之前，用 ListAgents 看 PC 会话的状态：idle 或 running 就发；offline 就只登记、不发，不阻塞主线。
- **关掉**：PC 会话关掉不用告别，主会话按 ListAgents 的状态判定。PC 再打开时重新握手。
- **PC 窗口项**：
  - 需要 PC 的验收项，在各阶段单独列成「PC 窗口项」。PC 报到后，主会话把积压的窗口项按顺序发给它，做完一条回执一条；
  - 阶段合入不被 PC 窗口项卡住，除非该项在计划里明写「必须 PC」；M8 多端联调里的项默认必须 PC；
  - 未做的 PC 窗口项在阶段报告里列出，下一次 PC 报到后补做，并补进报告。
- **放本机的项目与连接路径用例**：原「局域网模式」用例改用这个名字，不再搁置，改为 PC 窗口项。C6.6、C10、M8 里凡需要局域网第二台机器的，都标「PC 窗口项」。它们只验局域网直连（含局域网发现）；公网直连与中继仍属 `docs/plan/direct-connect-plan.md`，不在 M5～M8 之内。

### 6.5 时机表（2026-09-26 第二轮起）

| 编号 | 阶段 | 什么时候 | 云端做什么 | PC 做什么（PC 窗口项） | 笔记本做什么 | 断言 |
|---|---|---|---|---|---|---|
| **W-开工** | 恢复开工 | 笔记本会话开场自检通过之后 | 在 `to-local` 报到：`node -v`、Chrome、代理、出口地址、仓库 HEAD；复测 Node 的 WebSocket 经代理连阿里云（不带凭证应回 401；2026-09-27 已测通一次，每次开工再确认） | 打开时向主会话报到、等回执（6.4 节） | 自检：基线、顾问、子 Agent、SSH、集群令牌、信箱令牌、阿里云现状、第二实例 | 信箱两个方向都通；云端 HEAD 与指令一致 |
| **W-T9** | C6.6 | C6.6 集成、部署到阿里云之后 | 以独立渲染主机身份加入笔记本建在云端的项目，认领并完成预渲染任务（不装页面依赖，不当观察端） | **观察端**：PC 桌面版以成员身份加入，看素材先小后大、卡片源码自动装上（PC 不在线时由笔记本第二成员实例代替，6.8 节）；可选：放本机版 T9（笔记本当主机，PC 经局域网直连加入） | 以创建者身份跑桌面版：导入素材、生成素材小尺寸、发布预渲染任务、改卡片源码 | C6.6 T9（第 7 节 C6.6、设计稿第 6 节） |
| **W-C10a** | C10a | C10a 部署到阿里云之后 | 可选：无头 Chrome 以手机视口打开 `/editor` 预检 | — | 桌面版建放云端的项目并当渲染节点；用户用手机扫码 | C10a 验收（第 7 节） |
| **W-HT-a** | HT-a | HT-a 本机验收全过、部署到阿里云之后 | 以独立渲染主机身份持有任务时，主会话让它的传输断一次（切代理或杀连接），断言会话接续、租约不丢、任务照常完成 | 可选：PC 当第二渲染节点做同样的断线接续 | 建放云端的项目、发布任务；从外网经 443 做三项匿名拒绝；本机替身：笔记本第二渲染主机实例 | HT6、HT7、HT-a 接续项（`http-transport-contract.md` 第 11 节，2026-09-27 注） |
| **W7** | M7 | C10、M7 本机 E5 全过之后 | 可选：无头 Chrome 作纯浏览器节点所属的另一成员 | 可选：PC 上的浏览器作另一成员 | 用户 A 发布 | E5、B2 |
| **W8** | M8 | M6～M7 全部合入之后 | 渲染节点；混沌项（断网、代理丢包） | **必须 PC**：放本机的项目与连接路径用例、第二渲染节点、主机与预渲染分离 | 发布方、PC 节点、独立渲染主机；阿里云托管组合的重启与迁移演练 | M8 全部验收 |

### 6.6 原时机表（2026-09-24 版；W0～W6 已做，作记录）

原表的「笔记本」是当时的 Worker，「主 PC」是当时主会话所在的机器。

| 编号 | 阶段 | 什么时候下发 | 笔记本跑什么 | 主 PC 跑什么 | 断言 |
|---|---|---|---|---|---|
| **W0** | 开工前 | 现在：Worker 发来握手后 | 回报环境：OS、Node 版本（要 ≥ 22，内置 `WebSocket`）、Chrome、GPU、用 `server/bakery/environment.mjs` 探到的指纹、`curl http://8.219.80.16:8787/healthz`、能否访问主 PC 的局域网地址、`git` 能否拉到仓库 | — | 双向消息往返成功；Node ≥ 22；`/healthz` 通 |
| **W1** | M5a | 本机回环全绿、新版文档服务已按授权部署之后 | `ws-client-test.mjs`；`render-queue-e2e.mjs --role node` | 发布方 + 节点；用假素材服务地址做登记 | X1～X8（6.4 节） |
| **W2** | C5 | 素材服务单步验收在本机过了之后 | 从控制面拿到主 PC 素材服务地址，跨源分片上传、断点续传、Range | 起素材服务并登记地址；用户已放行 Windows 防火墙入站 | `cloud-task.md` 第 5 步跨源条目；地址来自控制面下发，不手填 |
| **W3** | C6 | 本机端到端通过之后 | 打开同一项目、取清单、按哈希拉块；订阅另一个项目的频道做对照 | 先产出并推送 | 笔记本渲染次数 0；就绪层全部来自锁定方的键；对照项目的消息 0 条漏进来 |
| **W4** | M5b | 本机 E1～E4、K1～K3 全过之后 | 作为第二个 PC 节点跑真实预渲染（笔记本指纹与主 PC 不同） | 发布方 + PC 节点 | 跨机版 E1、E2、E6、K1 |
| **W5** | M6 | 本机单主机测试全过之后 | 两个主机实例（不同代码版本），各凭项目凭证加入主 PC 上的共享项目；另起一个不属于该项目的实例作对照 | 主 PC 作共享项目的创建者（放本机，局域网直连）：文档服务、素材服务、PC 节点、发布方 | H1～H10 |
| **W6** | SP | 本机模拟托管组合的测试全过、主会话把托管组合部署到阿里云之后 | ① 同网段：发现并加入主 PC 上放本机的项目；② 换到手机热点（与主 PC 不在同一网络），不设集群令牌，凭项目凭证进入托管项目，跑一轮发布、认领、完成 | 建一个放本机的项目、一个放云端的项目 | SP8（SP 节） |
| **W7** | M7 | 〔已由 6.5 节取代〕 C10 已合入、本机 E5 全过之后 | 浏览器以同一共享项目的另一成员（用户 B）进入，作为纯浏览器节点 | 用户 A 发布 | E5、B2 |
| **W8** | M8 | 〔已由 6.5 节取代〕 M6～M7 全部合入之后 | 主机 + 浏览器节点 + 混沌操作（物理断网）；异地（手机热点）进入托管项目 | 发布方 + PC 节点；阿里云托管组合重启、换机迁移 | M8 全部验收 |

W6 需要用户配合的物理操作：笔记本切到手机热点。阿里云安全组已放行 TCP / UDP 8700～8799 和 ICMP（用户 2026-09-26 告知），素材服务的公网端口从中选 8788，不需要再动控制台；服务器自己的 UFW 现在只放行 22、8787，部署时由主会话加上 8788（0.3 节）。

### 6.7 W1 跨机断言（作记录）

| 编号 | 内容 | 通过标准 |
|---|---|---|
| X1 | 握手 | 两端 `node.welcome` 在 10 s 内到达，带的 `epoch` 相同 |
| X2 | 鉴权 | 不带令牌、令牌错误 → 握手 401；带对的令牌 → 成功，`principal` 为集群身份 |
| X3 | 抢同一批任务 | 主 PC 发布 50 个假任务，两端节点同时认领：`completed = 50`，`duplicateDone = 0`，两端各至少认领 1 个 |
| X4 | 双向通信 | 笔记本发布、主 PC 节点完成，笔记本在 `complete` 之后 5 s 内收到 `task.done`；反过来同样 |
| X5 | 干净断开后接手 | 笔记本认领后主动关连接：任务在 `RECONNECT_GRACE_MS + SWEEP_INTERVAL_MS + 2 s` ≤ **17 s** 内被主 PC 认领 |
| X6 | 物理断网后接手 | 笔记本认领后拔网：任务在 ≤ **37 s** 内被主 PC 认领（半开连接靠租约到期回收，不等 60 s 的心跳）；笔记本恢复后用旧令牌 `complete`，回 `lease-lost` |
| X7 | 远端重启 | 经授权 `pm2 restart`：两端重连后 `epoch` 变了；旧认领回 `lease-lost { reason: 'epoch' }`；发布方重新发布后 50/50 完成；主 PC 自动重新登记素材服务地址，笔记本 10 s 内收到新的下发 |
| X8 | 地址登记 | 主 PC 登记地址后，笔记本 5 s 内收到 `service.endpoints`；主 PC 断开后超过宽限期，笔记本收到撤回；未带令牌的连接登记被拒 |

---

### 6.8 对端不在线时工程照常（2026-09-27 用户定）

- 云端会话和 PC 会话都可能不在线：云端掉线、用户关掉 PC，或两者同时。对端不在线只影响跨机验收，不影响工程推进。
- 每个阶段的验收分两类：**本机项**（G0、本机探针、阿里云第二实例上的自测）和**跨机项**（第 6.5 节 W 表里要云端或 PC 参与的）。本机项全过即可合入 main、进入下一阶段；跨机项对端不在线时记为「待跨机复核」写进阶段报告，对端回来后补做并补进报告。
- 每个跨机项都要有**本机替身**：笔记本自己扮演对端（第二成员实例、第二渲染主机实例、阿里云第二实例），先用替身跑一遍，替身通过才算本机项通过；真跨机只做复核。W 表里云端与 PC 那两列都按此理解。
- 在不在线的判定：云端看信箱回执，一条指令发出后超过两个长轮询周期没有任何回执视为不在线；PC 按第 6.4 节的 ListAgents 状态。主会话给不在线的对端只登记指令，不等。
- 例外：M8 的多端联调项必须真跨机，云端与 PC 都不在线时 M8 停在该项等用户；其它阶段一律不等。

## 7. 各阶段拆解

分支名：集成分支 `claude/<阶段>`，子分支 `claude/<阶段>-<角色>`；worktree 在 `.worktrees/<阶段>-<角色>`。`.worktrees/` 目前没被忽略，开工前主会话把它加进 `.git/info/exclude`（只改本机，不写 main）。

每个阶段都沿用 M1～M4 的对抗式分工：实现方照契约写，测试方只照契约写测试、不看实现，主会话在集成分支上对账。

### M5a 网络层、集群令牌与服务地址登记

**目标**：主 PC 与笔记本上的节点、发布方经真 WebSocket（本机回环和公网两种）接到远端控制面上的队列，协议行为和进程内完全一致；只有带集群令牌的连接能进；主 PC 能登记服务地址，控制面能下发给节点；离线时回落本机。不接真实预渲染执行器，素材服务地址用假件。

**开工前主会话要做**：

- 按第 3 节改语义：S1、S2、定位三处，逐句确认后写入；
- 写契约 G 节，写完先请用户过目：
  - 第 5.4 节的通用挂载接口 `mount` 与消息信封；
  - 节点侧传输接口、端点解析、重连退避；
  - `/healthz` 新增 `epoch`、`protocol`、已挂模块列表；
  - 令牌的携带方式；
  - 服务地址登记模块的三条消息：`service.announce` / `service.endpoints` / `service.withdraw`。

**令牌怎么带**：浏览器的 `WebSocket` 设不了自定义请求头，M7 要能复用同一套，所以用 `Sec-WebSocket-Protocol` 子协议携带（形如 `promptcut.v1` 加 `token.<值>`，服务端只回显 `promptcut.v1`）。不放 URL 查询串，免得进访问日志。服务端用定长比较核对令牌。

**失败即关**：
- 文档服务绑定非回环地址而 `PROMPTCUT_CLUSTER_TOKEN` 没设时拒绝启动（M5 的做法。M6 起改为：拒绝启动的条件只剩「未加载项目凭证存储」，集群令牌只守管理接口，见第 12.2 节）；
- 只绑回环时允许匿名，供本机开发和现有测试使用。

| 子分支 | Agent | 文件（互不重叠） | 内容 | 端口段 |
|---|---|---|---|---|
| `claude/rq-m5a-net` | `opus-dev-high` | 新建 `server/render-node/ws-transport.mjs`、`server/render-node/endpoint.mjs`；`server/render-node/local-node.mjs` 里只加「传输可换」的注入点 | 用 Node 内置 `WebSocket` 实现契约 D.1 的传输接口，带集群令牌；断线指数退避重连（上限写进常量）；重连时带 `hello.resume`；`epoch` 变了就清本地认领、让发布方重发；端点按顺序取 `PROMPTCUT_DOCSERVICE_URL` → 本机回环 → 无（回落本机路径）；探活与协议版本比对；消费 `service.endpoints` | 5260～5269 |
| `claude/rq-m5a-svc` | `opus-dev-high` | `server/docservice/service.mjs`、`server/docservice/main.mjs`、`server/docservice/ecosystem.config.cjs`、`scripts/remote/docservice.mjs`；新建 `server/docservice/router.mjs`、`server/docservice/modules/render-queue.mjs`、`server/docservice/modules/endpoints.mjs` | 第 5.4 节的通用化：核心与模块分层、`mount` 接口、`mountRenderQueue` 改为包装、业务角色挪进队列模块；`authenticate` 读 `PROMPTCUT_CLUSTER_TOKEN`；失败即关；`/healthz` 加 `epoch`、`protocol`、模块列表；服务地址登记模块：只收带令牌的连接登记、按连接生命周期撤回（断开后过 `RECONNECT_GRACE_MS`）、地址格式校验、只存地址不转发字节；部署脚本多拷 `modules/`，把令牌写进远端 PM2 环境（值由用户配在环境变量里，脚本不回显） | 不需要（测试用端口 0） |
| `claude/rq-m5a-tests` | `opus-dev` | 新建 `server/test/render-node-ws.test.mjs`、`server/test/docservice-auth.test.mjs`、`server/test/docservice-endpoints.test.mjs`、`server/test/docservice-router.test.mjs`（R1～R4，含一个与渲染无关的示例文本模块）、`scripts/probes/render-queue-e2e.mjs`、`scripts/probes/render-queue-proxy.mjs`（延迟、丢包、半开代理） | 照契约 G 节独立写；e2e 探针分 `--role publisher\|node\|both`，能在两台机器上各跑一半；结果输出一行 JSON | 5270～5279，本地文档服务 8790～8799 |

**Worker 交互**：
- W0：本阶段开工前已完成；
- W1：本机回环全绿、经用户授权部署新版文档服务、并推送集成分支之后。

**验收**：

| 项 | 标准 |
|---|---|
| G0 通用门槛 | 见第 8 节 |
| 新增单测 | `render-node-ws`、`docservice-auth`、`docservice-endpoints`、`docservice-router` 全过 |
| 通用化 | 第 5.4 节 R1～R4 全过；原有 `docservice.test.mjs` 不改、全过 |
| 本机回环 | `ws-client-test.mjs` 退出码 0；`render-queue-e2e.mjs --role both --tasks 50` 退出码 0，`completed = 50`、`duplicateDone = 0` |
| 代理注入 | 丢包 5%、延迟 200 ms 下 50 个任务恰好各完成一次；半开断线后 ≤ 37 s 被接手 |
| 失败即关 | 非回环绑定且未设令牌时进程退出码非 0，并打出原因 |
| 离线回落 | 端点不可达时节点报「回落」，本机 `ready-index-probe` 退出码 0 |
| 跨机 W1 | X1～X8 全过 |
| 渲染 | 本阶段不动 `frame-pipeline.mjs` 和导出路径，G0-R 不跑，报告写明没跑、为什么；动了就必须跑 |

### C5 素材服务（`cloud-task.md` 第 5 步 + 第 5.2 节数据层抽象）

> **2026-09-25 更正**：`cloud-task.md` 第 5 步其实已经在 main 上：
> - `8b8ad2c`：分片上传、对账、校验、Range、跨源；
> - `2944743`：导出与拼图读素材只经素材服务。
>
> C5 因此收窄为三件事，外加 W2：
> 1. 现有 `server/asset-service.ts` 改成只经 `BlobStore` 读写，实现放在 `server/asset-store/`；
> 2. 非本机来源的写请求要带集群令牌；
> 3. 编辑器进程把局域网地址登记到控制面。
>
> 下面的原文按这个更正理解。契约见 `docs/plan/asset-store-contract.md`，它取代下表：C5 只派一个实现方 `claude/c5-impl`（`opus-dev-high`）和一个测试方 `claude/c5-tests`。另外，C5 用到 M5a 的 WebSocket 传输，要在 M5a 合入之后派出。

- 协议全文在 `cloud-task.md`。开工前按那份文件第 7 行的要求，先请 agy（`gemini-3.1-pro-high`）对着代码独立审查 A1 / A3b / I1 / I4 四节。
- 部署在主 PC（D7）：绑定局域网地址，启动后向控制面 `service.announce { kind: 'asset', urls }`；控制面不可达时照常服务本机。

| 子分支 | Agent | 内容 | 端口段 |
|---|---|---|---|
| `claude/c5-store` | `opus-dev` | `server/asset-service/store/`：`BlobStore` 接口、`fs-store`、`memory-store`、工厂与 OSS 插槽（第 5.2 节） | — |
| `claude/c5-http` | `opus-dev-high` | `server/asset-service/http.mjs` 与 `vite-plugin-media.ts` 的对接：分片上传、`GET media/<hash>/chunks` 对账、Range / 206、跨源头、令牌校验；只依赖 `BlobStore`；向控制面登记地址 | 5300～5309 |
| `claude/c5-tests` | `opus-dev` | 符合性测试、HTTP 端到端（两种实现各跑一遍）、守门测试 | 5310～5319 |

**Worker 交互 W2**：用户先放行主 PC 的 Windows 防火墙入站端口。笔记本从控制面拿地址，访问主 PC 的素材服务。不可达（不在同一局域网或被 NAT 挡住）属于跨机物理阻断，直接进回退第 3 级。

**验收**：

- G0 通用门槛；
- `cloud-task.md` 第 5 步的单步验收：
  - 断点续传只补缺的分片；
  - `complete` 校验通过才算完成；
  - Range / 206 正确；
  - 跨源访问成功；
  - Agent 进程和预渲染进程读素材只经 HTTP API；
- 数据层抽象：
  - 符合性测试对 `fs-store`、`memory-store` 全过；
  - HTTP 端到端换成 `memory-store` 全过；
  - 守门测试：HTTP 层 0 处文件系统引用；
- 访问控制：非回环来源不带令牌写入回 401；
- W2 全过，地址来自控制面下发。

### C6 文档服务本体、频道隔离与产物入库（`cloud-task.md` 第 6 步 + 第 5.1 节）

> **2026-09-25 按独立审查拆分**：审查由 agy `gemini-3.1-pro-high` 完成，原文存 `docs/reports/REVIEW-c6-agy.md`，主会话核过引用。C6 拆成下面六个子阶段，按顺序做，每个子阶段单独验收、单独合并。
>
> | 子阶段 | 范围 | M5b 需要吗 | 需要用户定的 |
> |---|---|---|---|
> | C6.1 | 第 5.1 节：频道隔离与背压（I1～I5） | 需要 | — |
> | C6.2 | 产物字节推送与拉取：素材服务加 `snap/`、`px/` 路由（落 A3b 的 `[DRAFT]`）、`commitSnapshots`、拉回的块发布进就绪索引、A5 推送优先级 | 需要 | — |
> | C6.3 | 最小文档服务本体：内容库三条消息、操作日志、`projectRev` / `cardRev`、本地插件 `vite-plugin-docservice.ts` 与远程共用核心 | 需要（`plan` 任务要 `projectRev`） | — |
> | C6.4 | 快照清单与 W3 端到端（换机不重渲） | 不需要 | **决议 11**：清单「后写的赢」在同指纹多节点分段产同一张卡时会丢帧，要改决议，或者把清单键按产出方、按段拆开 |
> | C6.5 | D1、D2、D4，与 I2 一起做（项目真身移到文档服务） | 不需要 | **操作格式与撤销 / 重做语义**：第 6 步没定义，属于语义未定 |
> | C6.6 | A1 两档与换档、A6 卡片源码同步 | 不需要 | — |
>
> C6.1～C6.3 自动推进。C6.4、C6.5 开工前，需要用户定的那一项先交用户裁决（中断边界 3：语义未定）。M5b 在 C6.3 合入后就可以开工，不必等 C6.4～C6.6。下面的子分支表是拆分前写的，以本表为准。

- 这是整个路线里最大的一步。开工前主会话按影响面再拆，至少拆成下表几条。
- 远程文档服务（`server/docservice/`）与本地文档服务（`vite-plugin-docservice.ts`）必须是同一份协议代码，不能出两套实现。

| 子分支 | Agent | 内容 | 端口段 |
|---|---|---|---|
| `claude/c6-channels` | `opus-dev-high` | 第 5.1 节全部，做在文档服务核心层、对所有模块通用（第 5.4 节）：频道分发器、订阅鉴权钩子、`watch: 'all'` 改摘要、每连接出站上限与合并、背压关闭、可观测字段。**先于其它 C6 子分支合入集成分支**，后面的消息都走它 | 5320～5329 |
| `claude/c6-docservice` | `opus-dev-high` | D1、D2、D4 的服务端侧、`vite-plugin-docservice.ts`、操作日志、`projectRev` / `cardRev`、内容库 | 5330～5339 |
| `claude/c6-a3b` | `opus-dev-high` | A1 的两档与换档、A5、A6、A3b；产物推主 PC 的素材服务 | 5340～5349 |
| `claude/c6-tests` | `opus-dev` | 频道隔离与背压的压力测试（I1～I5），`cloud-task.md` 第 6 步的验收用例 | 5350～5359 |

**Worker 交互 W3**：换一台机器打开同一项目，不用重新预渲染。两台机器指纹不同，这一条只能靠卡片级指纹锁的「锁定方已齐 → 直接投递」成立。断言笔记本的渲染次数为 0、就绪层全部来自锁定方的键；另订阅一个无关项目做对照，确认隔离。

**验收**：

- G0 通用门槛，加上 G0-R（渲染附加项）。
- `cloud-task.md` 第 6 步的单步验收：D 的条目、A1「第 6 步后」、A5、A3、端到端换机。
- 频道隔离与背压（本机压测，一行 JSON 出结果）：

| 编号 | 场景 | 标准 |
|---|---|---|
| I1 | 20 个项目，每个项目 10 个节点只订阅本项目；对项目 A 连续发布、认领、完成 500 次 | 非 A 的节点收到 A 的消息 **0 条** |
| I2 | 同上 | 每条增量的实际发送次数 = 该频道的订阅数，逐条核对，0 偏差 |
| I3 | 1 个节点连上后停止读取（慢消费者），其余 199 个正常 | 正常节点的投递延迟 p95 < 50 ms（本机回环）；慢连接超过 1 MB 待发后被 `1013` 关闭；文档服务 RSS 增长 < 50 MB |
| I4 | 独立主机 `watch: 'all'`，20 个项目同时有变化 | 每个项目每个扫描周期至多 1 条摘要；不收任何单任务增量 |
| I5 | 被 `1013` 关闭的节点重连并 `resume` | 认领接续，`queue.snapshot` 与重连前的服务端状态一致，0 丢失 |

### M5b 队列接真业务与防锁风暴

> **2026-09-25 用户裁决**：
> 1. **远端节点从文档服务取项目内容**：页面报摘要时，把项目 JSON 分片上传给文档服务，文档服务按 `projectRev` 存一份不可变快照；节点用 `project.snapshot.get` 按版本取。符合语义「节点按项目版本从文档服务取项目」，不改语义，为 C6.5 的 D1 铺路。落在 M5b 契约 J 节。
> 2. **C6.5 移出本轮 M5**：D1、D2、D4 以及操作格式与撤销语义另立专项，先写设计稿交用户审，再动编辑器数据流。本轮 M5 以 C6.1～C6.4、C6.6、M5b 收尾。（任务书 M5 + 第 5.3 节）

**前置**：M5a、C5、C6 已合入 main；第 3 节 S2 的语义改动已在 M5a 开工前写入。

| 子分支 | Agent | 内容 | 端口段 |
|---|---|---|---|
| `claude/rq-m5b-queue` | `opus-dev-high` | 第 5.3 节全部：按指纹的可见性过滤、锁变更的定向增量、拒绝限流、开关；契约补 H 节 | — |
| `claude/rq-m5b-node` | `opus-dev-high` | `local-node.mjs` 接真实执行器：认领 → 查素材服务去重（地址来自控制面下发）→ `FramePipeline` 产出 → 推素材服务、收全 → `complete`；闲时门槛；`throttled` 退避 | 5360～5369 |
| `claude/rq-m5b-pipeline` | `opus-dev-high` | `frame-pipeline.mjs` / `ready-index.mjs`：订阅本机项目的 `task.done`，把别的节点做的结果并进就绪索引（字节从素材服务取）；队列路径在开关后面，离线回落现有 preload | 5370～5379 |
| `claude/rq-m5b-page` | `opus-dev` | `src/editor/`：`preload` 改为向文档服务发布 `plan` 任务 | 5380～5389 |
| `claude/rq-m5b-tests` | `opus-dev` | E1～E4、E6、离线回落、K1～K3 | 5390～5399 |

**Worker 交互 W4**：笔记本作为第二个 PC 节点跑真实预渲染。

**验收**：

- G0 通用门槛，加上 G0-R。
- 任务书第 6 节的 E1～E4、E6（E5 挪到 M7）。
- 离线回落 O1：断开文档服务后，本机 `ready-index-probe`、`preview-fallback-probe` 两种模式都退出码 0。
- 契约 F 节原有的 Q、N、L、W 系列测试一条不改、全过。
- 防锁风暴，每项都跑两遍：过滤开（实验组）、过滤关（对照组）：

| 编号 | 场景 | 标准 |
|---|---|---|
| K1 | 两种指纹各 4 个节点，200 个任务，其中一半的卡已被另一种指纹锁住 | 实验组：稳态下 `card-locked` 拒绝 **0 次**，竞态窗口内的拒绝 ≤ 认领总数的 1%；对照组的拒绝数写进报告作为基数 |
| K2 | 同上 | 不匹配的节点收到已锁卡任务的 `task.opened` **0 条** |
| K3 | 运行中把 20 张卡的锁从 X 转到 Y（接手） | X 节点只收到这 20 张卡的撤回，Y 节点只收到补发，其它节点 0 条；每张卡恰好一次 `task.done`；一层里不混两种指纹 |
| K4 | 构造一个无视过滤、反复抢已锁卡的节点 | 一个周期内超过 20 次拒绝后回 `throttled`；其它节点的认领延迟不受影响（p95 与无干扰时相差 < 20%） |

### M6 独立渲染主机（任务书 M6）

> **2026-09-26 开工时按影响面拆成三段**（主会话裁定，第 11.1 节第 1 条），按顺序做、各自验收、最后一起经 W5 跨机验收后合入 main：
>
> | 段 | 范围 | 契约 |
> |---|---|---|
> | M6a | D9 全部：共享项目、凭证、连接角色、空间隔离、票据、限速、失败即关；集群令牌退出数据面 | `docs/plan/auth-contract.md` |
> | M6b | 独立渲染主机（`profile: 'host'`）、按代码版本过滤、多项目连接 | `docs/plan/render-host-contract.md`（M6a 合入集成分支后写） |
> | M6c | 第 11.2 节排在 M6 的推迟项（轨道流走队列、本地档能力闸、`watch: 'all'` 收紧、`plan` 就近认领、闲时门槛、style 顺序确定化、远端卡 PNG 缓存） | M6c 开工时写 |
>
> 下面的子分支表是拆分前写的，以本表为准：M6a 的分支为 `claude/m6-auth`、`claude/m6-auth-tests`，集成分支 `claude/m6`。

**开工前**：
- D9 已于 2026-09-26 定稿，见第 12 节。原来要求 D9 满足的三处，第 12 节都覆盖了：M6 按 `tenantId`（即共享项目）授权、M7 的 Q2、浏览器能携带凭证。
- 开工前要做两件事：
  - 第 3 节 S5 的语义改动经用户逐句确认后写入；
  - 按「查资料先行」（0.2 节）调 GPT 查 scrypt 参数、`Sec-WebSocket-Protocol` 的边界情况、浏览器媒体元素的鉴权限制，把解决路径附进契约；
  - 主会话写 `docs/plan/auth-contract.md`，把第 12 节落到消息和数据结构：握手消息、连接角色、票据格式、名单与特权操作、`principal` 字段。
- 集群令牌从数据面彻底退出，任何角色的连接都不带，只留给阿里云的管理接口（D9 第 8 条第二次修订，第 12 节）。

**项目真身在哪**（D10 修订后）：
- 放云端的共享项目都托管在阿里云，同一个文档服务上。独立主机连一次就能凭各项目的凭证加入多个项目，按第 5.1 节的项目摘要挑活。
- 放本机的项目在各创建者本机，主机要另连那台机器的文档服务。
- 所以主机同时维持「阿里云一条 + 每个局域网主机一条」连接，在已加入的项目之间挑活。
- 独立主机就是只开 `render` 连接的设备，和成员一样凭项目凭证进入，不带集群令牌；限定进入时由创建者在名单里给它加一条（12.2 节）。

| 子分支 | Agent | 内容 | 端口段 |
|---|---|---|---|
| `claude/rq-m6-host` | `opus-dev-high` | 无编辑界面的预渲染进程形态（`profile: 'host'`，只开 `render` 连接）；按 `frameCode` 代码版本分 worker 池；`codeVersions` 进 `node.hello`；多项目、多连接的挑活（见上段）；按 5.1 节的项目摘要挑项目 | 5400～5409 |
| `claude/rq-m6-auth` | `opus-dev-high` | 第 12 节全部：<br>共享项目的登记与存储（托管端或局域网主机）；<br>两种进入方式的挑战应答握手；<br>连接角色 `page` / `agent` / `render` 与 `render` 的归属；`principal`；<br>创建者三项特权；踢人；<br>素材服务读写的票据签发与校验（回环匿名保留）；口令错误限速；按 `tenantId` 授权；<br>集群令牌退出数据面，改为只守管理接口；「失败即关」的条件改为「未加载项目凭证存储」；<br>产物带认领令牌和节点 id 记账，出问题能按节点整批作废（设计第 7 节） | 5460～5469 |
| `claude/rq-m6-tests` | `opus-dev` | 照 `auth-contract.md` 独立写：多主机抢活、代码版本过滤、越权拒绝，以及 H4～H10 | 5410～5419 |

**Worker 交互 W5**：主 PC 作创建者，建一个放本机的共享项目；笔记本起两个主机实例（D6）凭项目凭证加入，另起一个没有该项目凭证的实例作对照。

**验收**：

- G0 通用门槛，加上 G0-R。
- H1：两台主机加主 PC 同时取活，每个任务恰好完成一次；产物与同指纹下单机渲染的结果逐像素相同。
- H2：代码版本不同的任务被认领 0 次。
- H3：没有该项目凭证的主机进不了这个项目：认领数为 0，收到该项目的消息 0 条。只出示集群令牌、不交项目凭证的连接同样进不来。
- H4 **进入方式**，各测 20 次：

  | 方式 | 测什么 | 结果 |
  |---|---|---|
  | 自由进入 | 项目名和项目密码都对 | 100% 进入 |
  | 自由进入 | 密码错 | 0 次进入 |
  | 限定进入 | 名单外的用户名，或密码错 | 0 次进入 |
  | 限定进入 | 删掉某个名单条目 | 这个人现有的连接 5 s 内全部断开，再连被拒 |
  | 自由进入 | 两台设备自报同一用户名 | 两人的 `userId` 不同，显示名都带「(设备名)」 |

- H5 **创建者特权**：
  - 改项目密码或名单、删项目、踢人这三项，没有创建者证明的一律回 `forbidden`，带证明的成功；
  - 逐个列出其余全部消息类型，创建者和普通成员的结果完全一致。
- H6 **踢人**：被踢者的全部连接 5 s 内断开；它手里的素材票据立刻失效，再用回 401；在创建者撤销前，同一身份再进入被拒。
- H7 **素材票据**，每条都对读和写各测一遍：
  - 非回环来源不带票据回 401；过期票据回 401；别的项目的票据回 403；
  - 回环来源不带票据照常；
  - 改项目密码后，旧票据全部回 401。
- H8 **口令不上线、错误限速**：
  - 抓握手的全部消息，里面不出现任何密码的明文；
  - 同一来源在 1 分钟内错 5 次后，进入 60 s 冷却，冷却期内密码对了也拒；别的来源不受影响。
- H9 **连接角色**：
  - 一台桌面版同时开 `page` 和 `render` 两条连接，两条的身份都是同一个「用户名 + 设备」；
  - Agent 开 `agent` 和归属于它这个对话的 `render`，它的写入记「用户 + 设备 + 第几个对话」；
  - 页面的写入记「用户 + 设备 + 页面」；
  - 成员列表按设备聚合成一行，标签与实际打开的连接一致。
- H10 **集群令牌只守管理接口**：
  - 数据面的任何角色，带不带集群令牌，结果都一样：令牌不给任何额外权限；
  - 素材服务的非回环写入不再认集群令牌（C5 的做法退役），只认票据；
  - 管理接口（服务地址登记、管理 HTTP、迁移导出）不带令牌回 401；
  - 绑非回环地址而没加载项目凭证存储时，进程退出码非 0 并打出原因；没配集群令牌照常启动，只是管理接口全部回 401；
  - 局域网主机的管理接口只绑回环地址，局域网里的其它机器连不上它，本机访问不要令牌。

### SP 共享项目接入：阿里云托管与局域网（D10，2026-09-26 新增并修订）

> **2026-09-26 修订**：用户家宽在运营商级 NAT 后面，没有公网 IPv4 也没有 IPv6，主 PC 当不了面向公网的主机。
> - 放云端即托管在阿里云；
> - 原 NET 阶段的牵线服务、端口映射、IPv6 直连、打洞、NAT 类型判断、创建时的可达性测试，整套搬到独立计划 `docs/plan/direct-connect-plan.md`（未开始，不在 M5～M8 之内）；
> - 本阶段由 NET 改名为 SP，只剩下面四件事。

**目标**：共享项目按两种放法路由。
- **放云端 = 托管在阿里云**：
  - 文档服务和素材服务都跑在 `8.219.80.16` 上。项目内容、任务队列、内容库、素材和预渲染产物都在那台机器上。
  - 成员直接连阿里云，凭项目凭证进入，不需要集群令牌。
- **放本机 = 本机当主机**：创建者本机同时作为这个项目的文档服务和素材服务，成员在同一网段发现并直连，不经阿里云。

**前置**：M6 已合入（D9 的项目凭证与票据）；第 3 节 S5 的语义改动已写入。

**开工前主会话要做**：
- 查资料先行（0.2 节）：调 GPT 查 UDP 组播在 Windows 与各路由器上的行为、PM2 与 UFW 的行为、`fs-store` 在 ext4 上的并发写入，把解决路径附进契约。
- 写 `docs/plan/shared-project-contract.md`，内容包括：
  - 新建、进入共享项目的消息；
  - 两种放法的路由；
  - 局域网发现的组播地址、端口与广播间隔；
  - 托管部署的配置项与数据目录布局。
- 迁移步骤已写在 `docs/plan/hosting-migration.md`，本阶段把它做成真的能跑。

**设计要点**（〔裁〕是主会话在 D10 修订后的授权范围内定的）：

1. **阿里云托管部署**：
   - **文档服务**：现有的 8787，挂 render-queue、endpoints、project、content 四个模块，加 M6 的凭证模块。
   - **素材服务**：新部署到同一台机器。
     - 公网端口 8788〔裁〕，取自安全组已放行的 8700～8799；
     - 数据层用 C5 的 `fs-store`，放在轻量服务器的本地磁盘上；OSS 插槽保持为空，记为 M8 之后的事（第 11.2 节）。
     - 素材服务启动后，按 D7 的机制向本机文档服务登记自己的公网地址，成员从 `service.endpoints` 拿到它。
   - **阿里云只存、只分发，不渲染**：预渲染由成员的桌面版（D9 第 6 条）和独立主机完成，产物推到阿里云的素材服务。
2. **地址是配置项，数据目录可以整个拷走**〔裁〕：
   - 两个服务的对外地址来自配置。代码里不许散落写死的 IP，加一条守门测试。
     - 服务端：`PROMPTCUT_DOCSERVICE_PUBLIC_URL`、`PROMPTCUT_ASSET_PUBLIC_URL`；
     - **客户端**（用户 2026-09-26 定）：
       - 内置一个缺省的托管地址，只在一处常量里定义；
       - 加入项目的界面上允许改（C6.5 做在旧入口「打开共享项目」里；第二轮起是开始页的加入表单）；
       - `PROMPTCUT_HOSTED_URL` 只用来覆盖，开发和测试时用。
     - 换托管服务器时，要么改缺省值并发版，要么由成员在界面上改（`hosting-migration.md` 第 8 步）。
   - 两个服务的全部持久数据放在一个根目录 `PROMPTCUT_DATA_DIR` 下：
     - `docservice/`：项目快照、操作日志、内容库、项目凭证与票据密钥；
     - `assets/`：`media`、`snap`、`px`。
   - 任务队列只在内存里（语义「队列只在内存里」），不进数据目录。
3. **两种放法的路由**：
   - **新建**：
     - 选放云端，就连托管地址，由阿里云的文档服务建项目、存凭证；
     - 选放本机，就在本机起文档服务和素材服务，绑局域网地址，开始组播广播。
   - **打开**：
     - 先在本网段按项目名发现，限时 3 s；再向托管地址按项目名查〔裁〕。
     - 两边都有同名项目时并列显示，标明「阿里云托管」或「局域网 · 主机设备名」，由用户挑；
     - 两边都没有时，明确提示没找到。
4. **局域网发现**：方案只写在 `docs/plan/direct-connect-plan.md`「设计要点」第 4 条，本阶段照它实现，不另写一份。组播地址、端口与广播间隔在本阶段的契约里定，直连计划沿用。
5. **凭证**（D9 第 8 条修订）：
   - 数据面的任何连接都只交项目凭证，不带集群令牌。数据面包括 `page`、`agent`、`render` 三种角色，也包括独立渲染主机。
   - 集群令牌只守阿里云的管理接口：服务地址登记、管理用的 HTTP 接口、迁移用的导出。管理接口没令牌回 401。
   - 绑非回环地址而未加载项目凭证存储时，拒绝启动。这是 M5a「失败即关」的新条件；没配集群令牌不影响启动，只是管理接口全部回 401。
   - **放本机时，主机（局域网主机）的管理接口只绑回环地址，不要求任何令牌**（用户 2026-09-26 定）。
   - 托管端的素材服务向同机文档服务登记地址，属于管理接口，走回环地址并带集群令牌。
6. **依赖**：只用 Node 内置能力。要装依赖先问用户。

| 子分支 | Agent | 内容 | 端口段 |
|---|---|---|---|
| `claude/sp-hosting` | `opus-dev-high` | 阿里云托管组合：素材服务上线（`fs-store`、公网端口、登记地址）、配置项、数据目录布局；部署脚本同时部署两个服务；按 `hosting-migration.md` 实现导出与导入 | 5470～5479 |
| `claude/sp-routing` | `opus-dev-high` | 新建与打开时两种放法的路由；客户端的缺省托管地址与覆盖；放本机时的本机主机（绑局域网地址、失败即关、管理接口只绑回环）；组播发现 | 5480～5489 |
| `claude/sp-tests` | `opus-dev` | 照契约独立写；本机起一份「托管组合」模拟阿里云；探针 `scripts/probes/shared-project-probe.mjs`（`--mode internet\|lan`、`--role creator\|member`，一行 JSON 出结果） | 5490～5499 |

**Worker 交互 W6**：
- **放本机（局域网直连）**：主 PC 建项目，笔记本在同一网段发现并加入；
- **放云端**：
  - 按授权把托管组合部署到阿里云；
  - 主 PC 建托管项目；
  - 笔记本换到手机热点，不设集群令牌，凭项目凭证进入。

**验收**：

| 编号 | 场景 | 标准 |
|---|---|---|
| SP1 | 托管的新建与进入（本机模拟托管组合） | 另一客户端不设集群令牌，凭项目凭证进入成功；项目快照读取、带票据的素材读和写都成功 |
| SP2 | 令牌的边界 | `page`、`agent`、`render` 三种角色以及独立主机，都不带集群令牌，凭项目凭证进入：成功。凭证错：被拒。托管端的管理接口不带令牌：401。局域网主机的管理接口从局域网另一台机器访问：连不上；从本机访问，不带令牌：成功 |
| SP3 | 数据都在托管端 | 发布、认领、完成一轮之后，全部成员断开，新成员从托管端取回全部内容：项目快照、内容库条目、素材、预渲染产物，逐项计数与断开前一致，且都落在 `PROMPTCUT_DATA_DIR` 之下 |
| SP4 | 放本机（局域网直连） | 同网段成员 5 s 内发现并接入，全程不连阿里云（托管端的连接数计数不变）；不同网段的成员发现不到 |
| SP5 | 打开时的路由 | 只有局域网有：直接进。只有托管有：直接进。两边都有：并列供挑。两边都没有：明确提示。四种各测一遍 |
| SP6 | 地址是配置 | 服务端改配置后按新地址宣布与登记。客户端的三种情况：<br>不设覆盖时连内置缺省；设了 `PROMPTCUT_HOSTED_URL` 时连它；界面上改了时连界面上的。<br>守门测试：缺省托管地址只在一处常量里定义，除此之外源码里 0 处写死 `8.219.80.16` |
| SP7 | 迁移演练（本机） | 按 `hosting-migration.md`，把一份托管组合的数据目录拷到另一份实例、改地址之后：项目、素材、预渲染产物全部可用；`projectRev` 连续，不归零；旧产物的块哈希不变，不重新预渲染 |
| SP8 | 跨机 W6 | **放本机（局域网直连）**：同 SP4。**放云端**：笔记本在手机热点下凭项目凭证进入托管项目，完成一轮发布、认领、完成，每个任务恰好一次 `task.done`，产物落在阿里云的素材服务上 |

- G0 通用门槛。本阶段不动渲染路径，G0-R 不跑，报告写明。
- 部署到阿里云、重启远端服务，由主会话直接做，每次记进 SP 报告（第 10 节）。
- 托管端的资源：2 核 CPU、2 GiB 内存、40 GiB 系统盘（已用 4 GiB）、峰值带宽 200 Mbps，位于新加坡（用户 2026-09-26 告知）。SP 验收时记录托管端的磁盘占用、内存和带宽，写进报告。

### C6.5 项目真身进文档服务，加共享项目界面（专项）

**范围**：
- 第 11.2 节登记的 C6.5：D1、D2、D4，操作格式与撤销 / 重做语义；
- 页面直接发布 `plan`；
- 内容库写入区分会话；
- D11 的两项界面。

**开工前**：
- 设计稿（操作格式与撤销语义）交用户审，涉及语义的改动逐句确认。
- 设计稿可以在 M6、SP 期间写好，但开工在 SP 合入之后。
  - 技术部分由主会话和 Opus 定；
  - 「用户感知到的撤销 / 重做行为」一节由 Gemini 出稿（0.2 节补充）。
- 下面的两项菜单、成员列表、创建者操作入口，以及进入失败、「本机关机后打不开」等提示，先由 Gemini 出交互流程稿和文案。主会话核对它与 `user-workflow.md`、`workflow/project.md` 的语义一致后，再交 Opus 实现。

> 〔2026-09-26 第二轮〕下面这两项菜单（旧入口）已被「多用户协作」取代：项目设置里的勾选加开始页的加入表单（`docs/semantics/workflow/project.md`），改造在 C10a。这里保留 C6.5 当时的做法作记录，入口名照当时界面上的文字写。

**D11 界面**，放在编辑界面左上角已有的「项目」菜单（`src/editor/TopBar.tsx` 的项目下拉），加两项：

- **「新建共享项目」**（旧入口）：
  - 要填：项目名、创建者用户名、创建者密码；二选一：自由进入 / 限定进入；二选一：放云端（托管在阿里云）/ 放本机（本机当主机）。
    - 自由进入还要设项目密码；
    - 限定进入可以当场加名单，名单每条是用户名加密码。
  - 选了模式，文档服务和素材服务的位置就定了，不提供别的放法。
  - 选放本机时，界面提示「本机关机后其他成员打不开这个项目」。
- **「打开共享项目」**（旧入口）：
  - 要填：项目名；自由进入时填项目密码和自报的用户名；限定进入时填名单里的用户名和密码。
  - 按 SP 的路由查找。局域网和阿里云都有同名项目时，并列供挑；都没有时明确提示。
  - 托管地址显示内置的缺省值，允许改（SP 设计要点第 2 条）。
- **配套**（用户已接受）：
  - **成员列表**：
    - 按设备聚合，一台设备一行，显示名的重名规则照 D9 第 4 条；
    - 后面挂标签：[编辑中]（有 `page` 连接）、[渲染中]（有 `render` 连接且在做任务）、[Agent ×n]（`agent` 连接数）；
    - 展开后逐个显示「用户名 · Agent · 第几个对话」。
  - **创建者操作入口**：三项特权，每次操作都要当场输入创建者密码；限定进入时，名单里可以给独立渲染主机加一条。
  - 没有这两块，D9 的踢人、改名单做不出来。

| 子分支 | Agent | 内容 | 端口段 |
|---|---|---|---|
| `claude/c65-docservice` | `opus-dev-high` | D1、D2、D4 的文档服务侧；操作日志与撤销语义按用户审过的设计稿；内容库写入带 D9 的 `principal` | 5500～5509 |
| `claude/c65-editor` | `opus-dev-high` | 页面改为经文档服务提交操作、直接发布 `plan`；D11 两项菜单、成员列表、创建者操作入口 | 5510～5519 |
| `claude/c65-tests` | `opus-dev` | 照设计稿与 `auth-contract.md`、`shared-project-contract.md` 独立写 | 5520～5529 |

**验收**（界面部分；项目真身部分的验收随设计稿定）：
- G0 通用门槛，加上 G0-R（改了编辑器数据流）。
- U1：用旧入口「新建共享项目」时，两种进入方式和两种放法的四种组合都能建成：
  - 放云端建在托管端（本机模拟的托管组合，跨机时是阿里云）；
  - 放本机在本机起文档服务和素材服务，并开始广播。
- U2：另一台机器用旧入口「打开共享项目」进入后，看到的项目与托管端或局域网主机的 `projectRev` 相同；在上面改一处，创建者 5 s 内收到。
- U3：成员列表按设备一行：
  - 重名者带「(设备名)」；
  - 标签与该设备实际打开的连接一致，一台桌面版显示 [编辑中] [渲染中]；
  - Agent 展开后格式正确；
  - 不是创建者的成员，看不到创建者操作的入口。
- U5：Gemini 的交互流程稿与文案、主会话的核对结论，都附在报告里。
- U4：界面截图贴进报告，按验证流程在浏览器里实际点过。

### C6.6 两档素材与换档、卡片源码同步（设计稿 `docs/plan/c66-design.md`）

- 四个子分支已推送、未集成，状态见 `docs/reports/PAUSE-2026-09-26.md` 第 2 节。顺序：两项 codex 攻坚（worktree 模式）→ 集成、跑 G0-R → 部署到阿里云 → T9 → 合入、写 `REPORT-C6.6.md`。
- **T9 改写**（2026-09-26 第二轮，设计稿第 6 节同改）：
  - 笔记本以创建者身份跑桌面版，导入、素材小尺寸的生成、预渲染都在笔记本上做；项目放云端。
  - **观察端**：PC 桌面版以成员身份加入（PC 窗口项，优先）；PC 不在线时由笔记本上第二个成员实例代替（本机替身，6.8 节）。断言：素材先以素材小尺寸、后以素材原尺寸出现；笔记本改过的卡片源码自动装上。
  - **云端在 T9 里当渲染节点**（2026-09-27 改定）：以独立渲染主机身份加入，认领并完成笔记本发布的预渲染任务；这一项同时覆盖原 HT9 的硬验收。云端不装页面依赖、不当观察端：2026-09-27 实测无头 Chromium 里的 WebSocket 过不了它的代理。
  - 查过代码（2026-09-26）：页面加入共享项目只开 `page` 连接（`src/editor/sync/syncManager.ts`），不会因此登记为渲染节点；编辑器只有启动时设了 `PROMPTCUT_QUEUE_NODE=1` 又给了 `PROMPTCUT_SHARED_CONFIG`，才起队列节点（`server/vite-plugin-frames.ts`）。观察端起编辑器时这两个变量都不设。语义「加入共享项目的桌面应用自动成为渲染节点」在代码里落地之后，要给观察端留一个开发者开关关掉它。
  - 云端起 Chrome 以 root 运行要 `--no-sandbox`（`PC_CHROME_ARGS`）；它的 Chrome 只连本机舞台，不出网，不涉及代理证书。
- **PC 窗口项**（不挡合入）：放本机版 T9，笔记本当主机，PC 经局域网直连加入，同样断言先小后大、卡片源码自动装上。
- **验收**：设计稿第 6 节 T1～T9 与通用项。

### C10a 在线浏览器模式 demo 最小可用（2026-09-26 第二轮新增）

- **排在**：C6.6 合入之后、C10 其余之前。
- **范围**：
  1. **入口**：阿里云托管端直接提供编辑器页面，地址是服务器地址加 `/editor`（`https://8-219-80-16.sslip.io/editor`）：静态构建加 nginx 路由。页面与 `/hosted`、`/media` 同源，打开即是编辑器，不需要安装（语义 `product/platforms.md`「在线浏览器模式」的入口）。
  2. **开始页的加入表单**：项目名、项目密码、自己的用户名；限定进入时用名单里的用户名和密码。**邀请码**是文档服务签发的令牌（`workflow/project.md`「多用户协作」、`product/document-service.md`「共享项目与权限」）：邀请链接是 `/editor` 加 `#` 片段里的邀请码，不带项目名和密码，`#` 片段不发给服务器、不进 nginx 日志；页面读到邀请码就跳过项目名和密码两项，用户只填用户名。桌面版与在线浏览器模式一致。
  3. **项目设置里的「多用户协作」勾选**，取代 C6.5 的两个旧入口。本轮至少做放云端那一支：勾上时选放云端，项目建在阿里云上，显示邀请链接与二维码；创建者可作废并重新生成邀请码（云端文档服务签发与核对，可限时限量，具体时限与次数在 C10a 契约里定）。放本机与向云端托管服务登记，留给直连计划。
  4. **低内存档**（手机、iPad 浏览器）：只看预渲染小尺寸与素材小尺寸、轻量修改、逐帧导出；不预渲染、不当渲染节点、暂停时不自己追精确画面（语义 `product/platforms.md`「面向的平台」；实现要点见 `cloud-task.md` L6）。
  5. **预渲染小尺寸的生成与拉取**：渲染节点产出预渲染原尺寸后，一并生成小尺寸，两档都推到素材服务；低内存档只拉小尺寸（语义 `product/rendering.md`「重管线：预渲染」）。缩多小、谁缩，在 C10a 契约里定。
  6. 页面的 HTTP 长轮询回落在这里接上：页面与 `/hosted` 同源，不用开跨源（HT 契约第 2 节）。
- **开工前**：
  - 主会话写 C10a 契约：入口与构建、部署、加入表单、「多用户协作」勾选的放云端一支、低内存档的判定与能力闸、预渲染小尺寸；
  - Gemini 出加入表单与「多用户协作」勾选的交互稿与文案，主会话核对它与 `workflow/project.md` 一致；
  - 按「查资料先行」查手机浏览器的内存上限与后台节流、邀请令牌的生成与作废做法（长度、有效期、`#` 片段承载）。

| 子分支 | Agent | 内容 | 端口段 |
|---|---|---|---|
| `claude/c10a-web` | `opus-dev-high` | 入口、构建与 nginx 路由、开始页的加入表单、「多用户协作」勾选（放云端）、邀请码与二维码、页面的 HTTP 回落 | 5630～5639 |
| `claude/c10a-lowmem` | `opus-dev-high` | 低内存档的能力闸、逐帧导出、预渲染小尺寸的生成与拉取 | 5640～5649 |
| `claude/c10a-tests` | `opus-dev` | 照契约独立写 | 5650～5659 |

**验收**：
- G0 通用门槛，加上 G0-R（改了预览与导出路径）。
- **手机 demo**：手机扫二维码打开 `/editor`，页面凭邀请码识别项目，只填用户名即加入笔记本建在云端的项目；创建者作废邀请码后旧二维码被拒；看到预渲染小尺寸与素材小尺寸；在手机上改一处，由笔记本（渲染节点）重渲，结果回到手机。截图与操作记录贴进报告。
- 低内存档逐帧导出一段：导出用素材原尺寸与预渲染原尺寸；原尺寸没到齐时提示等待上传方。
- 桌面版的开始页加入表单与在线浏览器模式一致。
- PC 窗口项：无。

### HT-a 文档服务的会话模型（2026-09-26 第二轮新增，2026-09-27 拆分）

- **契约**：`docs/plan/http-transport-contract.md` 第 2 版。本阶段只做「会话模型 + 序号确认」接到 WebSocket 传输上，加本机信任开关；HTTP 长轮询传输是 HT-b，记入 `TODO.md`，第 1 版代码留在分支上不接线、不验收。语义：`product/document-service.md` 与 `mechanism/document-service.md` 的「会话与传输」、`product/platforms.md`「只能出网的节点」、「本机」按真正的发起方判断（`product/document-service.md`「共享项目与权限」、`asset-service` 两本）。
- **排在**：C10a 之后、C10 其余之前（第 4 节，2026-09-27 改定）。
- **实测**：2026-09-26 写计划的容器与 2026-09-27 云端工作节点的容器都证明，Node 22 的 WebSocket 经代理可用，不带凭证的握手到达阿里云回 401；此时 Node 的 `WebSocket` 只触发 `onerror`、不触发 `onclose`，客户端不能只等 `onclose`，HT-a 实现时核对 `ws-transport.mjs` 的重连逻辑。无头 Chromium 里的 WebSocket 过不了该代理（握手回 200 / 404，`--proxy-server`、`--disable-http2` 无效），所以云端只当渲染节点，不当页面观察端。
- **返工**：`claude/http-transport`（`7477acd`）按契约第 15 节返工；第 1 版的裁定与实现记录逐条保留或废弃，见契约第 14 节。

| 子分支 | Agent | 内容 | 端口段 |
|---|---|---|---|
| `claude/http-transport`（续用） | `opus-dev-high` | 服务端：会话层 `session.mjs`、WebSocket 传输接会话层、`/healthz` 的 `sessions`、本机信任开关、部署脚本的开关与公网地址参数；HTTP 传输代码保留不接线 | 5600～5609 |
| `claude/ht-client` | `opus-dev-high` | 客户端会话层 `createDocEndpoint`；接入 `vite-plugin-frames.ts` 三处、`doc-link`、页面 `SyncLink`、探针；删共享项目配置的 `transport` 字段 | 5610～5619 |
| `claude/ht-tests` | `opus-dev` | 照契约独立写 HT1～HT6 | 5620～5629 |

**验收**：
- G0 通用门槛。HT 不动渲染路径，G0-R 不跑，报告写明。
- 契约第 11 节里 HT1、HT2、HT4、HT6、HT7 全过；HT3 只跑「只走 WebSocket」与「中途断开再接续」两种；HT5 只跑自动与强制 ws；HT8 归 HT-b；HT9 由 C6.6 T9 的云端渲染节点覆盖。其中用户点名的：
  - **本机信任**：HT6 的单测，加 HT7 从外网经 443 的三项拒绝（匿名 WebSocket 升级被拒、匿名读素材 401、不带令牌调管理接口被拒）；
- 跨机 W-HT-a：云端以独立渲染主机持有任务时传输断一次，会话接续、租约不丢、任务完成；本机替身是笔记本第二渲染主机实例（6.8 节）。
- **PC 窗口项**（不挡合入）：PC 当第二渲染节点做同样的断线接续。
- 阿里云的部署、nginx 改动、重启，逐次记进 `docs/reports/REPORT-HT-a.md`。

### C10 其余：在线浏览器模式 L1 / L2（`cloud-task.md` 第 10 步）

- 依赖 J 全部、A3b、K 全部（`cloud-task.md`「读法」）。开工前主会话要核对 R 系列的现状（`TODO.md` 记着 R 系列还有遗留），缺的前置先报用户。
- 2026-09-26 增补：
  - 依赖 C6.5、C6.6，以及 C10a（2026-09-26 第二轮）。
  - 浏览器进入共享项目的方式：
    - 放云端：页面由托管端直接提供（`/editor`，C10a），与 `/hosted` 同源；
    - 放本机（局域网直连）：浏览器不能收发 UDP 组播，发现不了主机，要手填主机地址，或由同网段桌面版给出的链接打开〔裁，开工时细化〕。这部分是 **PC 窗口项**。
  - 页面从哪里来：已定，由托管端经 443 直接提供（C10a），不再有混合内容的问题。
  - 用户侧流程先由 Gemini 出稿（0.2 节补充）：离线提示、素材导入置灰；加入表单已在 C10a。主会话核实后再交 Opus。
  - 开工前按「查资料先行」查 IndexedDB 配额、浏览器媒体元素的鉴权限制。
- 分支 `claude/c10-browser`（`opus-dev-high`）、`claude/c10-tests`（`opus-dev`）；端口段 5420～5429、5430～5439。
- 验收：G0 通用门槛，加上 G0-R，加上 `cloud-task.md` L 节的验收（含 L6 低内存档），环境是纯浏览器。放本机的项目经局域网直连的那一遍是 **PC 窗口项**，不挡合入。

### M7 纯浏览器节点（任务书 M7）

- 分支 `claude/rq-m7-node`（`opus-dev`）、`claude/rq-m7-tests`（`opus-dev`）；端口段 5440～5449、5450～5459。
- 内容：后台舞台闲时作为 `profile: 'browser'` 节点认领快照任务；用浏览器能带的方式携带凭证（子协议）。
- **跨机 W7**（第 6.5 节）：另一成员的浏览器可以是笔记本上的第二个浏览器、云端的无头 Chrome，或 PC 上的浏览器（PC 窗口项）。
- 验收：
  - G0 通用门槛；
  - E5：纯浏览器节点只见本人任务。同一共享项目另一成员的任务，在分发器和队列两层都被挡住：它收到 0 条，认领被拒 `forbidden`。自由进入下同名不同设备算两个人，也要挡住；
  - B2：只认领 `light` / `medium` 的快照任务，`heavy` 与 `stream` 任务被它认领 0 次。

### M8 多端物理联调（任务书 M8）

- 分支 `claude/rq-m8`（`opus-dev`）。只写探针和报告，原则上不改生产代码；发现缺陷就回到对应阶段的分支修。
- 参与方（2026-09-26 第二轮）：
  - 笔记本：主会话；共享项目的创建者、发布方、PC 节点、独立渲染主机；
  - 云端：渲染节点；
  - PC：辅助测试节点，本阶段的项**默认必须 PC**（第 6.4 节）。另建一个放本机的项目（笔记本或 PC 当主机，本机跑文档服务和素材服务），做放本机的项目与连接路径用例、第二渲染节点、主机与预渲染分离；
  - 远端：`8.219.80.16` 上的托管组合（文档服务 + 素材服务），M5～M8 唯一的文档主机。
- **跨机 W8**（第 6.5 节）。
- 验收：
  - G0 通用门槛，加上 G0-R；
  - 任务书第 6 节 E1～E6 在真实多端环境下全过：放云端的项目一遍；放本机的项目经局域网直连一遍（必须 PC）；
  - K1、I1 在真实多端环境下复测，结论与本机一致；
  - **异地接入托管**（D10 修订）：笔记本在手机热点下，不设集群令牌，凭项目凭证进入托管项目，完成一轮发布、认领、完成，每个任务恰好一次 `task.done`，产物落在阿里云的素材服务上；
  - **换机迁移**（用户 2026-09-26 要求）：
    - 演练在同一台阿里云服务器上起第二份实例，另用一组端口（从 8700～8799 里选），不临时租服务器（用户 2026-09-26 定）；真正的换机等团队测试通过、用户换新服务器时再做一次；
    - 按 `docs/plan/hosting-migration.md` 把托管组合整体迁到第二份实例，客户端在界面上改托管地址；
    - 迁移之后，项目、素材、预渲染产物全部可用：`projectRev` 连续；素材和产物按哈希逐个取回，校验通过的比例为 100%；已就绪的层不重新预渲染；
  - 混沌项：
    - 笔记本物理断网 30 s；
    - `pm2 restart` 阿里云的托管组合；
    - 代理丢包 10%；
    - 放本机的项目的主机素材服务重启（地址重新下发；必须 PC）；
    - 传输中断后会话在保留期内接续，消息不丢、不重（HT-a）。

    每一项下都满足：任务全部完成、每个任务恰好一次 `task.done`、没有一层混了两种指纹；
  - 报告 `docs/reports/REPORT-render-queue-m8.md`。

---

## 8. 量化验收标准

### G0 通用门槛（每个阶段合并前必须全部满足）

| 项 | 命令 | 标准 |
|---|---|---|
| 类型检查 | `npx tsc -b --force` | 退出码 0，**零错误** |
| 全量测试 | `npm test` | **失败 0**；跳过数 ≤ 1（只允许那条需要 5190 的）；非跳过用例通过率 100%；总条数 ≥ 合并前 main 的条数加本阶段新增的条数（当前基数 2194） |
| 本阶段新增测试 | 各阶段列出的测试文件 | 100% 通过；每条用例名里带编号，便于对账 |
| 独立探针 | 各阶段列出的 `scripts/probes/*.mjs` | **退出码 0**，`fails: []` |
| 压测与对照 | I、K 系列 | 按各表的数值门槛；实验组与对照组的原始数字都贴进报告 |
| 跨机断言 | 对应的 W 编号（第 6.5 节） | 表里每条都通过，云端（经信箱）或 PC 会话（经跨会话消息）回传的原始输出贴进报告；没做的 PC 窗口项在报告里列出（第 6.4 节） |
| 报告 | `docs/reports/REPORT-<阶段>.md` | 写明：做了什么、每项验证的命令与结果、没跑的项和原因、与对齐时不一致的地方、语义冲突、需要用户决定的事；另有一节「顾问调用记录」（0.2 节补充），一次都没调的阶段要写明为什么 |
| 主工作区 | `git status` | 干净；主工作区没有被子 Agent 写过 |

### G0-R 渲染附加项（改到渲染、预渲染、导出、卡片时必跑）

| 项 | 命令 | 标准 |
|---|---|---|
| 导出确定性 | `node scripts/verify-determinism.mjs --url "http://127.0.0.1:<端口>/?export=1"` | 1800/1800 帧逐像素相同 |
| 快照重放一致 | `node scripts/verify-unified-frames.mjs` | PASS |
| 导出像素基线 | 临时起 main 的 worktree，逐像素比两边导出 | **0 不同、0 缺失**；要变须事先经用户确认 |
| 预渲染探针 | `ready-index-probe`、`stream-produce-probe`（含 `--group`）、`preview-fallback-probe`（含 `--page-preload`） | 全部退出码 0 |

---

## 9. 端口、路径、资源与风险

**端口**：

- 要避开的：
  - 5190～5192：用户常驻；
  - 5203～5205：`dev-test`；
  - 5230～5232：探针惯用；
  - 5240～5259：M1～M4 各角色用过；
  - 8787：远端文档服务；8788：远端素材服务（SP 起）。
- 本文分配 5260～5529，按第 7 节各表，每段 10 个（2026-09-26 增补 5460～5529 给 M6 鉴权、SP、C6.5）；本机测试用的文档服务在 8790～8799，单测一律用端口 0。直连计划另用 5530～5559。2026-09-26 第二轮增补：PC 会话 5560～5579（避开 PC 上用户常驻的 5190～5192），云端会话 5580～5589，HT 5600～5629，C10a 5630～5659。
- 阿里云安全组放行了 TCP / UDP 8700～8799 与 ICMP（用户 2026-09-26 告知），托管组合的公网端口只在这个段里选。
- 局域网组播的地址和端口在 `shared-project-contract.md` 定，要避开本机的「坏端口」（见 `docs/local.md`「本机端口」）。
- 主 PC 素材服务对局域网开放的端口在 C5 开工时定，由用户在 Windows 防火墙放行。

**路径**：`.claude/launch.json` 只在验证时临时加条目，用完还原（`preview_start` 只读主工作区的 launch.json）。

**风险**：

| 风险 | 影响 | 对策 |
|---|---|---|
| 令牌明文过公网（D4、S4） | 截获者能冒充节点或发布方 | 用户已接受当前阶段的风险；生产环境由外部网关解决。控制面只存地址和任务元数据，不经手字节 |
| 单一集群令牌分不出用户（S3） | M5 做不到用户、租户隔离 | D9 已定稿（第 12 节），M6 落实；M5 不验依赖用户边界的用例 |
| 文档服务通用化后模块互相干扰 | 一个模块的慢处理或大量广播拖累别的模块 | `handle` 同步且不阻塞核心；出站上限按连接统一管（第 5.1 节）；R1 验证两个模块同时在线互不干扰 |
| 主 PC 素材服务的可达性（D7） | 笔记本不在同一局域网或被 NAT 挡住时，W2 起的跨机产物路径不通 | W0 就探测能否访问主 PC 的局域网地址；不通按回退第 3 级处理 |
| 半开连接要约 60 s 才被判断开 | 断网后接手变慢 | 租约 30 s 兜底，X6 按 37 s 验收；需要更快就在契约 G 节把心跳参数化 |
| 两台机器指纹不同 | 跨机复用只能靠卡片级指纹锁 | W3、W4、K3 专门断言 |
| 慢消费者拖垮控制面 | 出站积压、内存涨 | 5.1 节的出站上限与 `1013` 关闭；I3 压测 |
| C6 规模大，`cloud-task.md` 的折叠稿没经过独立审查 | M5b 长期被卡住 | C5 开工前先让 agy 做独立审查；C6 开工时再拆 |
| C10 依赖 R 系列（J、K） | M7 被卡住 | C10 开工前核对前置 |
| 按代码版本分池（设计第 7 节说「落地成本最大」） | M6 工期长 | M6 开工前按「查资料先行」调 GPT 查 worker 池与多版本并存的做法，方案由主会话与 Opus 定（Gemini 不做架构审查） |
| 阿里云轻量服务器资源小（2 核、2 GiB 内存、40 GiB 盘已用 4 GiB、峰值 200 Mbps，新加坡） | 连接多了顶不住；SP 起素材和预渲染产物也存在它的磁盘上，磁盘和出口带宽可能先满 | 远端不跑渲染。SP 验收记录托管端的磁盘占用与带宽；M8 记录 CPU、内存、磁盘、流量。满了按迁移步骤换机，OSS 插槽留到 M8 之后 |
| 所有放云端的项目都在一台阿里云上（D10 修订） | 这台机器宕机，所有托管项目都打不开 | 数据目录可整体拷走、地址是配置项，按 `hosting-migration.md` 换机；M8 验收迁移 |
| 放本机的项目，创建者本机是项目的唯一所在 | 创建者关机，其他成员就打不开项目 | 用户定的形态，界面上明确提示；可在项目设置里搬到云端 |
| 托管端在反向代理之后，把代理转进来的请求当成本机（2026-09-26 出现过约 4 分钟） | 匿名者得到本机身份、免票据读素材、免令牌调管理接口 | HT：`PROMPTCUT_TRUST_LOOPBACK=0`，接到握手、共享端点、素材服务、管理接口四处；HT6、HT7 验收；nginx 的 `proxy_bind` 保留作多一层 |
| PC 间歇在线 | 需要局域网第二台机器的用例迟迟做不了 | PC 窗口项，不挡阶段合入（M8 的除外）；主会话派任务前用 ListAgents 看状态，offline 只登记；未做的列进报告，下次报到后补做（第 6.4 节） |
| 云端容器的出网环境变化（代理策略、证书） | 云端接入或无头 Chrome 失效 | 每次开工 W-开工复测；云端只当渲染节点，不当页面观察端；WebSocket 被挡时启动 HT-b（`TODO.md`） |
| 票据、会话在公网明文传输（S4 延续） | 截获者能在票据有效期内冒用 | 密码不上线（挑战应答）；票据 15 分钟过期、改密码全部作废；加密仍按 S4 不做，写进报告 |

---

## 10. 授权范围（2026-09-26 改定，同日第二轮修订）

- M6 到 M8 期间，阶段验收全过即由主会话自动合并，并进入下一阶段。改写 main 的历史仍须授权。
- 远端 `8.219.80.16` 的部署、重启、装软件、改配置，由主会话直接做，在阶段报告里记录。
- **新增费用先问用户**：换机型、加带宽、租外部服务，任何阶段碰到都先问（`docs/semantics/guide_files/constraints.md`）。新增依赖、装软件不再要单独同意（第二轮删去）。
- **改语义**：按 `docs/semantics/guide_files/suggested_agent_behavior.md`「对齐」的按阶段规则（取代原来的「发给用户但不等」）：
  - 定计划时，一级、二级、三级的语义改动都先 dry run，用户确认后才写；
  - 执行计划时按现有语义做不下去，先穷尽解法；都不行时，一级停下问用户，二级、三级由主会话最小修改、标〔裁〕，二级当场用 `task-announce` 播报，用户合入 main 前审（0.2 节第 4 级）。
- **由用户完成**：Windows 防火墙与阿里云控制台安全组；打开或关掉 PC 会话；需要人在机器旁做的物理操作（切网络、扫码）。Git 推送、阿里云管理接口的集群令牌、信箱，由主会话做（0.3 节）。
- **跨机指令**：给云端经信箱、给 PC 经跨会话消息下发（第 6 节）。每条指令下发前先在对话里列出内容，指令与回执的原文写进阶段报告。
- **发给用户但不等**（非语义的，2026-09-27 补回）：设计稿、契约、需要用户拍板但不涉及语义的问题，写清楚发给用户，同时按主会话认定的最优方案继续推进，标〔裁〕；用户有异议会插话。改语义不走这条，按上面「改语义」。
- **裁定权在终点之下**（2026-09-27 补）：主会话对执行路径、技术选型、排期有裁定权，但不得用它缩小范围。非前提项可以推迟，必须排在 M8 之内；第 11.2 节列的前提阶段不得推迟到 M8 之外。

原来这里的「用量闸」（额度到 90% 暂停）整段删去（2026-09-26 第二轮）。原来还列过「每个阶段开工」「每次合并进 main」「每次部署或重启远端」三项，已删去；M5 期间按原规定执行。

---

## 11. 权限边界与推迟项登记

### 11.1 现行权限边界（用户 2026-09-25 第二次下达，取代同日第一次的补充授权）

1. **目标与裁定权**：终极目标是完整实现 M5～M8 的全套计划。在这个前提下，主会话对执行路径、技术选型、任务排期有动态调整权和最终裁定权。
2. **顾问**（用户 2026-09-26 重定）：
   - GPT / codex（`gpt-6-sol` / `high`）：写契约前查资料；接手需要反复试错的攻坚，在分支上自己迭代（0.2 节第 2 级）。
   - Gemini（`gemini-3.1-pro-high`）：交互流程与文案；卡死时的发散方案，至少三条（0.2 节第 3 级）。不做架构审查，产出一律先验证再采纳。
   - 每个阶段的报告都写「顾问调用记录」（0.2 节补充）。
3. **独立评估**：外部模型只提供辅助视角，缺全局上下文。主会话基于现有代码与里程碑目标自行评估，可以采纳、修正或否决。
4. **推迟**：不影响当前阶段连通性目标的非核心功能，可以推迟到后续阶段，但**必须在 M5～M8 之内排上**，不许丢弃。
   - SP、C6.5、HT-a、C6.6、C10a、C10 是 M8 之后团队测试的前提，不得推迟到 M8 之外；推迟只能在 M8 之内挪位置（用户 2026-09-26）。
5. **不挂起**：常规设计细节没定义时，由主会话自行裁定、推进落地。只在两种情况下中断汇报：需要物理干预（权限、环境配置），或者经过充分探索仍无法绕过的底层死锁。
   - 「经过充分探索仍无法绕过的底层死锁」按 0.2 节第 5 级定义（第二轮前是第 4 级）：原因网格逐格扫完，codex 和 Gemini 各补一轮新假设也扫完，两家都给不出新思路。失败次数和耗时都不是中断理由；暂时性故障无限重试加退避，也不是中断理由（用户 2026-09-26）。
   - 2026-09-26 第二轮起，「物理干预」只剩三类：Windows 防火墙与阿里云控制台安全组；新增费用；切网络、扫码、打开 PC 会话之类只能由人在机器旁做的事。
   - 远端服务器上的部署、重启、装软件、改配置，Git 推送，阶段之间的合并与推进，都由主会话自己做，不再为此中断（0.3 节、第 10 节）。
6. **语义不变**：语义已经定下的内容照语义做。改语义按 `suggested_agent_behavior.md`「对齐」的按阶段规则（第 10 节，2026-09-26 第二轮）。
7. **授权贯穿 M6～M8**（用户 2026-09-26）：M6 起一次性授权到 M8，阶段验收全过即自动合并并进入下一阶段。阶段报告照写；M8 收尾时出总验收报告，并按全局约定播报。
8. **裁定留痕**：主会话自行裁定的地方一律标〔裁〕，每处写明定了什么、为什么（用户 2026-09-26）。

### 11.2 推迟项与它们在 M5～M8 里的新位置

| 项 | 推迟原因 | 排到哪里 |
|---|---|---|
| C6.5：D1、D2、D4，操作格式与撤销语义 | 用户 2026-09-25 裁决移出本轮 M5；语义未定 | **SP 之后、C6.6 之前的专项「C6.5」**（2026-09-26 从「M6 之后、M7 之前」细化，理由见第 4 节）：先写设计稿交用户审（改语义需确认），再落地；D11 的两项界面并在这里（第 7 节 C6.5）。C10、M7 需要文档服务持有项目真身 |
| C6.6：A1 两档与换档、A6 卡片源码同步 | 不阻塞 M5 的连通性目标 | **C6.5 之后、C10 之前**：在线浏览器模式（L）依赖两档素材与卡片源码同步 |
| 页面直接发布 `plan` 任务（M5b 由预渲染进程代发） | M5b 连通性不依赖它 | **随 C6.5**：页面接入文档服务的操作流时一并改 |
| 轨道流走队列（M5b 只队列化快照任务） | 流的编码器签名与跨机复用有专门的规则，M5b 连通性不依赖它 | **M6**：独立渲染主机需要能产流 |
| 没有哈希的素材在远端节点上的能力闸（`requires.localMedia`） | M5b 靠 `plan-mismatch` 拦住本地档 | **M6** |
| 全量 `watch: 'all'` 的收紧（第 5.1 节第 3 条） | 摘要订阅已经提供；现有本机节点依赖全量订阅 | **M6**。D9 之后 `watch` 的范围再收窄到主机已凭凭证加入的项目；阿里云托管端承载所有放云端的项目，摘要订阅在那里最有用 |
| 同一用户不同会话写内容库时的区分 | 现在只有一种写入方 | **随 C6.5**：身份字段用 D9 的 `principal`（用户、设备、页面或 Agent 对话，第 12.3 节），M6 先把字段带上 |
| `plan` 任务的指纹不过滤认领（由谁来做计划） | M5b 两台机器指纹相同，不影响连通 | **M6**：独立渲染主机上线时定「发布方就近认领计划」 |
| 本机队列节点的闲时门槛放宽（现在要等 preload 到 `ready`） | 只影响本机节点起步的早晚 | **M6** |
| 快照 style 声明顺序跨进程确定化 | 同样内容的块哈希不同，会重复上传；不影响正确性 | **M6**（`src/render/snapshot/inlineStyles.ts` 一侧） |
| 远端节点渲的卡在本机没有 PNG 缓存（`?preview=legacy` 下是占位） | 只影响回滚预览 | **M6** |
| 浏览器在放本机的项目里发现不了主机（浏览器不能收发 UDP 组播） | 不影响放云端的项目；放本机时桌面版照常发现 | **C10**（PC 窗口项）：手填主机地址或由桌面版给出链接，开工时细化 |

**用户明确不做的，或排在 M8 之后的**（2026-09-26，不属于推迟，不在 M5～M8 里排）：
- 本机项目（2026-09-26 用户定，同日第二轮修订）：M5～M8 的主线只验证项目放云端。放本机的项目只验局域网直连（含局域网发现），作为 PC 窗口项（第 6.4 节；M8 里必须 PC）；公网直连与云端托管服务的中继属于 `docs/plan/direct-connect-plan.md`，不在 M5～M8 之内；
- 第三方登录、密码找回；
- 公网直连（打洞、端口映射、IPv6）与云端托管服务的中继见 `docs/plan/direct-connect-plan.md`（未开始），不在 M5～M8 之内；
- 素材服务的 OSS 数据层：插槽保持为空，M8 之后再做。托管端在 M5～M8 里一直用轻量服务器本地磁盘上的 `fs-store`。

---

## 12. D9：用户、共享项目与权限（2026-09-26 定稿）

### 12.1 用户定下的十条（第 10 条是 2026-09-26 第三轮补的）

1. **隔离单位**是「共享项目」。M6 里的 `tenantId` 就是共享项目 id。
2. **两种进入方式**，创建项目时二选一：
   - **自由进入**：知道项目名和项目密码的人都能进，进入时自报用户名；
   - **限定进入**：创建者维护一份允许进入的名单，每条是用户名加密码，只有名单里的人能进。
3. **创建者**：创建者账户必带密码，特权绑「创建者用户名 + 创建者密码」。特权只有三项：
   - 改项目密码或名单（2026-09-27 补：邀请码的作废与重发归入此项，见 `workflow/project.md`）；
   - 删项目；
   - 踢人。

   其余操作人人平等。
4. **设备名**：软件按硬件信息生成设备名。自由进入下用户名可以重名，重名者显示时自动带「(设备名)」。
5. **Agent**：
   - 沿用所属用户的凭证，显示为「用户名 · Agent · 第几个对话」，界面用标签区分真人与 Agent；
   - 写入身份沿用语义里的「哪个用户的哪个页面，或哪个 Agent 的哪个对话」。
6. **渲染节点**：
   - 加入项目的桌面版自动成为该项目的渲染节点，能认领本项目任何成员发布的任务；
   - 纯浏览器节点只替本人干活，语义不变。
7. **素材服务**：
   - 读和写都要凭项目凭证换来的票据，因为主机会暴露到公网（D10 修订后，暴露在公网的是阿里云托管端）；
   - 回环地址上的匿名访问保留；部署在反向代理之后的托管端关掉（2026-09-26 第二轮，HT 的 `PROMPTCUT_TRUST_LOOPBACK=0`）。
8. **集群令牌**（同日两次修订，取代原文「降为底层门槛、与凭证叠加」）：
   - 集群令牌从数据面彻底退出，任何角色的连接都不带；
   - 只保留给阿里云的管理接口：地址登记、管理 HTTP、迁移导出；
   - 项目凭证是数据面唯一的门槛。
10. **角色绑在连接上，不绑在身份上**（用户 2026-09-26 定）：
    - 身份 = 用户名 + 设备名，凭项目凭证进入。
    - 每条连接另带角色：`page`、`agent`、`render`。`render` 连接可以标归属：归真人，或归某个 Agent 对话（对应语义里 Agent 的专用渲染实例）。
    - 一台设备可以同时开多条不同角色的连接：桌面版是「页面 + 渲染」两条，Agent 是「`agent` + 它的 `render`」。
    - 独立渲染主机（`profile: 'host'`）就是只开 `render` 连接的设备，凭项目凭证进入；限定进入时，由创建者在名单里给它加一条。
    - 成员列表按设备聚合一行，后面挂标签：[编辑中] [渲染中] [Agent ×n]。
    - 写入身份沿用语义：页面写入记「用户 + 设备 + 页面」，Agent 写入记「用户 + 设备 + 第几个对话」。
    - 纯浏览器节点「只替本人干活」，按用户名加设备判。
    - 放本机时，主机（局域网主机）的管理接口只绑回环地址，不要求任何令牌。
9. **暂不做**第三方登录和密码找回。凭证的编码、票据有效期、握手细节由主会话定（见 12.2）。

### 12.2 主会话定的细节〔裁〕

| 项 | 定为 | 理由 |
|---|---|---|
| 限定进入要不要保留项目密码 | **不保留**。限定进入只认名单里的用户名加密码 | 名单每条已有自己的密码。再加一道共享密码，只多一样要传、要改的东西，却挡不住任何名单外的人（名单外的人本来就进不来）。撤销某个人只要删他那一条 |
| 创建者怎么进入 | 两种方式下，创建者都可以凭创建者用户名加创建者密码直接进入。限定进入下，创建者自动是名单的第一条，不能删 | 特权绑创建者凭证，创建者不该还要另记一个项目密码 |
| 口令怎么核对 | **挑战应答，密码不上线**。<br>项目所在的文档服务（放云端时是阿里云托管端，放本机时是创建者本机）存派生密钥 `K = PBKDF2-HMAC-SHA256(密码, 盐, 60 万次)`（2026-09-26 按 codex 查资料改定，原为 scrypt；细节见 `auth-contract.md` 第 2、13 节），按项目密码、名单每条、创建者各存一份。<br>连接建立后，文档服务下发盐和一次性随机数；客户端算 `HMAC-SHA256(K, 带用途前缀的随机数)` 交回，文档服务比对。<br>都用 `node:crypto`，不引依赖 | S4 定了不加密，明文密码会原样过公网。挑战应答让截获者拿不到密码，也没法重放。<br>代价：磁盘上存的 K 对本协议等同于密码。托管时它在阿里云的数据目录里（`PROMPTCUT_DATA_DIR/docservice/`），放本机时在创建者本机的用户数据目录里 |
| 特权操作怎么证明 | 每次特权操作都带一份新的创建者证明（重新挑战应答），不靠会话里记着的角色 | 绑「创建者用户名 + 创建者密码」的字面要求；创建者离开座位时，别人不能借他的会话删项目 |
| `userId` 怎么构成 | 两种进入方式、包括创建者，一律是 `用户名@设备 id`（第 10 条：身份 = 用户名 + 设备名） | 纯浏览器「只替本人干活」按用户名加设备判；自由进入下重名合法，同名不同设备算两个人 |
| `principal` 字段 | 身份部分，一台设备上的所有连接都相同：`{ userId, tenantId: 共享项目 id, username, deviceId, deviceName, creator: boolean }`。<br>连接部分，每条连接各带一份：`{ role: 'page' \| 'agent' \| 'render', conversation?: 第几个对话（agent 必填）, owner?: { kind: 'user' } \| { kind: 'agent', conversation }（render 可填） }` | 第 10 条。`creator` 只用于界面标记，特权仍按每次的证明判断。<br>写入身份用 `userId`，加上 `page`，或者加上 `agent` 与对话号 |
| 设备名 | 桌面版：取操作系统的主机名，加硬件标识的哈希前 4 位，写入用户数据目录后不再变。<br>浏览器拿不到硬件信息：取「浏览器名 + 系统名」加一个随机 4 位，存在页面本地 | 可读、稳定。浏览器那一种是 D9 第 4 条在浏览器上的近似，需要用户认可（见汇报） |
| 会话 | 凭证只在建立连接时核对一次，连接活着会话就有效；断线重连时重新挑战应答（客户端缓存 scrypt 结果，不缓存密码）。〔2026-09-26 第二轮修订〕文档服务与每一方之间改为会话（语义「会话与传输」）：保留期内传输断开，凭会话号接续，不重新挑战应答；过了保留期照本行（`http-transport-contract.md` 第 4 节） | 与 M5a 的重连、`resume` 一致，不另造会话令牌 |
| 素材票据 | 形如 `v1.<base64url(JSON{ p: 项目 id, u: userId, r: 'r' \| 'rw', g: 代数, exp })>.<HMAC-SHA256>`。<br>密钥是创建项目时生成的 32 字节随机数。两种放法下，文档服务和素材服务都在同一台机器上（托管端，或放本机时的创建者本机），共用这把密钥。<br>有效期 15 分钟，客户端在剩 1/3 时经文档服务连接续签。<br>放在 `Authorization: Bearer` 里；`<video>`、`<img>` 带不了请求头，改用查询串 `?t=`，只允许只读票据，素材服务不记录查询串 | 无状态校验，素材服务不用回头问文档服务。<br>查询串方式是浏览器媒体元素唯一可行的办法，限定只读、短期 |
| 票据作废 | 票据里带的代数：<br>改项目密码或名单时，该项目的代数加一，已发的票据全部作废；<br>踢人时，被踢者的代数加一 | 不用维护吊销表 |
| 踢人 | 断开被踢者的全部连接，作废其票据，把「用户名 + 设备 id」记进本项目的禁入表，创建者可以撤销。自由进入下，界面同时提示创建者：想彻底挡住，要改项目密码 | 自由进入下，设备名可以伪造，禁入表只挡诚实客户端，改密码才是硬手段 |
| 口令错误限速 | 按来源地址：1 分钟内错 5 次，冷却 60 s | 托管端暴露在公网，挡暴力猜测 |
| 集群令牌 | 按第 8 条的第二次修订：<br>数据面的任何连接（`page`、`agent`、`render`，包括独立主机）只带子协议 `promptcut.v1`，然后交项目凭证；带了令牌也不给任何额外权限。<br>令牌只守阿里云的管理接口，没令牌回 401。<br>M5a 的「失败即关」改为：绑非回环地址而未加载项目凭证存储时拒绝启动；没配令牌照常启动，管理接口全部回 401。<br>局域网主机的管理接口只绑回环地址，不要令牌 | 用户 2026-09-26 定 |

### 12.3 与队列、语义的对应

- `source.userId`、`source.tenantId` 仍只取自连接的 `principal`，消息里自报的丢弃（契约 A 节不变）。
- 本机 PC 与独立主机：凭项目凭证加入后，认领本项目任何成员的任务（D9 第 6 条），按 `tenantId` 授权（H3）。
- M5a 起用的集群身份 `{ userId: 'cluster', tenantId: 'cluster' }` 在 M6 起只剩管理接口在用，不再能认领任何项目的任务。
- 纯浏览器：按 `userId`（用户名 + 设备）相等把关（E5）；同名不同设备不算本人。
- 写入身份：页面写入记「用户 + 设备 + 页面」；Agent 写入记「用户 + 设备 + 第几个对话」，满足语义「哪个用户的哪个页面，或哪个 Agent 的哪个对话」。
- `render` 连接归属于某个 Agent 对话时，就是语义里 Agent 的专用渲染实例；它认领的任务照常按项目授权。
- 设计 `distributed-prerender-queue.md` 里的「团队 / 租户」统一读作「共享项目」（第 137、214、306 行已随本次改）。
