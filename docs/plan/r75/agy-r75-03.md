# 第 75 轮 · 第 3 份：第 4 步·舞台面：E0～E7、A4、D3 的用户命中部分、D4 的单飞队列、F2

> **来源说明（谁写的哪一部分）**：agy（gemini-3.1-pro-high）跑了 1 轮 + 1 轮返工，产出了报告结构和第 2 节的三条阻塞项（2.1 / 2.2 / 2.3）。第二轮返工没跑完——agy 个人配额耗尽（429，2 小时后恢复）。
> 因此 **第 3 节全部条目、第 4.1 节整张表、第 4.2 节、第 5 / 6 节和文末「manager 核对记录」由 manager 补做**。
> agy 第一版的 4.1 表约七成「源码逐字」是凭记忆重建的假文本（例如把 `solid.ts:158` 的 `hitTest(root, x, y)` 写成 `hitTest(x, y, root)`、把 `scene-3d.tsx` 注释的意思写反），已整张作废重做。
> 现在这张表的第三列是用脚本按行号从工作区直接读出的真实文本（HEAD = b5c65dc，工作树除 `.claude/worktrees/`、`nul` 两个未跟踪项外干净），未经人手转述。

## 1. 结论

**有条件能。** 把第 2 节三条补掉之后，E0～E7 / A4 / D3 第 4 步部分 / D4 单飞队列 / F2 可以照着动工。条件逐条：

1. **补齐 E7 第 1 条的搬运清单**（2.1）：照现在列的行号范围搬，`src/render/VideoTrack.tsx` 会少三个符号，编译不过。
2. **给 `mediaSync.ts` 一个明确去处或豁免**（2.2）：不处理它，"舞台 bundle 不带编辑器模块"这条自己就不成立。
3. **补上 `FrameScene` 新 props 清单里漏的三个**（2.3）：按 L166 的清单写类型，L174 那句 JSX 直接 TS 报错。

另有 12 条非阻塞项（第 3 节），其中 3.1 / 3.2 / 3.4 / 3.7 / 3.15 是"任务书把已落地的事写成了待办"，会让执行者重复劳动；3.11 / 3.12 / 3.13 是 F2 / E3 / 验收缺落点。

## 2. 阻塞项

### 2.1 E7 第 1 条的搬运范围漏了三个符号，照行号搬完 `VideoTrack.tsx` 编译不过

- **任务书行与原句**：**L167** —「`MediaLayers.tsx:151-312` 连同 `:138-150` 的三个辅助函数一起抽到 `src/render/VideoTrack.tsx`；`driveMedia` / `releaseMedia` / `syncMediaEl` 和四个 `WeakMap`（`:28-104`）抽到 `src/render/mediaDrive.ts`」
- **证据**：被搬走的 `VideoTrack`（`:151-312`）用了三个定义在所有列出范围之外的符号——
  - `src/editor/preview/MediaLayers.tsx:106`：`function targetTimeOf(clip: TrackClip, t: number) {`，`VideoTrack` 在 `:235` 调用
  - `src/editor/preview/MediaLayers.tsx:115`：`function filterOf(clip: TrackClip, filters: FilterDef[] | undefined, t: number): string | undefined {`，`:291` / `:298` 调用
  - `src/editor/preview/MediaLayers.tsx:128`：`interface Slot {`，`:138` 的 `emptySlot` 返回它、`:185` 的 `useRef<Slot[]>` 用它
  并且 `targetTimeOf` 还被留在 `MediaLayers` 的 `AudioLayer` 用：`src/editor/preview/MediaLayers.tsx:319`：`  const target = nodeId ? Math.max(0, t - clip.start) : targetTimeOf(clip, t);` —— 不能整体搬走，只能放到共享文件里。
- **建议改法**：把 L167 的「连同 `:138-150` 的三个辅助函数一起抽到 `src/render/VideoTrack.tsx`」改成「连同 `:128` 的 `interface Slot`、`:138-150` 的三个辅助函数一起抽到 `src/render/VideoTrack.tsx`；`:106-114` 的 `targetTimeOf` 和 `:115-127` 的 `filterOf` 抽到 `src/render/mediaDrive.ts`，`MediaLayers` 的 `AudioLayer` 改从那里 import」。

### 2.2 E7 第 1 条的「舞台 bundle 不带编辑器模块」与 `VideoTrack` 必须 import `mediaSync.ts` 冲突

- **任务书行与原句**：**L167** —「**`VideoTrack.tsx` 不 import `src/audio/previewAudio` 和 `src/editor/timeline/useScrub`**（`MediaLayers.tsx:5`、`:9`），音频路由留在 `MediaLayers`、`scrubbing` 从 prop 来，舞台 bundle 不带编辑器模块」
- **证据**：`src/editor/preview/MediaLayers.tsx:10`：`import { planSlots, planSync, type SlotClip } from "./mediaSync";`；`VideoTrack` 体内 `:192`：`  const plan = planSlots({ slots: slots.current, shown: shownRef.current, cur: curV, next: nextV, t, playing });`。按 L7 路径缩写表，`mediaSync.ts` = `src/editor/preview/mediaSync.ts`，是编辑器目录下的模块。任务书只点名禁了两个 import，没给这个第三个依赖任何处置，照做完舞台 bundle 照样带编辑器模块。
- **建议改法**：在 L167 那句后补一句「`mediaSync.ts`（纯函数 + 单测）随 `VideoTrack` 一起搬到 `src/render/mediaSync.ts`，`MediaLayers` 改 import 新路径」；或明写「`src/editor/preview/mediaSync.ts` 是纯函数模块，不计入『编辑器模块』，允许舞台侧 import」。二选一，但必须选一个。

### 2.3 E7 给 `FrameScene` 列的新 props 清单，比 L174 那句 JSX 实际传的少三个

- **任务书行与原句**：**L166** —「`FrameScene` 加 props：`mediaMode: 'placeholder' | 'live'`、`mediaT`、`scrubbing`、`playing`、`proxy`、`suppressed`、`streamPlanes`、`snapshots`、`localHashes`（A1）、**`graph`**」
- **冲突的另一处**：**L174** —「`StageView.tsx:606` 改成 `<FrameScene project={project} graph={graph} t={t} directT={t} mediaT={mediaT} playToken={token} mediaMode="live" proxy={…} scrubbing={…} playing={…} suppressed={…} streamPlanes={…} snapshots={…} remountGen={…} settling={…} awaiting={…} localHashes={…} />`」
- **证据**：`remountGen` / `settling` / `awaiting` 只在 L166 的后半句以「`remountGen?: Map<clipId, number>`（K3，`FrameScene` 原样透传）」「`FrameScene` 把 `settling` / `awaiting` 原样透传给 `Stage`」出现，不在「`FrameScene` 加 props：」那句清单里。`src/render/FrameScene.tsx:68` 现在的签名是 `export function FrameScene({ project, sourceProject = project, t, directT = t, playToken, graph }: { project: Project; sourceProject?: Project; t: number; directT?: number; playToken: number; graph?: Timeline["graph"] })` —— 按 L166 写 props 类型，L174 的三个实参就是 TS 报错。
- **建议改法**：把 L166 的清单改成「…、`snapshots`、`localHashes`（A1）、**`graph`**、`remountGen`、`settling`、`awaiting`」。

## 3. 非阻塞项

> 扫查方向覆盖说明：(a) 现状断言体检 → 3.1 / 3.2 / 3.4 / 3.7 / 3.9 / 3.15；(b) 引用指向体检 → 3.3 / 3.10；(c) 数量词体检 → 3.5 / 3.6；(d) 搬运可行性 → 已在 2.1 / 2.2；(e) 跨行冲突 → 3.7 / 3.8；(f) 验收可执行 → 3.11 / 3.12 / 3.13；(g) 清单对账 → 已在 2.3。

### 3.1 E4 在 b5c65dc 已经落地，任务书仍写成第 4 步待办
- **L162** —「**E4 `advanceToAsync` 加参数 `yieldEvery?: number`**（`stageClock.ts:176-186` 今天只在 `:178` `await Promise.resolve()` 让微任务）」
- **证据**：`src/render/stageClock.ts:67`：`    yieldEvery?: number;`（已在 `advanceToAsync` 的 opts 类型内）；`src/render/stageClock.ts:177`：`        if (opts.yieldEvery && ran > 0 && ran % opts.yieldEvery === 0) await new Promise<void>((r) => realTimeout(r, 0));`；`src/StageView.tsx:425`：`              yieldEvery: 8,`（`render` 已经在传 8）。
- **建议改法**：L162 首句改成「**E4 `advanceToAsync` 的 `yieldEvery?: number` 已落地（b5c65dc：`stageClock.ts:67` 声明、`:177` 实现，`StageView.tsx:425` 的 `render` 已传 8）**」，后半句关于"K5 的暂停态补跑传 8、K3 的轻卡重推不传"保留为第 4 步待办。

### 3.2 E4b 说 `__pcRealNow`「今天全仓没有」，实际四样口子里已有三样
- **L163** —「真实的四样各留一条口子（`__pcRealNow` 是 K1 新增的、今天全仓没有，与 `__pcRealDateNow` / `__pcRealSetTimeout` / `__pcRealSetInterval` 一起在 `stageClock.ts` 接管前保存）」
- **证据**：`src/kernel/clock.ts:33`：`    __pcRealNow?: () => number;`；`src/render/stageClock.ts:91`：`  window.__pcRealNow = performance.now.bind(performance);`；`:92`：`  window.__pcRealSetTimeout = window.setTimeout.bind(window);`；`:100`：`  window.__pcRealRaf = window.requestAnimationFrame.bind(window);`；`src/StageView.tsx:71`：`const realNow = () => (window.__pcRealNow ?? (() => Date.now()))();`。真正缺的只有 `__pcRealDateNow` / `__pcRealSetInterval`。
- **建议改法**：把括号改成「`__pcRealNow` / `__pcRealSetTimeout` / `__pcRealRaf` 已有（`stageClock.ts:91` / `:92` / `:100`），E4b 只补 `__pcRealDateNow` / `__pcRealSetInterval`」。

### 3.3 E0 指的 `syncProject` 行号错了
- **L157** —「D4 的页面侧测量——`right/index.tsx:96-98` 的 `syncProject(backRole(), …)` 在 E1 之后就是会掐断探针的那次 `setProject`」
- **证据**：`src/editor/right/index.tsx:94`：`    await syncProject(backRole(), st.project);`；而 `:96` 是 `    await s.setProxy(false);`、`:97` 是 `    await s.setTime(st.t);`、`:98` 是 `    list = await s.rectsWithBounds({ pixels: "all", clipIds: cardIds });`。
- **建议改法**：把 L157 里的 `right/index.tsx:96-98` 改成 `right/index.tsx:94`。

### 3.4 D4 说 `getLayout` 现在「`if (c.cardId)` 跳过素材段要改」，b5c65dc 里没有这个过滤
- **L142** —「同一条里『不传 clipId 返回全部卡片的加舞台尺寸』→『不传 clipId 返回全部卡片和素材段的加舞台尺寸』（D4(b) 把素材段纳进来了，`right/index.tsx 的 `getLayout`` 现在 `if (c.cardId)` 跳过素材段要改）」
- **证据**：`src/editor/right/index.tsx:635`：`        const ids = p.tracks.flatMap((tr) => tr.clips.map((c) => c.id));` —— 没有 `cardId` 过滤；`:634` 的注释就是 `        // 全部卡片和素材段,一次往返(素材段的 contentBox = 它的 frameCss 框)`；素材段的分支已在 `:83`：`    if (!hit.clip.cardId) { out.set(id, { contentBox: roundBox(frameBox(hit.clip.frame, stage)) }); continue; }`。
- **建议改法**：删掉括号里「`right/index.tsx 的 `getLayout`` 现在 `if (c.cardId)` 跳过素材段要改」整句（顺带修掉这里坏掉的反引号）。

### 3.5 「四个 `WeakMap`」数不上，`MediaLayers.tsx` 里只有三个
- **L167** —「`driveMedia` / `releaseMedia` / `syncMediaEl` 和四个 `WeakMap`（`:28-104`）抽到 `src/render/mediaDrive.ts`」
- **证据**：`src/editor/preview/MediaLayers.tsx` 全文只有三处 `new WeakMap`：`:28` `const lastSeekAt = new WeakMap<HTMLMediaElement, number>();`、`:41` `const wants = new WeakMap<HTMLMediaElement, Want>();`、`:42` `const retryTimers = new WeakMap<HTMLMediaElement, number>();`。
- **建议改法**：「四个 `WeakMap`」改成「三个 `WeakMap`（`:28` / `:41` / `:42`）」。

### 3.6 「`Stage.tsx:187-200` 条件渲染的两个平面」：b5c65dc 那个区间只渲一个
- **L171** —「`streamPlayer` 只往这个已存在的 `<canvas>` 上画，不做外部 `insertBefore`（否则和 `Stage.tsx:187-200` 条件渲染的两个平面抢兄弟位置）」
- **证据**：`src/kernel/Stage.tsx:187`：`              {proxy ? (() => {` 到 `:200`：`              })() : null}` 之间只渲一个 `[data-pc-proxy-plane]`（`:191`）；L30 的只读上下文也写「包裹层的直接子节点只有卡片组件本身…和一个可选的 `[data-pc-proxy-plane]` 兄弟（`:191`）」。第二个平面（快照平面）要 E7 第 4 条自己加。
- **建议改法**：改成「和 `Stage.tsx:187-200` 的代理平面、以及 E7 第 4 条新加的快照平面抢兄弟位置」。

### 3.7 E0 说要「合并 `Preview.tsx:323` 的 N+1 往返」，但合并已经落地
- **L158** —「（合并 `Preview.tsx:323` 的 N+1 往返；`'selected'` 时父页把 store 的 `selection` 作为 `clipIds` 带过去…）」
- **冲突的另一处**：**L30** —「选中描边由 `Preview.tsx:323` 一次 `rectsWithBounds` 取框（不再 N+1）」
- **证据**：`src/editor/Preview.tsx:323`：`        const list = await s.rectsWithBounds(pixels === "selected" ? { pixels, clipIds: selectionRef.current } : { pixels });` —— 已经是合并后的一次往返，`selection` 也已经在带。
- **建议改法**：把 L158 那个括号改成「（`Preview.tsx:323` 的合并已在第 3 步落地；第 4 步只补 `'all'` / `'selected'` 分档和 GL Worker 的 `measure({ clipIds })` 那一路）」。

### 3.8 A4 说「用 control 快照替换组件」，与不做清单和 E7 第 4 条相反
- **L108** —「**A4 整场景一帧靠拼（第 4 步）。** direct 卡当场渲，stateful 卡由 `Stage` 在包裹层里用 control 快照替换组件，素材层按 A2。」
- **冲突的另一处**：**L355**（不做）—「在同一个 key 下把卡片组件换成快照 div（快照是兄弟平面）。」以及 **L170**（E7 第 4 条）—「`snapshots?.has(clip.id)` 时，`Stage.tsx:167-177` 的组件照常渲（不动三目；行号是 b5c65dc 的…），包裹层加类 `.pc-snapshot`，并在组件旁…渲一个兄弟 `<div data-pc-snapshot-plane`」
- **证据**：三处口径互斥：A4 写"替换组件"，不做清单明令禁止"把卡片组件换成快照 div"，E7 写"组件照常渲 + 兄弟平面"。只看 A4 的执行者会去做被禁的那件事。
- **建议改法**：把 L108 的「用 control 快照替换组件」改成「在包裹层里挂一张 control 快照的兄弟平面、组件照常挂着（E7 第 4 条）」。

### 3.9 「全仓卡片没有内联 `<style>`」不成立
- **L170** —「`<style>` 标签内的选择器不改名（全仓卡片没有内联 `<style>`）。」
- **证据**：`src/cards/native/terminal-3d.tsx:46`：`      <style>{\``（块内含 `@keyframes pc-term-blink` 和 `.pc-term-cursor` 两个全局名字）；`src/cards/_probe/probe.tsx:31` 同样有一个 `<style>`。
- **建议改法**：把括号改成「全仓只有 `src/cards/native/terminal-3d.tsx:46` 和 `src/cards/_probe/probe.tsx:31` 两处内联 `<style>`，里面只有 `@keyframes` 名和类名、没有 id 选择器，A2(7) 的 id 改名不受影响；`freezeScene` 已给内联样式加 `animation:none !important`（`snapshotFreeze.ts:58`），快照里的 keyframes 不会跑」。

### 3.10 E6 引的 effect 区间多带了一行
- **L165** —「**`Preview.tsx:386-397` 那个 effect 拆成两个**」
- **证据**：`src/editor/Preview.tsx:386`：`  const lastRenderKey = useRef("");`（不是 effect）；effect 从 `:387` `  useEffect(() => {` 起、到 `:397` `  }, [stageReady, t, playToken, stage, refreshRects]);` 止。
- **建议改法**：改成 `Preview.tsx:387-397`。

### 3.11 F2 的「服务端旧调度器」那一半没有落点
- **L205** —「**F2 回滚。** `?preview=legacy`（前端整帧 + 服务端旧调度器）；A1 哈希缺失退回文件名；A2 和 C1 用 `scripts/verify-unified-frames.mjs` 做像素回归；E7 `placeholder` 模式全长导出比对。」
- **证据**：b5c65dc 里 `?preview=legacy` 只有页面侧一个落点：`src/StageView.tsx:53`：`const LEGACY = typeof window !== "undefined" && new URLSearchParams(location.search).get("preview") === "legacy";`；服务端没有任何读这个开关的分支，帧请求体也不带它（`src/render/frameClient.ts:43`：`    ? { session: key.session, localRev: key.localRev, lane: options.lane, ...extra }`）。任务书没说"服务端旧调度器"由谁读、页面怎么把这个标志传过去。
- **建议改法**：在 L205 里把这半句展开成落点，例如「页面把 `preview=legacy` 作为 `lane: 'legacy'` 随帧请求带给 `FramePipeline`，服务端按 lane 走旧调度器」；或者删掉「服务端旧调度器」，明写 F2 只回滚前端。

### 3.12 E3 说「非 legacy 下 `Preview.tsx:145-149` 那段不再触发」，但没给谁去关那个 rAF 播放循环
- **L161** —「播放到头见 K4 的 `ended`（非 legacy 下 `Preview.tsx:145-149` 那段不再触发）」
- **证据**：`src/editor/Preview.tsx:126`：`    if (!playing) return;`——`:125-158` 这个 rAF 循环只看 `playing`，和 legacy 无关；`:145`：`      if (acc >= project.duration) {`。E0～E7 里只写了 `Preview.tsx:387-397`（E6）和 `:395`（E0）要改，没有一条负责把 `:125-158` 这个循环在非 legacy 下停掉。
- **建议改法**：在 E6（L165）里补一句「`Preview.tsx:125-158` 的 rAF 播放循环同样按 `LEGACY` 分支：非 legacy 下不启动，播放头由 K4 的 `frame` 事件推进」，或明写这条归 K4。

### 3.13 验收 L317 里的 `drawParticles` 不是仓库里可观测的东西
- **L317** —「被抑制的粒子卡在 `streams` 开着时不再执行 `drawParticles`、但它的包裹层轨迹和 `data-pc-local-frame` 仍随 `t` 变」
- **证据**：全仓 `drawParticles` 只在 `src/cards/native/particles.tsx` 的三处注释里出现（`:49` / `:99` / `:182`），没有同名函数、也没有可 hook 的导出。验收没给别的观测口径。
- **建议改法**：把断言换成可观测的，例如「被抑制的粒子卡的 `<canvas>` 像素在抑制期间不变（`canvasPixels` 哈希不变），且包裹层 `data-pc-local-frame` 仍随 `t` 变」。

### 3.14 `setTime({ probe: true })` 没有角色闸门，不做清单 L342 只能靠父页自觉
- **L156** —「带 `probe: true` 时一律走跳转路径…等一次 `__pcRealRaf`、冻一次本控件 HTML（`__bfFreeze`，K1 要求 `frameMs` 含冻结时间）再回包 `{ elapsedMs }`」
- **证据**：`src/StageView.tsx:379`：`          if (root) freezeScene(root);` —— 在 `setTime` 的 probe 分支里，不查角色；E0 只给 `render` 安排了闸门（L154「`render` 舞台侧无角色闸门，E1 加」）。不做清单 L342 写的是「可见舞台（`front`）自己冻结快照…`J1` 的 `__bfFreeze` 在舞台页只由 `back` 角色调用」。
- **建议改法**：在 L154 那句「`render` 舞台侧无角色闸门，E1 加」后面补「`setTime({ probe: true })` 同样加：非 `back` 收到回 `{ path: 'set', aborted: true, reason: 'role' }`」。

### 3.15 D3 第 4 步的「`rects()` 遍历根下所有 `[data-pc-clip]`」已经是现状
- **L141** —「第 4 步：`rects()` 遍历根下所有 `[data-pc-clip]`，素材层由 E7 第 1 条保证带属性（视频和图片都带）」
- **证据**：`src/render/solid.ts:223`：`  for (const el of root.querySelectorAll("[data-pc-clip]")) {` —— 第 3 步就已经这样了；第 4 步真正要做的只有"让 live 素材层带上 `data-pc-clip`"和"删 `mediaRects`"两件。
- **建议改法**：把这半句改成「`rects()` 已经遍历根下所有 `[data-pc-clip]`（`solid.ts:223`），第 4 步只需 E7 第 1 条给 live 素材层补上该属性」。

## 4. 行号核对表 与 前置接口核对表

### 4.1 行号核对

核了 130 处（负责段落里的引用基本全覆盖，另含 11 处"反证行"——任务书没引、但用来证明第 3 节某条结论的行）。
**没核的**：(1) L155 里 `changedClips` 的 patch 结构（属 A7 第 2 步已落地范围，不是本步引用）；(2) L158 里对 GL Worker `measure({ clipIds })` 的引用——该接口属目标 M，仓库里还不存在；(3) L159（E1）引的 `scripts/probes/oac-probe.mjs` 只核了文件存在、没核实测数字；(4) L205 引的 `scripts/verify-unified-frames.mjs` 只核了文件存在。

第三列是脚本按行号从 HEAD=b5c65dc 的工作区直读的真实文本（超过 114 字符处截断并以 … 标注，`|` 已转义）。

| 任务书行 | 引用 | 源码该行逐字（<=120 字符）| 判定（对 / 错 / 应为 :N）|
|---|---|---|---|
| L154 | stageRpc.ts:69 |   path: "continuous" \| "set"; | 对 |
| L154 | stageRpc.ts:120 |   setSnapshots(patch: Record<string, string \| null>, opts?: { reset?: boolean }): Promise<{ ok: true; bytes: numbe… | 对 |
| L155 | StageView.tsx:295 |       async setProject(next, opts = {}) { | 对 |
| L155 | StageView.tsx:320 |         if (LEGACY) { | 对 |
| L155 | StageView.tsx:327 |               ref.current.settle = window.setTimeout(() => legacyJump(ref.current.t), SETTLE_MS); | 对 |
| L156 | StageView.tsx:342 |       async setTime(tSec, opts = {}) { | 对 |
| L156 | StageView.tsx:337-338 |        * 唯一例外:往前且不到 CONTINUOUS_MAX 秒,按连续播放用 advanceTo 同步推几帧(≤ 30 步)。 | 对 |
| L156 | StageView.tsx:361-364 |         if (!opts.probe && dt >= 0 && dt < CONTINUOUS_MAX) { | 对 |
| L156 | solid.ts:102 | export function isSolid(el: Element, root: SceneRoot): boolean { | 对 |
| L156 | solid.ts:102 的 visibility 判据 |   if (cs.visibility === "hidden" \|\| parseFloat(cs.opacity) === 0) return false; | 对，判据实体在 :111 |
| L157 | StageView.tsx:394 |       async render(tSec, opts = {}) { | 对 |
| L157 | StageView.tsx:279 |         if (gen !== renderGen.current) return; | 对 |
| L157 | StageView.tsx:410 |           remountAt(target, fps); | 对 |
| L157 | stageRpc.ts:46-48 | export interface RenderAborted { | 对 |
| L157 | stageRpc.ts:48 的 reason 联合 |   reason: "superseded" \| "project" \| "timeout" \| "detached"; | 对 |
| L157 | stageRpc.ts:191 |         if (method === "render") resolve({ aborted: true, reason: "detached" }); | 对 |
| L157 | stageRpc.ts:218 |         if (p.method === "render") p.resolve({ aborted: true, reason: "detached" }); | 对 |
| L157 | right/index.tsx:96-98 的 syncProject |     await s.setProxy(false); | 错，应为 :94 |
| L157 | right/index.tsx:94 真实 syncProject 行 |     await syncProject(backRole(), st.project); | 对 |
| L158 | Preview.tsx:323 |         const list = await s.rectsWithBounds(pixels === "selected" ? { pixels, clipIds: selectionRef.current } : {… | 对 |
| L158 | Preview.tsx:352 |       try { card = await s.hitTest(x, y); } catch { card = null; } | 对 |
| L158 | Preview.tsx:367 |     void syncProject("front", project).then(() => refreshRects(), () => {}); | 对 |
| L158 | Preview.tsx:395 |       .then(() => s.setTime(t)) | 对 |
| L158 | Preview.tsx:274-284 |     const onMessage = (e: MessageEvent) => { | 对 |
| L158 | Preview.tsx:276 |       if (!win \|\| e.source !== win \|\| (e.data as any)?.type !== "pc-stage-ready") return; | 对 |
| L158 | Preview.tsx:379-380 |    * E0:暂停、拖动只发 setTime(不重挂载、不递增 playToken —— 时钟拨过去、提交 React、钉动画); | 对 |
| L158 | Preview.tsx:407-490 起 |   const handleOverlayPointerDown = async (e: React.PointerEvent) => { | 对 |
| L158 | Preview.tsx:407-490 止 |   }; | 对 |
| L158 | contentBox.ts:143-145 |     const ctx = scratchContext(w, h); | 对 |
| L158 | contentBox.ts:129 | export function canvasPixels(el: HTMLCanvasElement): CanvasPixels \| null { | 对 |
| L158 | contentBox.ts:100-101 |  * 这是整个测量里最贵的一步:一张 1920×1080 的画布要 drawImage + getImageData 读回八百万字节, | 对 |
| L158 | scene-3d.tsx:146-151 |     /* | 对，实际在 src/cards/native/ |
| L158 | stageRpc.ts:111 |   play(fromSec: number): Promise<{ ok: boolean; reason?: string }>; | 对 |
| L158 | stageRpc.ts:91-96 | export interface PauseReply { | 对 |
| L158 | StageView.tsx:517-522 |       async play() { | 对 |
| L158 | StageView.tsx:566 |     window.__pcStage = api; | 对 |
| L158 | StageView.tsx:574 |     postStageReady(detectHostCapabilities()); | 对 |
| L158 | StageView.tsx:444 |                   postStageEvent({ type: "probe-frame", clipId: c.id, localFrame: Math.round((ms / 1000 - clipStar… | 对 |
| L158 | stageBridge.ts:76-77 |     if (patch.kind === "full") await client.setProject(project, { reset: true }); | 对 |
| L158 | solid.ts:267 的 rectsWithBounds | export const solidApi = { isSolid, bounds, rects, hitTest, stageOrigin, canvasBox, canvasPaintedBox, rectsWithBoun… | 对 |
| L162 | stageClock.ts:176-186 |       while (ms < target) { | 对 |
| L162 | stageClock.ts:178 称今天只让微任务 |         else await Promise.resolve(); | 错：:177 已有 yieldEvery 的宏任务让出 |
| L162 | 反证：yieldEvery 已声明 |     yieldEvery?: number; | E4 参数已存在 |
| L162 | 反证：StageView 已传 yieldEvery: 8 |               yieldEvery: 8, | E4 已落地 |
| L163 | stageClock.ts:93 |   performance.now = () => now; | 对 |
| L163 | stageClock.ts:101 |   window.requestAnimationFrame = (cb: FrameRequestCallback) => { | 对 |
| L163 | 反证：__pcRealNow 保存点 |   window.__pcRealNow = performance.now.bind(performance); | 错：任务书称今天全仓没有 |
| L163 | 反证：__pcRealSetTimeout 保存点 |   window.__pcRealSetTimeout = window.setTimeout.bind(window); | 错：同上 |
| L163 | 反证：__pcRealNow 已在用 | const realNow = () => (window.__pcRealNow ?? (() => Date.now()))(); | 错：同上 |
| L165 | Preview.tsx:386-397 |   const lastRenderKey = useRef(""); | 应为 :387-397 |
| L165 | Preview.tsx:387 effect 真起点 |   useEffect(() => { | 对 |
| L166 | solidMode.ts:138 |   s.textContent = `.pc-proxy > *:not([data-pc-proxy-plane]) { display: none !important; }`; | 对 |
| L166 | Stage.tsx:169 |                 <PartTree parts={clip.parts} size={frameBox(clip.frame, timeline)} t={Math.max(0, t - clip.start)}… | 对 |
| L166 | Stage.tsx:175 |                 <DirectCard def={def} params={clip.params} playToken={playToken} t={Math.max(0, cardT - clip.start… | 对 |
| L166 | Stage.tsx:177 |                 <C params={{ ...def.defaults, ...clip.params }} playToken={playToken} t={Math.max(0, cardT - clip.… | 对 |
| L166 | FrameScene.tsx:76 |   const active = (clip: { start: number; end: number }) => directT >= clip.start && directT < clip.end; | 对 |
| L166 | FrameScene.tsx:77 |   const legacyTimeline = (tr: Project["tracks"][number], clip: Project["tracks"][number]["clips"][number]) => | 对 |
| L167 | MediaLayers.tsx:151-312 起 | function VideoTrack({ | 对 |
| L167 | MediaLayers.tsx:151-312 止 | } | 对 |
| L167 | MediaLayers.tsx:138-150 | const emptySlot = (): Slot => ({ clip: null, full: null, ready: false, token: 0, opacity: 1 }); | 对 |
| L167 | MediaLayers.tsx:28-104 | const lastSeekAt = new WeakMap<HTMLMediaElement, number>(); | 对，但四个 WeakMap 数不上 |
| L167 | MediaLayers.tsx:5 | import { releasePreviewAudio, routePreviewAudio } from "../../audio/previewAudio"; | 对 |
| L167 | MediaLayers.tsx:9 | import { isScrubbing, subscribeScrub } from "../timeline/useScrub"; | 对 |
| L167 | MediaLayers.tsx:238 |         if (!muted) routePreviewAudio(el, audioFx, cur.clip, t, playing); | 对 |
| L167 | MediaLayers.tsx:241 |         if (!muted) routePreviewAudio(el, audioFx, null, t, playing); | 对 |
| L167 | MediaLayers.tsx:297 |         <div key={image.clip.id} style={boxOf(image.clip)}> | 对 |
| L167 | 缺口：targetTimeOf 定义 | function targetTimeOf(clip: TrackClip, t: number) { | 不在搬运范围，见 2.1 |
| L167 | 缺口：filterOf 定义 | function filterOf(clip: TrackClip, filters: FilterDef[] \| undefined, t: number): string \| undefined { | 不在搬运范围，见 2.1 |
| L167 | 缺口：interface Slot 定义 | interface Slot { | 不在搬运范围，见 2.1 |
| L167 | 缺口：mediaSync 的 import | import { planSlots, planSync, type SlotClip } from "./mediaSync"; | 编辑器模块，见 2.2 |
| L167 | kernel/project.ts:427-429 |       if (!c.mediaId \|\| t < c.start \|\| t >= c.end) continue; | 对 |
| L167 | kernel/project.ts:460-467 |   for (const tr of [...p.tracks].reverse()) { | 对 |
| L167 | kernel/project.ts:465 |       if (!c.mediaId \|\| c.start <= t) continue; | 对，与任务书括号内逐字一致 |
| L167 | kernel/project.ts:466 |       if (best && c.start >= best.clip.start) continue; | 对 |
| L167 | FrameScene.tsx:83 |       {layers.filter(l => l.trackId === tr.id && !graphVisualNode(graph, l.clip.nodeId) && !replaced(l.clip.id)).m… | 对 |
| L170 | Stage.tsx:167-177 |               {clip.cardId === "composite" && clip.parts?.length ? ( | 对 |
| L170 | Stage.tsx:172 |                 <GraphCard def={def} clip={clip} graph={timeline.graph} fps={timeline.fps} | 对 |
| L170 | Stage.tsx:190-191 与 :190-198 |                   <div | 对 |
| L170 | Stage.tsx:191 |                     data-pc-proxy-plane="" | 对 |
| L170 | odometer.tsx:43 |     <div className={`hud-wrapper ${getPositionClass(params.position)}`}> | 对，实际在 src/cards/native/ |
| L170 | odometer.tsx:21 |     <span className="inline-flex relative overflow-hidden" style={{ height: "1em", verticalAlign: "bottom" }}> | 对 |
| L170 | checklist.tsx:59 |     <div className={`hud-wrapper ${getPositionClass(params.position)}`}> | 对 |
| L170 | hud.css:1-3 |   position: absolute; | 对 |
| L170 | stageClock.ts:167-169 |        * 凡是要**量布局** | 对 |
| L170 | pinAnimations.ts:26 |         if (a.playState === "finished" \|\| a.playState === "idle") continue; | 对 |
| L170 | Preview.tsx:602-606 |                  * 这里**不加 proxy=1**:2D 预览的契约是「预览所见 = 导出所得」, | 对 |
| L170 | 反证：卡片内联 style |       <style>{` | 错：任务书称全仓卡片没有内联 style |
| L171 | Stage.tsx:41 |   const active = timeline.clips.filter((c) => cardMountedAt(c, timeOf(c))); | 对 |
| L171 | Stage.tsx:122 |           const m = clip.motion ? motionAt(clip.motion, Math.max(0, cardT - clip.start)) : null; | 对 |
| L171 | Stage.tsx:128 |           const op = hasOpacityControls(clip) ? cardOpacityAt(clip, cardT) : 1; | 对 |
| L171 | Stage.tsx:135 |               data-pc-local-frame={Math.round((cardT - clip.start) * timeline.fps)} | 对 |
| L171 | Stage.tsx:187-200 |               {proxy ? (() => { | 对，但此区间只渲一个平面 |
| L171 | particles.tsx:91-105 |  * 渲染核心:给一个「取配置」的函数和随机种子,把粒子画进卡片层,**由时间轴的 t 驱动**。 | 对 |
| L174 | StageView.tsx:606 |       <Stage timeline={timeline} t={t} playToken={token} proxy={proxy ? proxyOf : undefined} /> | 对 |
| L174 | StageView.tsx:583 |   if (!project) return null; | 对，graph 的 useMemo 在 :144 |
| L174 | App.tsx:105 |             <Stage timeline={timeline} t={t} playToken={playToken} /> | 对 |
| L141 | StageView.tsx:587 |     <div | 对 |
| L141 | ExportView.tsx:275 |     <div | 对 |
| L141 | ExportView.tsx:147-156 |       window.__pcExportMs = 0; | 对 |
| L141 | ExportView.tsx:153 |       window.__pcCanvasBox = solidApi.canvasPaintedBox; | 对 |
| L141 | Preview.tsx:305-308 |   const mediaRects = useCallback((): { clipId: string; left: number; top: number; width: number; height: number }[… | 对 |
| L141 | Preview.tsx:332 |     setRects([...mediaRects(), ...cards]); | 对 |
| L141 | Preview.tsx:358 |       [...mediaRects()].reverse().find(inside) ?? | 对 |
| L141 | Preview.tsx:317-319 |   const refreshRects = useCallback(async (pixels: "selected" \| "all" = "selected") => { | 对 |
| L141 | 第 4 步 rects 遍历的现状 |   for (const el of root.querySelectorAll("[data-pc-clip]")) { | 已是遍历根下所有 data-pc-clip |
| L142 | right/index.tsx:75 | async function measureContentBoxes(clipIds: string[]): Promise<Map<string, { contentBox: ContentBox; contentNote?:… | 对 |
| L142 | right/index.tsx:123 | function frameLayoutOf(clipId: string) { | 对 |
| L142 | right/index.tsx:130 | async function contentLayoutOf(clipIds: string[]) { | 对 |
| L142 | right/index.tsx:160 |   return { ok: true, clipId, layout: frameLayoutOf(clipId), look: lookHint(clipId) }; | 对 |
| L142 | right/index.tsx:520 |         const r = withFrame(args.clipId, (prev, stage) => { | 对 |
| L142 | right/index.tsx:531 |         const r = withFrame(args.clipId, (prev, stage) => | 对 |
| L142 | right/index.tsx:538 |         const r = withFrame(args.clipId, (prev, stage) => { | 对 |
| L142 | right/index.tsx:545 |           ? { ...r, note: "这张卡的画布铺满舞台、也没缩小,对齐看不出效果。先 set_rect(放进一个矩形)或 nudge({ scaleBy: 0.6 })缩小,再对齐。" } | 对 |
| L142 | right/index.tsx:549 |         const r = withFrame(args.clipId, (prev, stage) => { | 对 |
| L142 | right/index.tsx:628-637 |       getLayout: async (args) => { | 对 |
| L142 | 称 getLayout 有 if (c.cardId) |         const ids = p.tracks.flatMap((tr) => tr.clips.map((c) => c.id)); | 错：没有该过滤 |
| L142 | mcpExecutor.ts:425-428 |           else if (tool === "set_position") result = api.setPosition(args); | 对 |
| L142 | mcpExecutor.ts:429 |           else if (tool === "get_layout") result = await api.getLayout(args); | 对 |
| L142 | mcp-tools.mjs:228 |     description: "给卡片定位:把它的锚点放到画面上某个坐标,可选尺寸、缩放、旋转。**这是把任何卡片摆到任何位置的正道**——不再受卡片自带 position 档位(center/bottom/…)限制,不用为… | 对，已是改后文本 |
| L142 | mcp-tools.mjs:253 |     description: "把卡片放进画面上的一个矩形(两个对角点,顺序随意)。**要把卡放到「空的那一边」首选它**。mode 默认 fit:画布不动,整体缩放到刚好装进矩形、保持比例,按 align 对齐在矩形里(默… | 对，已是改后文本 |
| L142 | mcp-tools.mjs:269 |     description: "把卡片贴到画面的边或中心,带边距:h 是 left/center/right,v 是 top/center/bottom,只传一个另一个方向不动。锚点会跟着对齐方式走,缩放过的卡片贴的是可见框… | 对，已是改后文本 |
| L142 | mcp-tools.mjs:284 |     description: "在现有位置上微调:dx/dy 加像素(右、下为正),scaleBy 乘倍数(0.8 = 缩小两成),rotateBy 加角度(顺时针)。看完 look 觉得「再往左一点、再小一点」就用它,不用… | 对，已是改后文本 |
| L142 | mcp-tools.mjs:405 |     description: "读卡片的布局:local(存下来的框,没设过为 null 即铺满全屏)、world(算出来的画面绝对位置:锚点坐标、尺寸、box 是画布矩形、visualBox 是缩放旋转之后画布真正占的矩形… | 对，已是改后文本 |
| L142 | vite-plugin-frames.ts:88-90 |      * 和 `/playback` 一样**两边都能答**:编辑器进程手里也有 FramePipeline,不另开一层转发。 | 对 |
| L142 | vite-plugin-frames.ts:90 |     server.middlewares.use("/api/cards/layout", (req, res, next) => { | 对 |
| L142 | frame-pipeline.mjs:931-936 |   async layout(project, options = {}) { | 对 |
| L142 | capture-snapshot.mjs:39-50 |       await document.fonts.ready; | 对，fonts.ready 在 :40、钩子在 :42 |
| L142 | capture-snapshot.mjs:51 |     return await captureFrame(bakery, screenshot); | 对 |
| L205 | ?preview=legacy 唯一落点 | const LEGACY = typeof window !== "undefined" && new URLSearchParams(location.search).get("preview") === "legacy"; | 对，只有页面侧 |
| L316 | StageView.tsx:568 |     // 场景根就是 rootRef 指着的那个 [data-pc-scene] div(不走 document.querySelector:预渲染页上 #root 只是 display:none)。 | 对 |

### 4.2 前置接口

| 接口 | 这一步怎么用 | 前置定义在哪 | 判定 |
|---|---|---|---|
| `setProject(patch\|full, {reset})` | E0/E7：`front` 只更新项目；`back` 被掐断回 `'project'` | `src/render/stageRpc.ts:100`、实现 `src/StageView.tsx:295` | 给了且一致 |
| `setTime(tSec, opts)` | E0/E2/E3：暂停、拖动唯一入口；`probe` 走 K1 | `stageRpc.ts:101` / `:55-70`，实现 `StageView.tsx:342` | 给了且一致 |
| `render(tSec, opts)` | E0/E4：`back` 探针与续推 | `stageRpc.ts:102` / `:72-78`，实现 `StageView.tsx:394` | 给了且一致（`RenderAborted` 缺 `'role'`，E1 自己加，任务书已写明） |
| `hitTest(x, y)` | D3：点选 | `stageRpc.ts:103`，实现 `src/render/solid.ts:158`（`hitTest(root, x, y)`） | 给了且一致 |
| `rectsWithBounds(opts)` | D3/D4：选框、`contentBox` | `stageRpc.ts:104`、`solid.ts:253`（同步、`(root, opts)`）、选项 `solid.ts:240-248` | 给了且一致 |
| `size()` / `setProxy(on)` | E7：画幅、实体模式 | `stageRpc.ts:105` / `:106` | 给了且一致 |
| `setRole(role, { job })` + `BackJob` | D4 单飞队列：测量进场发 `('back', { job: 'catchup' })` | `stageRpc.ts:107`、`BackJob` `:20`、`SetRoleOptions` `:84-85`，实现 `StageView.tsx:501` | 给了且一致（`job: 'bake'` 现回 `unsupported`，L1 再开） |
| `setPlan({ plan, costs })` | E0：`front` 判 K3/K5 | `stageRpc.ts:109` | 给了（第 3 步只存不判，第 4 步补消费） |
| `play(fromSec)` / `pause(opts)` | K4/E0：播放与武装停 | `stageRpc.ts:111` / `:112`、`PauseReply` `:91-96`，占位 `StageView.tsx:517-522` | 给了但形状不符——`play` 是内联 `{ ok: boolean; reason?: string }`、`PauseReply` 的 `ok` 可选，任务书 L158 已点名 K4 落地时统一 |
| `setSuppressed` / `setStreamPlanes` | E7 第 5 条 | `stageRpc.ts:113` / `:114` | 给了（占位，第 4 步补实现） |
| `setScrubbing` / `setPlaying` / `setMediaT` | E3/E7 | `stageRpc.ts:115` / `:116` / `:117` | 给了（占位） |
| `setLocalHashes(hashes)` | E7：A1 换档 | `stageRpc.ts:118` | 给了（占位，消费者在第 5 步） |
| `setSnapshots(patch, opts)` | E7 第 4 条 / C4 | `stageRpc.ts:120`（`Record<string, string\|null>`、回 `bytes`） | 给了且一致 |
| 七种 `StageEvent` | E0：`frame`/`ended`/`settled`/`mediaReady`/`probe`/`demote`/`probe-frame` | `stageRpc.ts:123-130` | 给了且一致（只有 `probe-frame` 在发，`StageView.tsx:444`） |
| `postStageReady` + `HostCapabilities` | E1：跨源握手 | `stageRpc.ts:271` / `:22-31`、`detectHostCapabilities` `:277`，父页 `Preview.tsx:276` | 给了且一致 |
| `createStageRpc` / `serveStageRpc` | E1：每个 iframe 一个客户端 | `stageRpc.ts:169` / `:232`，`'detached'` 在 `:191` / `:218` | 给了且一致 |
| `stageBridge` 的 `setStageClient` / `frontStage` / `backRole` / `backStage` / `syncProject` / `pushedProject` | E1/D4：双舞台接线点 | `src/editor/stageBridge.ts:35` / `:43` / `:49` / `:55` / `:65` / `:87` | 给了且一致（现在只有 `front` 注册） |
| `solidApi` | D3(c)：导出页 window 面 | `solid.ts:267`（`isSolid, bounds, rects, hitTest, stageOrigin, canvasBox, canvasPaintedBox, rectsWithBounds`） | 给了且一致（不含 `clipWrapper`，任务书也没要求） |
| `renameSnapshotIds(html, clipId)` | E7 第 4 条的 `renamed` | `src/render/snapshotRename.ts:143` | 给了且一致（零生产调用点，E7 自己接线） |
| `freezeScene(root)` | K1 探针 / `__bfFreeze` | `src/render/snapshotFreeze.ts:43`，舞台页 `StageView.tsx:569-572` | 给了且一致 |
| `advanceToAsync(target, opts)` | E4/K3/K5 | `src/render/stageClock.ts:150`，opts 类型 `:54-70`（含 `yieldEvery` `:67`） | 给了且一致（任务书说"要加 `yieldEvery`"是过期描述，见 3.1） |
| `cardMountedAt` / `mountFrameOf` | E7 第 2 条 / `render(jump)` | `src/render/frameWindow.mjs:6` / `:12`，`CARD_MOUNT_LEAD` `:5` | 给了且一致 |
| `/api/cards/layout` | D4(b)：Agent 侧 `get_layout` | `server/vite-plugin-frames.ts:90`（两端都挂 `:88`）、`server/frame-pipeline.mjs:931` 借 `agent` lane | 给了且一致 |
| `captureSnapshot` 的 `afterFonts` + `screenshot:false` | D4(b) | `scripts/capture-snapshot.mjs:42` / `:49` | 给了且一致 |
| `videoLayersAt` / `nextVideoLayerAfter` | E7 第 1 条 | `src/kernel/project.ts:428` / `:460-467` | 给了但形状不符——`nextVideoLayerAfter` 还没有 `skip?` 谓词，E7 自己加（任务书已写明） |
| `planSlots` / `planSync` / `SlotClip` | E7 第 1 条的 `VideoTrack` | `src/editor/preview/mediaSync.ts:205` / `:98` / `:166` | 给了但落点不符——在编辑器目录下，见阻塞 2.2 |
| `targetTimeOf` / `filterOf` / `Slot` | E7 第 1 条的 `VideoTrack` | `src/editor/preview/MediaLayers.tsx:106` / `:115` / `:128` | 没给（不在任何搬运范围内），见阻塞 2.1 |

## 5. 对 user_pinned_goal.md 的建议

无。本步（E0～E7 / A4 / D3 / D4 / F2）没有和 pinned 交互 1～3、pinned 平台 1～6、pinned 架构 1～10、pinned 划分、pinned 渲染 1～10 冲突的地方；pinned 渲染 1（用户交互不允许卡顿、舞台渲染与交互异步）和 pinned 架构 8（store 跟随 t）在 E0 / E6 / E7 里都有对应落点。

## 6. 范围外顺带

- 路径缩写表（**L7**）里没有 `particles.tsx` / `odometer.tsx` / `checklist.tsx` / `hud.css` / `scene-3d.tsx` / `terminal-3d.tsx`，而 L158 / L170 / L171 用裸名引用它们；实际都在 `src/cards/native/`。本轮的 agy 就因此先去 `src/render/scene-3d.tsx` 扑了个空。建议把这几张卡补进 L7。
- `src/editor/Preview.tsx:605` 的注释里用了 2026-09-17 术语约定禁用的那个旧词（那句是「代理只活在 3D 视图里,而且只是 …… 没跟上时的过渡」，原词按约定应写「预渲染」；代码标识符 `bake*` 不在禁用范围，注释不算标识符）。顺手可改。任务书正文和本报告都没有出现那个字。
- L16（顺序表第 3 步）说要顺手改 `probe-card-costs.mjs:16 / :19 / :23` 注释里的旧行号：`:16` 确实过期（引 `StageView.tsx:183`，现为 `:228`）；`:19` 引的 `ExportView.tsx:108-118` 仍然对得上；`:23` 只写了 `StageView.tsx` 的 `budgetMs`、没有行号可改。

---

## manager 核对记录

**核对人**：manager（Claude），独立于 agy，用自己的 Read / Grep 对工作区 HEAD = b5c65dc 核（`git rev-parse --short HEAD` = b5c65dc，`git status --porcelain` 只有 `.claude/worktrees/` 和 `nul` 两个未跟踪项，`src/` / `server/` / `scripts/` 与 HEAD 一致）。

**一、agy 产出的处置**

| agy 原条目 | 处置 | 理由 |
|---|---|---|
| 格式（六个标题、4.1/4.2 两张表）| 保留 | 第二轮返工后合规 |
| 阻塞 2.1（`targetTimeOf` / `filterOf` 漏搬）| 保留并补强 | manager 独立复核成立，并补上 agy 漏掉的 `interface Slot`（`:128`）和 `AudioLayer` 也在用 `targetTimeOf`（`:319`）这两点 |
| 阻塞 2.2（`mediaSync.ts` 跨层）| 保留并补证据行 | 成立，补 `MediaLayers.tsx:10` 和 `:192` 的调用点 |
| 阻塞 2.3（`FrameScene` props 清单漏三个）| 保留并补证据行 | 成立，补 `FrameScene.tsx:68` 的现签名 |
| 阻塞 2.4（`right/index.tsx:96-98` 行号错）| 降级为非阻塞 3.3 | 行号错会浪费时间，但执行者进到那个函数就能看见 `syncProject` 在 `:94`，不至于写错 |
| 非阻塞 3.2（D4(a) 的五条 description / 四个写工具"已落地却写成待办"）| **删除** | 违反纪律：L142 开头就写「(a)、(b) 的端点与页面侧已落地（b5c65dc：…五条 description…）」，后面的祈使句是落地口径不是待办 |
| 非阻塞 3.3（称 `setTime` 的 `advanceTo` 例外被写成占位）| **删除** | 误读：L154 的「只有签名或占位」点名的是 `play` / `pause` / 六种事件 / `setPlan` / `setSuppressed` / `setStreamPlanes` / `setSnapshots` / `setLocalHashes` / `setScrubbing` / `setPlaying` / `setMediaT`，`setTime` 不在其中；同一行还明写 `setTime` 的 `path` 回包已落地 |
| 结论「不能」| 改为「有条件能」 | 三条阻塞都是任务书文字层面的补漏，改一两句话即可，不是设计层面的死结 |
| 4.1 表（agy 版 94 行）| **整张作废重做** | 抽核发现约七成第三列是凭记忆重建的假文本，详见下表 |
| 4.2 表（agy 版）| 重做 | 行号有错（把 `advanceToAsync` 记成 `stageClock.ts:176`，实为 `:150`），且全列"给了且一致"没有区分占位 / 形状不符 |

**二、agy 4.1 表编造证据的抽核样本**（manager 逐行比对，下列第三列均与 HEAD 实际内容不符）

| agy 写的 | HEAD 实际 |
|---|---|
| `solid.ts:158` → `hitTest(x: number, y: number, root: SceneRoot)` | `export function hitTest(root: SceneRoot, x: number, y: number): StageHit \| null {`（参数顺序相反）|
| `solid.ts:253` → `export async function rectsWithBounds(opts, root): Promise<...>` | `export function rectsWithBounds(root: SceneRoot, opts: RectsWithBoundsOptions = { pixels: "none" }): RectWithBounds[] {`（不是 async、参数序相反）|
| `solid.ts:267` → `solidApi` 含 `clipWrapper` | 实际导出对象里没有 `clipWrapper` |
| `scene-3d.tsx:146-151` → 「不用 preserveDrawingBuffer」 | 注释说的是 `preserveDrawingBuffer: true` **不是可有可无的**（语义相反）|
| `pinAnimations.ts:26` → `for (const a of anims) if (...) syncTo(a, time);` | `        if (a.playState === "finished" \|\| a.playState === "idle") continue;` |
| `StageView.tsx:444` / `:574` → `win.postMessage(...)` | `postStageEvent({ type: "probe-frame", ... })` / `postStageReady(detectHostCapabilities());` |
| `mcpExecutor.ts:425-428` / `:429` → `case "align":` / `case "get_layout":` | `else if (tool === "align") result = api.align(args);` / `else if (tool === "get_layout") result = await api.getLayout(args);` |
| `mcp-tools.mjs:253` / `:269` / `:284` / `:405` 四条 description 开头 | 四条开头实际是「把卡片放进画面上的一个矩形(两个对角点,顺序随意)。」「把卡片贴到画面的边或中心,带边距:」「在现有位置上微调:dx/dy 加像素(右、下为正)」「读卡片的布局:local(存下来的框,没设过为 null 即铺满全屏)」|
| `frame-pipeline.mjs:931` → `export async function layout(lane, session, localRev, t, clipIds)` | `  async layout(project, options = {}) {`（是类方法）|
| 另有 `Stage.tsx:122/133/169/172/175/177/191`、`Preview.tsx:323/332/352/358/379-380`、`stageBridge.ts:76-77`、`contentBox.ts:100-101`、`stageClock.ts:167-169/176-186`、`AnimClock.tsx:9/30`、`ExportView.tsx:147-156/276`、`capture-snapshot.mjs:39-50`、`project.ts:427-429`、`vite-plugin-frames.ts:88-90`、`stageRpc.ts:191/218`、`StageView.tsx:394/517-522`、`FrameScene.tsx:76` | 全部对不上 |

**三、manager 重做 4.1 的方法**：把 130 条 `(任务书行, 引用, 文件, 行号, 判定)` 写成数据，用一个只读脚本按行号直接从工作区读出该行原文填第三列，全程不经人手转述，所以不存在编造。脚本与数据留在 `C:\Users\admin\AppData\Local\Temp\claude\C--Users-admin-Documents-PromptCut\13871018-a412-468d-b617-644a68f3a10b\scratchpad\r75-mgr03\`（`rows_all.json` / `mkrows.mjs` / `table41.md`），可复现。

**四、第 3 节的 15 条与第 6 节，均由 manager 独立走查得出，每条都附了 HEAD 上的实际行内容**（agy 第二轮返工因配额 429 中断，没能补做）。

**五、agy 未跑完的部分**：第二轮返工要求它重做 4.1 并补 (c)(e)(f) 三个扫查方向——这些已由 manager 补做完毕，不再欠。
