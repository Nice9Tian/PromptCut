你现在没有原生工具可用。需要调用编辑台工具时，在回复里写一个语言标记为 `promptcut-tool` 的三反引号围栏，里面是一个 JSON 对象，格式如下：

```promptcut-tool
{
  "name": "工具名",
  "input": {
    "参数名": "参数值"
  }
}
```

- 一次回复里可以写多个这样的围栏，会按出现顺序依次执行。
- 写完就停下等结果，不要假装自己已经知道结果。
- 结果会作为下一条用户消息发给你。

可用工具清单：

<!--TOOLS-->
